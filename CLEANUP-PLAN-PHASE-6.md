# تدقيق وخطة المرحلة 6 — طبقة الـJS

> **الحالة: تدقيق وخطة فقط — لم يُعدَّل أي ملف. بانتظار موافقتك.**
> التاريخ: 2026-08-25 · سابقه: [CLEANUP-REPORT-PHASE-5.md](CLEANUP-REPORT-PHASE-5.md)

---

## 0. المنهج

ملف التسليم يقول عن هذه المرحلة: «لم يُفحص بعمق بعد — ابدأ بتدقيق قبل أي خطة».
اتّبعتُ ذلك حرفياً، وله سبب مثبَت: في المرحلة 5 تبيّن أن **ثلاثة من خمسة بنود** في
قائمة الـpartials المقترحة غير صحيحة عند فحص الكود، وأن أحدها كان سيحذف بادجاً ظاهراً
للمستخدم. كل بند أدناه مقيس لا مُقترَح.

**وأصحّح خطأً في تقريري السابق:** كتبتُ في «ما يليه» أن `js/features/account.js` غير
محمَّل من أي مكان وربما يكون كوداً ميتاً. **غير صحيح** — يحمّله
`MyInfoController` عبر `extraScripts` (السطر 54). فحصتُ الـ35 ملفاً كلها الآن: **لا يوجد
ملف JS واحد غير محمَّل.** بنيتُ استنتاجي وقتها على قائمة الفوتر وحدها ونسيت آلية
`extraScripts` التي أنا نفسي استعملتها في تلك المرحلة.

---

## 1. حالة الطبقة بالأرقام

| | |
|---|---:|
| ملفات JS | 35 |
| أسطر | 5,584 |
| ملفات غير محمَّلة (كود ميت) | **0** |
| `console.log` متبقٍ في الإنتاج | 2 |
| `var` مقابل `const`/`let` | 27 مقابل 762 |
| ملفات فيها `'use strict'` | 7 من 35 |

### الأكبر

| الملف | أسطر |
|---|---:|
| `admin/products.js` | 369 |
| `features/auth.js` | 336 |
| `features/notifications.js` | 330 |
| `core/ui.js` | 325 |
| `features/products-catalog.js` | 321 |
| `admin/category-picker.js` | 299 |
| `admin/branding.js` | 282 |

---

## 2. ما وجدتُه — مرتّباً بالأثر

### 2.1 🔴 `fixImagePath` في JS دالة فارغة تُرجع مدخلها

```js
// js/core/utils.js
function fixImagePath(imgPath) {
    return imgPath || '';
}
```

اسم يَعِد بسلوك لا يفعله. تعليقها يقول «إرجاع مسار الصورة المعالج من PHP» — أي أنها
تفترض أن الخادم عالج المسار أصلاً، فهي مجرّد حارس ضد `null`.

**لماذا يهمّ:** المقابل في PHP يفعل شيئاً حقيقياً — يضيف بادئة `images/` لأي مسار بلا
شرطة مائلة، ويمرّر الروابط المطلقة كما هي، ويعيد صورة بديلة عند الفراغ. وقد **انفجر
هذا بالضبط** في المرحلة 5: محرّر السلايدر بنى المسار بيده فأعطى `/airpods.jpg` بدل
`/images/airpods.jpg` — ست صور 404 وأدمن لا يرى أي منتج يشير إليه كل سلايد.

الثلاثة الذين يستدعونها اليوم يمرّرون مسارات **مُصلَحة مسبقاً من PHP**، فلا عطل قائم.
لكن الدالة فخّ: اسمها يشجّع على الاعتماد عليها، وهي لا تحمي.

**الخيارات:** (أ) نقل منطق `fixImagePath` الحقيقي إلى JS ليكون مصدراً واحداً في
الطرفين، أو (ب) إعادة تسميتها إلى ما تفعله فعلاً (`imagePathOrEmpty`) وتوثيق أن إصلاح
المسار مسؤولية الخادم دائماً.

### 2.2 🔴 قاعدة بادج المخزون مكتوبة **ثلاث مرات** في لغتين

| الموضع | 0 | 1–50 | > 50 |
|---|---|---|---|
| `app/helpers/stock_badge_helper.php` | Out of Stock / `bg-danger` | Limited (N left) / `bg-warning text-dark` | حسب الوسيط: أخضر أو لا شيء |
| `js/features/wishlist.js::getStockBadgeJs` | نفسه | نفسه | **لا شيء** |
| `js/features/product-details.js` (سطر 101، مضمّن بلا دالة) | نفسه | نفسه | **In Stock (N) / `bg-success`** |

النصوص والعتبات والأصناف متطابقة اليوم **بالمصادفة**. العتبة 50 مكتوبة في ثلاثة أماكن،
وتغييرها يتطلب تعديل ثلاثة ملفات بلغتين. ولاحظ أن النسختين في JS تختلفان عن بعضهما
في الفرع الثالث — وهو بالضبط الفرق الذي اكتشفته في المرحلة 5 بين PHP والـview.

### 2.3 🟠 نصف مواضع POST تتجاوز شبكة أمان CSRF القائمة

المشروع يملك `fetchWithCsrfRetry` في `js/core/csrf.js`: تلتقط فشل التوكن، تجلب توكناً
جديداً من `/auth/csrf` أو `/admin/csrf`، وتعيد الطلب مرة واحدة. المرحلة 2 وثّقت أنها
كانت معطّلة صامتة لفورم ينقصه الحقل، وأُصلحت.

**لكن 22 ملفاً ترسل POST، و13 منها لا تستدعيها إطلاقاً**، وأربعة تستدعيها جزئياً:

| الملف | POST | يستعمل retry |
|---|---:|---|
| `admin/products.js` | 4 | **صفر** |
| `admin/category-picker.js` | 4 | **صفر** |
| `admin/users.js` | 3 | **صفر** |
| `features/account.js` | 3 | **صفر** |
| `admin/my-info.js` · `admin/backup.js` | 2 | **صفر** |
| `admin/admin-notifications.js` | 4 | 1 فقط |
| `admin/manage-admins.js` | 3 | 1 فقط |
| `admin/admins.js` | 2 | 1 فقط |
| … وسبعة أخرى بـPOST واحد بلا retry | | |

**الأثر:** أي دورة يتغيّر فيها التوكن (بعد فشل دخول، أو تجديد جلسة) تُسقط هذه الطلبات
برسالة «Invalid CSRF token» بدل إعادة المحاولة الصامتة التي بُنيت لأجلها.

**عائق حقيقي يجب حلّه أولاً:** `fetchWithCsrfRetry` تعيد بناء الجسم عند إعادة المحاولة
لـ`FormData` و`urlencoded` **فقط**. وصفحة الدفع و`admin/my-info` ترسلان JSON — ولهذا
لكلٍّ منهما دالة `fetch` خاصة (وثّقتُ ذلك في المرحلة 5). توحيد الاستعمال يتطلب توسيع
الدالة الأصلية لتدعم أجسام JSON.

### 2.4 🟠 ثلاثة ملفات تربط نفس زرَّي الإشعارات، وأحدها لا يفعل شيئاً

`#adminNotifMarkAll` و`#adminNotifDeleteAll` في فوتر الأدمن يربطهما:

1. `admin/admin-notifications.js` — التنفيذ الحقيقي
2. `admin/admin-layout/admin-footer.js` — **كعبان فارغان** لا يفعلان إلا
   `console.log('TODO: …')`
3. `features/notifications.js` — يقرأ نفس المعرّفين رغم أنه **ملف المتجر** ولا
   يُحمَّل في الأدمن إطلاقاً (تحقّقت: صفر إشارة إليه في فوتر الأدمن)

الضغط على الزر في اللوحة يشغّل المعالج الحقيقي **و** الكعب معاً. والـ`console.log`ان
هما الوحيدان المتبقيان في الإنتاج كله.

والموضع الثالث دليل على أن ملفَي الإشعارات (المتجر 330 سطراً واللوحة 188) نُسخ أحدهما
من الآخر — كاشف التكرار وجد بينهما ثلاث كتل مشتركة.

### 2.5 🟡 `wishlist.js` يُحمَّل **مرتين** على صفحة المفضّلة

فوتر المتجر يحمّله على كل صفحة، و`WishlistController::index` يحمّله مجدداً عبر
`extraScripts`. قِستُه في المتصفح: **وسمان لنفس الملف** (الطلب الشبكي واحد لأن
المتصفح يخزّنه، لكن التنفيذ يقع مرتين).

**لماذا لا يظهر عطل:** العرض يستبدل `innerHTML` بالكامل، والمستمعات تُربَط بعناصر
ينشئها العرض — فالتنفيذ الثاني يمسح أثر الأول. هدرٌ لا كسر: 266 سطراً تُحلَّل وتُنفَّذ
مرتين، ومعالج `DOMContentLoaded` يعمل مرتين.

الإصلاح سطر واحد: حذف الوسم من `extraScripts`.

### 2.6 🟢 اتهام «`innerHTML` ببيانات من الخادم» — غير صحيح إلى حدٍّ كبير

هذا البند في ملف التسليم كان الأكثر إثارة للقلق، وقياسه يبدّده:

- **الملفات الخمسة الأكبر في اللوحة** (`products` · `orders` · `users` ·
  `manage-admins` · `admins`) وكذلك `features/account.js`: **صفر `innerHTML`**. كلها
  تعمل على ماركب مُولَّد من الخادم.
- الملفات التي تبني HTML فعلاً وتُدخل بيانات (`cart` · `wishlist` ·
  `products-catalog` · `notifications` · `admin-notifications` · `branding` ·
  `category-picker`) **تستعمل `escHtml` من `core/utils.js`** أصلاً.
- بقية مواضع `innerHTML` (`auth.js` ثمانية، `backup.js` اثنان، `site-settings.js`
  ثلاثة) كلها **نصوص حرفية ثابتة**: تسميات أزرار ودوّامات تحميل. لا بيانات ولا خطر.

**التوصية: لا عمل هنا.** أذكره لأن الخطة رفعته كقلق، والقياس يقول إنه ليس كذلك.

### 2.7 🟢 المؤقتات نظيفة إلى حدٍّ مطمئن

سبعة `setInterval`، خمسة منها تُنظَّف بـ`clearInterval` عند الشرط الصحيح. الاثنان
الباقيان استطلاع دوري كل 30 ثانية (`admin-notifications` و`features/notifications`)
يعيشان بعمر الصفحة — وهذا سلوك طبيعي في تطبيق متعدّد الصفحات، وتفريغ الصفحة يقتلهما.
**لا تسريب.**

### 2.8 🟢 تكرار صغير: ماركب بطاقة التحميل الهيكلية

`products-catalog.js` و`wishlist.js` يتشاركان ثماني أسطر من ماركب
`skeleton-card`. حقيقي لكنه صغير — مرشّح لدالة في `core/ui.js` إن لُمس الملفان لسبب آخر.

---

## 3. الخطة المقترحة

### المرحلة 6أ — مصدر واحد لقواعد العرض *(نصف يوم · مخاطرة منخفضة)*

1. **بادج المخزون**: نقطة واحدة في JS (`core/ui.js` أو `core/product-rules.js`)
   بنفس توقيع PHP: `stockBadge(stock, showInStock)`. يستعملها `wishlist.js`
   و`product-details.js`. العتبة 50 تبقى في مكانين لا ثلاثة — PHP وJS — ويوثَّق
   الارتباط في الملفين.
2. **`fixImagePath`**: قرارك بين نقل المنطق الحقيقي إلى JS، أو إعادة التسمية لتعكس
   ما تفعله. **أوصي بالثاني**: كل البيانات تصل الـJS من PHP أصلاً، والتوحيد الحقيقي
   هو ألّا يبني أحد المسارات في المتصفح — وهذا ما فعلناه في إصلاح السلايدر.

**التحقق:** اختبار وحدة يقارن مخرجات JS بمخرجات PHP على القيم نفسها (0 · 1 · 50 · 51
· 9999)، ومقارنة بصرية لصفحة المفضّلة وصفحة تفاصيل المنتج.

### المرحلة 6ب — توحيد مسار CSRF *(يوم · مخاطرة متوسطة)*

1. **توسيع `fetchWithCsrfRetry` لتدعم أجسام JSON** — بلا هذا لا يمكن توحيد
   صفحة الدفع و`admin/my-info`. هذه الخطوة أولاً وإلا فالباقي نصف حل.
2. تحويل الـ13 ملفاً بالتدريج، ملفاً ملفاً مع اختبار HTTP حقيقي لكل نقطة POST.

**⚠️ هذه أخطر خطوة في المرحلة**: تمسّ كل نقاط الكتابة في التطبيق. أقترح تنفيذها
**على دفعات** مع commit وتحقق بعد كل ملف، لا دفعة واحدة.

### المرحلة 6ج — تنظيف *(ساعتان · مخاطرة منخفضة)*

1. حذف الكعبين الفارغين من `admin-footer.js` (والـ`console.log`ين معهما).
2. حذف إشارات معرّفات الأدمن من `features/notifications.js` — ميتة في سياقها.
3. حذف التحميل المزدوج لـ`wishlist.js`.
4. الـ27 `var` المتبقية → `const`/`let` حيث يكون التحويل بلا أثر (`var` في نطاق
   دالة مقابل `let` في نطاق كتلة ليس تحويلاً آلياً آمناً دائماً — سأفحص كل موضع).

### ما لا أنوي فعله — وأسبابه

| البند | لماذا لا |
|---|---|
| مراجعة `innerHTML` للـXSS | قِسته: الملفات التي تُدخل بيانات تُهرّب أصلاً، والباقي نصوص ثابتة (القسم 2.6) |
| تنظيف المؤقتات | نظيفة أصلاً؛ الاثنان غير المنظَّفين استطلاع بعمر الصفحة (القسم 2.7) |
| توحيد ملفَي الإشعارات (330 + 188 سطراً) | متشابهان لا متطابقان: جلستان مختلفتان، ونقطتان مختلفتان، وماركب مختلف. الدمج يبني تجريداً بوسائط أكثر من الوفر — نفس الحكم الذي أدّى لإلغاء طبقة الـvalidator في المرحلة 3 |
| تقسيم `admin/products.js` (369 سطراً) | لم أجد فيه تكراراً ولا منطقاً مبعثراً — طوله من عدد الأزرار التي يخدمها. **لا تطارد الأرقام** |
| إضافة `'use strict'` للـ28 الباقية | تغيير سلوك محتمل في كل ملف مقابل فائدة نظرية. أوصي بتركه |

---

## 4. ترتيب مقترح للتنفيذ

| # | العمل | مخاطرة | الأثر |
|---|---|---|---|
| 1 | 6ج التنظيف | منخفضة | يزيل تشويشاً يعيق فهم الباقي |
| 2 | 6أ مصدر واحد لقواعد العرض | منخفضة | يمنع تكرار عطل السلايدر |
| 3 | 6ب-1 توسيع `fetchWithCsrfRetry` لـJSON | متوسطة | يفتح الباب للباقي |
| 4 | 6ب-2 تحويل الملفات على دفعات | **عالية** | يغلق أكبر فجوة قائمة |

**توقّفتُ هنا كما طلبت.** لا شيء نُفِّذ.

---

## 5. أسئلة تحتاج قرارك

1. **`fixImagePath`**: أنقل المنطق إلى JS أم أعيد تسميتها لتصف ما تفعله؟ (أوصي
   بالثاني)
2. **6ب**: أبدأها أصلاً؟ إنها أخطر ما في المرحلة وتمسّ كل نقاط الكتابة. البديل
   المحافظ: توسيع الدالة فقط (خطوة 3) وترك التحويل لتذكرة مستقلة.
3. **الترتيب**: أبدأ بالترتيب المقترح أعلاه أم عندك أولوية أخرى؟
