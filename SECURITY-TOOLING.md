# أدوات الفحص الأمني — Cairo Store

> **الحالة: ثلاث أدوات فعّالة ومُثبَتة بضوابط سالبة.**
> التاريخ: 2026-08-26 · الإعدادات: [.pre-commit-config.yaml](.pre-commit-config.yaml)

---

## 1. الخلاصة

| الأداة | المرحلة | الزمن | ماذا تمنع |
|---|---|---:|---|
| **gitleaks** 8.30.1 | `pre-commit` | ~19 ث | أسرار في التغيير المُدرَج |
| **semgrep** 1.174.0 | `pre-push` | ~141 ث | ثغرات كود في المستودع كله |
| **trivy** 0.74.0 | تلقائي عند تغيّر `composer.lock` · ويدوي | ~2 ث | ثغرات CVE في اعتماديات composer |

**المبدأ الحاكم:** سرّ يدخل التاريخ لا يُمحى بسهولة، فيُمنع عند لحظة
الالتزام. وثغرة كود تُصلَح بـcommit تالٍ، فيكفي منعها قبل مغادرة الجهاز.

---

## 2. التركيب على جهاز جديد

```bash
winget install Gitleaks.Gitleaks
winget install AquaSecurity.Trivy
pip install semgrep
pre-commit install --hook-type pre-commit --hook-type pre-push
```

**السطر الأخير لازم.** بلا `--hook-type pre-push` يبقى
`.git/hooks/pre-push` غير موجود ولا يعمل semgrep إطلاقاً — والإعداد وحده
لا يكفي.

> ⚠️ **بعد أي تثبيت جديد أغلق الطرفية وافتحها.** الأدوات تُنادى بأسمائها
> المجرّدة فيجب أن تكون على `PATH` الذي يراه git، والعملية القائمة ترث
> `PATH` قديماً ولا ترى التثبيت الجديد. هذا بالضبط ما جعل gitleaks يبدو
> «غير مثبَّت» وهو مثبَّت.

### التشغيل اليدوي

```bash
pre-commit run trivy --hook-stage manual          # ثغرات الاعتماديات
pre-commit run --hook-stage pre-push --all-files  # semgrep على كل شيء
semgrep --config auto .                           # semgrep مباشرة
```

---

## 3. لماذا هذا التقسيم — بالقياس لا بالتقدير

محاولة commit واحدة بالأداتين معاً كلّفت **174 ثانية**. وظننتُ أن الحلّ
حصر semgrep بالملفات المُدرَجة — **والقياس نفى ذلك**:

| ما فُحص بـsemgrep | الزمن |
|---|---:|
| المستودع كله (264 ملفاً) | ~90 ث |
| **ملف واحد** | **78 ث** |

الفرق 12 ثانية. أي أن التكلفة في **تحميل القواعد من سجلّ semgrep** لا في
الفحص، وتصغير النطاق كان سيوفّر فتاتاً. فالحلّ نقل الأداة إلى لحظة تحتمل
تكلفتها لا تقليص ما تفحصه.

**الحصيلة:** الـcommit من 174 ثانية إلى **19**، وsemgrep يفحص المستودع
كله عند الدفع — تغطية **أوسع** من فحص المُدرَج، لا أضيق.

---

## 3-ب. تغطية gitleaks — مقيسة على ثمانية أنواع

«الأداة تعمل» لا يقول شيئاً عن **ماذا** تمسك. فاختُبرت القواعد
الافتراضية على ثمانية أنواع:

| النوع | افتراضياً |
|---|---|
| AWS · مفتاح RSA خاص · Slack | ✅ |
| GitHub · Google API · Stripe | ✅ |
| **سلسلة اتصال قاعدة بيانات** | ⚠️ يمرّ |
| **كلمة سر مكتوبة حرفياً** | ⚠️ يمرّ |

> **اعتراف منهجي:** أول تشغيل أعطى «3 مُسك · 5 مرّ». وقبل أن أستنتج
> ضعف التغطية تحقّقت من قيمي المختلَقة، فتبيّن أنها **مشوَّهة**: توكن
> GitHub 35 محرفاً بدل 36، ومفتاح Google 36 بدل 35، وStripe بأحرف
> كبيرة تخالف قاعدته. بأطوال مضبوطة برمجياً: **6/6**. الخلل كان في
> الاختبار لا في الأداة.

والنوعان الباقيان يخصّان هذا المشروع تحديداً — `.env` يحمل
`DB_PASSWORD`. فأُضيف [.gitleaks.toml](.gitleaks.toml) بقاعدتين:

| القاعدة | تمسك |
|---|---|
| `cairo-hardcoded-password` | `password = "Xk7#mQp2vLzR9wTy"` |
| `cairo-db-connection-string` | `mysql://user:Pass@host/db` |

**كُتبت بعد مسح المستودع لا قبله.** قاعدة «كلمة سر» سيئة السمعة
بالإنذارات الكاذبة، وقاعدة تُنذر كذباً تُعطّل كل commit فتُحذف بعد
يومين. والمسح أظهر أن كل ما يشبه سرّاً في الكود هو **تسلسل متغيّرات**
يحوي مسافات (`'password=' . $q(DB_PASS)`)، فاشتُرطت القيمة بلا أي
مسافة و`entropy = 3.0` — وهذا وحده أسقط كل الإنذارات.

⚠️ **لا تحذف `useDefault = true`.** بضابط سالب: بتحويله إلى `false`
**مرّ مفتاح AWS** — السطر هو ما يحفظ القواعد المئة الافتراضية.

---

## 3-ج. لماذا لم أوسّع قواعد semgrep — قياس رفض فرضيتي

بدت تغطية PHP ضعيفة (**38 قاعدة** على 129 ملفاً، مقابل 153 قاعدة JS
على 34 ملفاً)، فاقترحتُ استبدال `--config auto` بحزم صريحة. **والقياس
نفى الفرضية:**

| الإعداد | قواعد كلية | قواعد PHP | النتائج |
|---|---:|---:|---:|
| `--config auto` | **1074** | **38** | 0 |
| `p/php` + `p/security-audit` + `p/owasp-top-ten` + `p/secrets` | 726 | 23 | 3 |

الحزم الصريحة فيها قواعد **أقل** لا أكثر. والنتائج الثلاث التي
أضافتها **كلها إنذارات كاذبة** من قاعدة واحدة (`echoed-request`) على
نمط `echo json_encode(...)`:

```php
header('Content-Type: application/json; charset=utf-8');   // ← قبله بأسطر
echo json_encode(['success' => true, 'products' => $products]);
```

الترويسة مؤكَّدة **حيّاً** على النقطة الحقيقية:
`HTTP 200 · Content-Type: application/json` — فالمتصفح لا ينفّذ HTML
من هذا الرد.

**والأسوأ أن الإصلاح المقترح يُفسد الرد.** semgrep يعرض autofix هو
`htmlentities(json_encode(...))`، ومُقاساً:

```
الأصلي          : {"success":true,"name":"قميص \"أزرق\""}
بعد htmlentities: {&quot;success&quot;:true,…}
json_decode بعده: null   ← الرد لم يعد JSON
```

**القرار: لا تغيير.** `--config auto` أوسع تغطية وأنظف نتيجة.

> هذا القياس موثَّق هنا كي لا يُعاد. الفرضية كانت معقولة والنتيجة
> عكسها، وهذا سبب القياس قبل التنفيذ.

### تصحيح: توصية سحبتُها

كانت هذه الفقرة تقول إن «الطريق الحقيقي لرفع تغطية PHP هو
`semgrep login` الذي يفتح 1856 قاعدة». **سُحبت التوصية**، ولثلاثة
أسباب:

**1. الرقم إعلان لا قياس.** مصدره سطر في آخر مخرَج semgrep نفسه:

```
💎 Missed out on 1856 pro rules since you aren't logged in!
```

نُقل كأنه فائدة مقيسة. ولم يُفحص كم منها يخصّ PHP، ولا هل تجد شيئاً
في هذا الكود أصلاً.

**2. الدليل الوحيد المتاح يقول العكس.** آخر مرة زيدت القواعد على
هذا المستودع (الجدول أعلاه) كانت الحصيلة ثلاث نتائج **كلها كاذبة**
وإصلاحاً مقترحاً **يُفسد الرد**. فلا أساس لافتراض أن 1856 قاعدة
أخرى ستتصرّف على نحو مختلف.

**3. ثمن لم يُذكر.** `semgrep login` وصفه في الأداة: «Obtain and
save credentials for **semgrep.dev**» — أي ربط بوابة محلية بحساب
وشبكة خارجيين. وهذا بالضبط ما عطّل العمل مرتين في هذا المشروع:
gitleaks حين احتاج Go، وsemgrep حين احتاج 141 ثانية لجلب القواعد.
والمشروع تجاري خاص، فربط فحصه بحساب طرف ثالث قرار له وجه يتعلّق
بالبيانات لا يُمرَّر في سطر عابر.

**والبديل في القسم 3-د.**

---

## 3-د. البديل: قواعد محلية تُشفّر أعطال هذا المشروع

أخطر أربعة أعطال هنا **لم تكشفها قاعدة عامة قط**. فبدل استيراد 1856
قاعدة عامة، كُتبت أربع قواعد في [.semgrep/cairo-store.yml](.semgrep/cairo-store.yml)
تُشفّر ما أخطأ فيه هذا المشروع فعلاً.

**المعيار صريح: قاعدة لا تُطلق على عطل وقع فعلاً لا تُضاف.** فكل
واحدة مُثبَتة على النسخة المعطوبة المستخرَجة من التاريخ بـ
`git show <commit>^:<path>`:

| القاعدة | العطل التاريخي | تُطلق |
|---|---|---:|
| `cairo-raw-die-in-controller` | `BackupController` قبل `1f9d364` | 3 |
| `cairo-unlink-unvalidated-path` | `AdminProductsController` قبل `2acfac5` | 2 |
| `cairo-execute-then-return-true` | `AdminProductModel` قبل `d033e0e` | 1 |
| `cairo-bare-fetch-post` | `checkout.js` قبل `8ca637e` | 1 |

### القاعدة الرابعة كادت تكرّر الغفلة نفسها

أول صياغة بحثت عن `method: 'POST'` فلم تُطلق على `checkout.js`
التاريخي — لأنه يستعمل الشكل المختصر `method,`، **وهو بالضبط ما جعله
يفلت من جرد المرحلة 6**. قاعدة تغفل هذا الشكل تعيد إنتاج الغفلة.

### والقاعدة الثالثة وجدت عطلاً لم يكن معروفاً

`AdminProductModel::update` تُرجع `true` بلا شرط بعد `UPDATE` — **نفس
عطل `delete` الذي أُصلح في البند 7.4**، في دالة أخرى. والكنترولر يكتب
سجل تدقيق وإشعاراً بناءً على ذلك الـ`true`.

⚠️ **ولهذا القواعد على `manual` مؤقّتاً.** وضعها على `pre-push` قبل
إغلاق هذه النتيجة يعني حجب كل دفع — أي بوابة معطوبة يُلتفّ حولها بعد
يومين. تُرفَع فور إصلاحها.

    pre-commit run semgrep-local --hook-stage manual

### وقياس كذّب ادّعائي

كتبتُ أن القواعد المحلية «تكلفتها ثوانٍ لا دقائق» لأنها بلا شبكة.
القياس: **112 ثانية** مقابل 141 للفحص الكامل. تكلفة semgrep في **بدء
التشغيل** لا في عدد القواعد — ملف واحد بـauto يكلّف 78 ثانية
والمستودع كله 90. **المكسب دقّة الاستهداف لا السرعة**، وبلا تبعية
شبكة أو حساب.

---

## 4. ثلاثة أعطال كانت في الإعدادات — وكيف تُكتشف مثلها

### 4.1 🔴 gitleaks كان يفحص غير المُدرَج

```
entry: gitleaks git --pre-commit --redact -v      ← بلا --staged
```

`--pre-commit` وحدها تفحص `git diff` — أي تغييرات العمل **غير** المُدرَجة.
والمُدرَج هو ما يدخل التاريخ.

| الأمر | على سرّ مُدرَج |
|---|---|
| بلا `--staged` | «no leaks found» · **كود 0** ← يسمح |
| مع `--staged` | **كود 1** ← يمنع |

**والأخطر:** السرّ لم ينفلت لأن semgrep أمسكه بالصدفة بقاعدة
`detected-aws-access-key-id-value`. أي أن الحماية كانت تتّكئ على **تداخل
عرضي** بين أداتين، وأي سرّ بصيغة يعرفها gitleaks ولا تغطّيها قواعد
semgrep العامة كان يمرّ بصمت.

### 4.2 🟠 خطّاف يمنع العمل بلا أن يفحص

الإعداد الأول كان يشير إلى مستودع gitleaks البعيد، وخطّافه الرسمي
`language: golang` — فيطلب من pre-commit أن **يبني** الملف التنفيذي بـGo.
وGo غير مثبَّت، فكان كل commit يُرفض بـ`Executable gitleaks not found`.

**خطّاف أمني يوقف العمل ولا يفحص سطراً واحداً هو أسوأ من غيابه**: يعطي
إحساساً بالحماية بلا حماية. الحلّ: `repo: local` و`language: system`
ينادي الملف المثبَّت مباشرة — بلا Go ولا بناء ولا شبكة.

### 4.3 🟡 تثبيت يفحص العدم

gitleaks كان يعمل في المرحلتين (السلوك الافتراضي). وعند `pre-push` لا
يوجد فهرس، فكان يفحص **27 بايتاً** ثم يطبع `Passed` — تسع عشرة ثانية على
فحص العدم، وطمأنينة بلا سبب. حُصر في `pre-commit`.

### الدرس المشترك

في الحالات الثلاث بدا الفحص سليماً وهو ليس كذلك. **لا يكفي أن يمرّ
الخطّاف أو أن يتوقف عن الفشل** — يجب إثبات أنه يستطيع المنع فعلاً.

---

## 5. الضوابط السالبة — لكل أداة

خطّاف لا يمكن أن يمنع شيئاً ليس خطّافاً. لذلك لكل أداة اختبار يثبت
الاتجاهين، ويقرأ الأمر **من ملف الإعدادات نفسه** لا من نسخة مكتوبة في
الاختبار — كي لا يفترقا بعد أي تعديل.

### gitleaks — 5 تأكيدات

الاختبار يستدعي gitleaks **معزولاً عن semgrep**، وهذا جوهره: في التشغيل
الكامل كان semgrep يخفي العطل.

1. سرّ AWS مُدرَج → كود 1 (يرفض)
2. **ضابط سالب**: نفس الحالة بلا `--staged` → كود 0 (يسمح)
3. الفرق بينهما هو `--staged` وحدها
4. ملف مُدرَج نظيف → كود 0 (لا يعطّل العمل)
5. سرّ على القرص والفهرس نظيف → كود 0 — يثبت أنه يفحص **الفهرس** لا
   الشجرة

### قواعد `.gitleaks.toml` — 15 تأكيداً في اتجاهين

**تمسك ستة**: كلمة سر · سرّ · مفتاح API · وثلاث سلاسل اتصال
(MySQL · MongoDB · Redis).

**وتسكت عن تسعة** منسوخة **حرفياً من كود المشروع**: تسلسل توكن
الاستعادة · تسلسل csrf في JS · بناء ملف إعدادات mysqldump · قيمة
نائبة · متغيّر بيئة · قالب `<your-key>` · حقل فورم HTML · مقارنة
بمتغيّر · نصّ منخفض العشوائية.

الاتجاه الثاني أهم: **قاعدة تمسك كل شيء تساوي قاعدة لا تمسك شيئاً**،
لأنها تُحذف بعد يومين من تعطيل العمل.

### تشغيل trivy المشروط — 5 تأكيدات

| الحالة | النتيجة |
|---|---|
| ملف عادي مُدرَج | `(no files to check) Skipped` |
| `composer.lock` مُدرَج | `Passed` — عمل تلقائياً |
| `--hook-stage manual` | `Passed` بلا شرط |

### semgrep — أُثبت في جولة سابقة

21 نتيجة على المستودع، منها 6 مشاكل حقيقية أُصلحت و15 إنذاراً كاذباً
وُثِّق. النتيجة الآن **0**. التفاصيل في
[CLEANUP-REPORT-POST-PHASE-6.md](CLEANUP-REPORT-POST-PHASE-6.md).

### trivy — 7 تأكيدات

«صفر ثغرة» على المشروع لا تثبت شيئاً وحدها. فبُني `composer.lock` بحزمة
مثغورة معروفة (`phpmailer v5.2.21`) في مجلد مؤقّت **خارج المستودع**:

```
Total: 6 (MEDIUM: 2, HIGH: 4)
CVE-2018-19296 · CVE-2020-13625 · …
```

وخرج بكود **1**. وضابط ثانٍ: نفس الأمر بلا `--exit-code 1` خرج بكود **0**
رغم الست ثغرات — نفس فخّ `--error` في semgrep.

---

## 6. كيف تُوثَّق إنذارات كاذبة

**القاعدة: توثيق في مكان الكود، لا إسكات في ملف بعيد.** ترك النتائج
الكاذبة يعني أن الفحص القادم يبدأ من رقم غير صفر، فيتعوّد قارئه على
تجاهلها — وذلك بالضبط ما يجعل النتيجة الحقيقية التالية تمرّ.

```php
// السبب المفحوص مكتوباً هنا: لماذا هذا الموضع آمن فعلاً
// nosemgrep: php.lang.security.unlink-use.unlink-use
@unlink($disk);
```

### تفصيلان كلّفا جولتين

1. **`nosemgrep` يُعترف به على سطر النتيجة أو السطر الذي يسبقه مباشرة.**
   وضعُ الشرح بينهما يبطل مفعوله.
2. **السطر الواحد قد يطابق قاعدتين.** `@unlink($disk)` يطابق
   `unlink-use` و`tainted-filename` معاً — تُذكران مفصولتين بفاصلة.

---

## 7. حدود هذه الأدوات — ما لا تفعله

| ليس مغطّى | لماذا |
|---|---|
| منطق الصلاحيات | لا أداة تعرف أن هذه النقطة كان يجب أن تكون للأدمن الجذر وحده |
| ثغرات المنطق | «حذف منتج غير موجود يُجيب نجح» لم تكشفه أي أداة — كُشف بالقراءة |
| كلمة سر على سطر الأوامر | semgrep أبلغ عن `exec` كحقن (**إنذار كاذب**) ولم يرَ أن كلمة السر مقروءة لكل مستخدمي الجهاز |
| أسرار في تاريخ قديم | `--staged` تفحص ما تلتزمه الآن. لفحص التاريخ كله: `gitleaks git .` |
| اعتماديات JS | لا `package-lock.json` في المشروع |

**الأدوات شبكة أمان لا بديل عن القراءة.** أكثر الأعطال خطورة في هذا
المشروع — سجل تدقيق يكذب، وصفحة دفع بلا شبكة CSRF، وتثبيت جلسة — لم
تكشفها أداة واحدة منها.

---

## 7-ب. أخطاء الاختبار — ستّ مرات في يوم واحد

في كل مرة ظهر «فشل» وكان مصدره الاختبار لا الكود أو الإعداد:

| ما بدا فاشلاً | السبب الحقيقي |
|---|---|
| تغطية gitleaks 3/8 | قيمي المختلَقة بأطوال خاطئة |
| «لا استدعاء لـflash-messages» | `str_contains` طابق **تعليقي أنا** |
| «صفر أثر للمنتج» | لم أفلتر بـ`target_type` فطابق حذف **مستخدم** |
| `export-csv` يمرّ | جلستي كانت رتبة A فعلاً |
| كل حالات trivy المشروط | `.pre-commit-config.yaml` غير مُدرَج |
| `trivy` «لم يعمل» | حكمتُ بظهور الاسم، وpre-commit يطبعه مع `Skipped` |

**الأخير كان سيعطي «فشل» دائماً على إعداد سليم** — ولو صدّقته لعدتُ
أعدّل شيئاً لا عيب فيه.

القاعدة المستخلَصة: **قبل أن تشخّص الكود، تحقّق أن الاختبار يقيس ما
تظنّه.** ومن علاماته أن تفشل حالات كثيرة دفعة واحدة، أو أن يفشل ما
لا علاقة له بتغييرك.

---

## 8. قواعد ثابتة

1. **لا تتجاوز خطّافاً فاشلاً.** لا `--no-verify` ولا `SKIP=`. الخطّاف
   الفاشل عطل يُصلَح لا عقبة تُلتف حولها.
2. **ميّز «الماسح وجد شيئاً» عن «الماسح لم يستطع العمل».** الأول نتيجة
   والثاني عطل في الأداة، والخلط بينهما يقود إلى «إصلاح» كود سليم.
3. **بعد أي تعديل على الإعدادات، أعد الضابط السالب.** سرّ مزروع يجب أن
   يُرفض. خطّاف لا يمكن أن يفشل ليس خطّافاً.
4. **الإنذار الكاذب يُوثَّق ولا يُترك.** فحص يبدأ من رقم غير صفر يُدرِّب
   قارئه على التجاهل.

---

## 9. الملفات ذات الصلة

| الملف | المحتوى |
|---|---|
| [.pre-commit-config.yaml](.pre-commit-config.yaml) | الإعدادات، وكل خيار وسببه فوقه |
| [.gitleaks.toml](.gitleaks.toml) | القاعدتان الموسَّعتان وقوائم السماح |
| [CLEANUP-REPORT-POST-PHASE-6.md](CLEANUP-REPORT-POST-PHASE-6.md) | جولة التحصين: 6 إصلاحات وsemgrep من 21 إلى 0 |
| [CLEANUP-PLAN.md](CLEANUP-PLAN.md) | الخطة الأصلية (المراحل 0–6، مكتملة) |
