# تقرير: إصلاح `last_activity` · الهروب الأمني · المرحلة 2

> **الحالة: كلها مكتملة ومدموجة في `master`.**
> التاريخ: 2026-08-25 · سابقه: [CLEANUP-REPORT-PHASE-0-1.md](CLEANUP-REPORT-PHASE-0-1.md)

---

## 1. ما طلبتَه وما نُفِّذ

| قرارك | التنفيذ |
|---|---|
| `last_activity`: تحديث دوري كل 15 دقيقة | ✅ |
| الهروب الأمني: قدّمه كمرحلة مستقلة الآن | ✅ |
| المدى: المرحلة 2 فقط ثم مراجعة | ✅ — توقّفت هنا |

---

## 2. إصلاح `users.last_activity`

**الدالة:** `touchUserActivity()` في `app/helpers/auth_helper.php`.

الخنق يُخزَّن في **الجلسة** لا في قاعدة البيانات، فلا يكلّف استعلام قراءة: الكلفة كتابة
واحدة كل ربع ساعة لكل مستخدم بدل واحدة مع كل عرض صفحة.

**موضع الاستدعاء — القرار الأهم هنا:** `Controller::view()` حصراً، لسببين:

1. تحقّقت أن `view()` تخصّ المتجر وحده — صفر كنترولر أدمن يستدعيها، كلها تمرّ عبر
   `AdminController::adminView()`.
2. **وضعها في الـbootstrap كان سيكسر مصادقة الأدمن.** `isUser()` تستدعي `session_start()`
   باسم `PHPSESSID` الافتراضي، وهذا يسبق `startAdminSession()` التي تضبط
   `session_name('admin_session')` — فتبدأ جلسة الأدمن باسم خاطئ ولا يُعرف الأدمن أبداً.

نشاط الأدمن يبقى متتبَّعاً منفصلاً في `AdminModel`.

**التحقق:** اختبار مباشر على قاعدة البيانات — الاستدعاء الأول يكتب، والثاني الفوري يمنعه
الخنق، وأُعيدت القيمة الأصلية بعده. وجلسة الأدمن سليمة: `/admin/home` لا يزال يرسل
`admin_session` بينما صفحات المتجر ترسل `PHPSESSID`.

---

## 3. مرحلة الهروب الأمني

### المنهج

بدل المرور اليدوي على 276 موضعاً، بنيتُ **`scripts/audit-escaping.php`** (بقي في المشروع):
يستخرج كل `<?= ... ?>` في الـviews — **771 موضعاً** — ويصنّفها حسب **سياق الإخراج** لا
حسب شكل التعبير: نص عادي · سمة HTML · رابط · كتلة JS. ثم راجعتُ كل فئة غير آمنة يدوياً
بتتبّع مصدر كل متغيّر إلى الكنترولر أو الـview الذي يعرّفه.

```bash
php scripts/audit-escaping.php
php scripts/audit-escaping.php app/views --list ATTR
```

### النتيجة: 18 موضعاً فقط احتاج تعديلاً من أصل 248 مشتبهاً

| ما أُصلح | العدد |
|---|---:|
| `src="<?= $imgSrc ?>"` — مسار صورة من قاعدة البيانات | 2 |
| قيم نصية من قاعدة البيانات (`order_id` · `status` · `role`) | 3 |
| معرّفات من قاعدة البيانات داخل سمات/روابط/استدعاءات JS — صبّ `(int)` | 13 |

لافت أن `product_dit.php:42` كان يُهرّب `alt` ويترك `src` بلا هروب في **نفس الوسم**.

### الباقي: إيجابيات كاذبة ثبتت بالتتبّع

- ternary لا يُنتج إلا نصوصاً حرفية (`'selected'` / `'checked'` / `''`)
- مصفوفات معرَّفة حرفياً **داخل الـview نفسه**: `$tiles` في `admin/home.php` ·
  `$countryPrefixes` · `$roleLabels` · خرائط الجندر والكاتوجري
- كلاسات CSS محسوبة من `match`/ternary: `$statusClass` · `$stockClass` · `$badgeColor`
- روابط ترقيم مبنية بـ`http_build_query` (مُرمَّزة أصلاً)
- HTML مقصود من الكنترولر: `$extraHead` · `$statusBadge` · `themeBootScript()`

### قرار: نمط `htmlspecialchars(addslashes($x))` لم يُغيَّر

موجود في خمسة مواضع داخل `onclick`. فحصته فوجدته **صحيحاً فعلاً** لهذا التداخل:
`addslashes` تهرّب للـJS، ثم `htmlspecialchars` للـHTML، والمتصفح يفكّهما بالترتيب
العكسي. تغييره إلى `json_encode` يتطلب إعادة بناء الاقتباسات ويخاطر بكسر كود يعمل.

**ثغرة نظرية متبقية:** `addslashes` لا تهرّب سطراً جديداً. اسم أدمن يحوي سطراً جديداً قد
يكسر الـ`onclick`. البيانات مُدخَلة من أدمن في صفحة أدمن، فالخطورة منخفضة.

---

## 4. المرحلة 2 — الموديلات

### صفر SQL في الكنترولرز

| | قبل | بعد |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | **14** | **0** |
| `Database::connect()` في الكنترولرز | 9 | **0** |

### موديل جديد: `StockNotificationModel`

جدول `stock_notifications` كان مستعلَماً مباشرة من ثلاثة كنترولرز، وأربعة من استعلاماته
**متطابقة حرفياً**. الجدول كيان قائم بذاته لا يخصّ المنتج ولا المستخدم وحدهما:

`exists` · `productIdsForUser` · `productIdsForUserWithin` · `add` · `countForProduct` ·
`waitingUserIds` · `clearForProduct`

`add()` تُرجع `true` فقط عند إضافة صفّ جديد فعلاً، فلا يُشعَر الأدمنية مرتين لو ضغط
المستخدم الزر مجدداً — منطق كان مبعثراً في الكنترولر.

### دوال أُضيفت للموديلات القائمة

| الدالة | مصدرها |
|---|---|
| `ProductModel::getNameById` | `WishlistController` — و`AdminProductModel::getNameById` صارت **تفوّض إليها** بدل تكرار نفس الاستعلام |
| `ProductModel::findVariantsStock` | `CartController` |
| `UserModel::getFullNameById` | `WishlistController` |
| `SupportModel::getMessageText` | `AdminSupportController` |

### تكرار أُزيل

`AdminProductsController` كان يكتب استعلام `SUM(stock_quantity)` **مرتين** رغم وجود
`AdminProductModel::getTotalStock()` بنفس النص حرفياً.

### تحسين جانبي

`getVisitorGender()` صار وسيط `PDO` فيها اختيارياً وتفتح اتصالها عند الحاجة، فلم تعد
الكنترولرز مضطرة لاستيراد `Database` لمجرّد تمريره.

---

## 5. عطل رابع اكتُشف — «حذف رسالة دعم» كان معطّلاً تماماً

`AdminSupportController::deleteMessage` كان يستدعي `Database::connect()` **بلا**
`use App\Core\Database`. داخل `namespace App\Controllers` يُحلّ الاسم غير المؤهَّل إلى
`App\Controllers\Database` — كلاس غير موجود. النتيجة: **Fatal error في كل محاولة حذف
رسالة دعم**، منذ كُتب السطر.

اختفى العطل مع نقل الاستعلام إلى `SupportModel`.

**ولمنع تكراره أُضيف `scripts/audit-imports.php`:**

```bash
php scripts/audit-imports.php
```

يحلّل الملفات بـ`token_get_all` **لا بـregex** — فلا يُبلّغ عن أسماء واردة داخل التعليقات
أو النصوص. (النسخة الأولى بـregex أعطتني إيجابيَّين كاذبين: `new Chart(...)` داخل سكربت
Chart.js المضمّن، وذكر `AdminOrdersController` في docblock.)

تحقّقت من أنه يمسك هذا العطل بالذات بإعادة إنتاجه مؤقتاً ثم استرجاع الملف. المشروع الآن
نظيف: صفر حالة.

---

## 6. التحقق

| الفحص | النتيجة |
|---|---|
| سكربت الدخان — 44 راوت GET | ✅ 44/44 |
| `php -l` — كل ملفات المشروع | ✅ |
| `scripts/audit-imports.php` | ✅ صفر حالة |
| اختبار وظيفي مباشر لكل دالة منقولة | ✅ 14 تأكيداً على قاعدة البيانات الحقيقية، بما فيها حالات الحدود (مصفوفة فارغة، معرّف غير موجود) |
| `POST /cart/check-stock` | ✅ JSON صحيح بكل الأعمدة |
| `GET /handlers/product_stock_handler.php` | ✅ |
| دورة "نبّهني عند التوفر" كاملة | ✅ بجلسة مستخدم حقيقية وCSRF: الطلب يُسجَّل، والمكرر لا يضيف صفاً ولا إشعاراً. نُظّفت بيانات الاختبار بعدها |
| صفحة المنتجات في المتصفح | ✅ 9 صور تُحمَّل بلا كسر، والروابط و`data-pid` صحيحة |

---

## 7. القياس التراكمي

| المؤشر | البداية | الآن |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | 14 | **0** ✅ |
| حُرّاس `function_exists()` زائدة | 16 | **0** ✅ |
| كود ميت في `Core` | 2 | **0** ✅ |
| مواضع `<?= ?>` بلا هروب (قياس آلي) | 276 | **257** |
| تحذيرات PSR-4 من composer | 48 | **0** ✅ |
| كلاسات غير مستوردة (Fatal كامن) | 1 | **0** ✅ |
| أسطر `<script>` مضمّنة | 579 | 579 |
| أسطر `<style>` مضمّنة | 55 | 55 |
| كنترولرز بلا توثيق OpenAPI | 10 | 10 |
| partials في `views/shared` | 1 | 1 |

**إجمالي الأسطر: 29,588 ← 29,789 (+201).** الزيادة مقصودة: `StockNotificationModel`
(160 سطراً) و`audit-imports.php` (170 سطراً) وتعليقات توثّق قرارات غير بديهية. الهدف
كان نقل المسؤوليات لا تقليل الأسطر.

> ملاحظة على عدّاد الهروب: القياس الآلي في `audit.php` تقريبي ويعدّ إيجابيات كاذبة.
> الرقم الدقيق من `audit-escaping.php` هو **686 آمن من 771**، والباقي رُوجع يدوياً وثبت
> أنه آمن بحكم السياق.

---

## 8. الأدوات المتاحة لك الآن

```bash
php scripts/smoke-test.php        # 44 راوت + صياغة المعدَّل (سريع)
php scripts/smoke-test.php --lint-all
php scripts/audit.php             # قياس حالة الكود
php scripts/audit-imports.php     # كلاسات غير مستوردة (Fatal كامن)
php scripts/audit-escaping.php    # تصنيف مخرجات الـviews أمنياً
```

---

## 9. ما يليه

| المرحلة | المحتوى |
|---|---|
| **3 — الكنترولرز** | استخراج `ProductImageService` و`StockNotifier` و`AdminAuditLogger`، وطبقة تحقق تشيل ~200 سطر مكرر. أطول الدوال الآن: `AdminBrandingController::save` (143) · `AdminAuthController::login` (141) · `AuthController::register` (115) |
| **4 — الـcore** | `Controller::view()` بدعم layouts — يشيل HTML مكتوباً يدوياً من 4 صفحات |
| **5 — الـviews** | طبقة partials + إخراج 579 سطر `<script>` و55 سطر `<style>` |
| **6 — الـJS** | 4940 سطر، بحاجة تدقيق منفصل |

**سؤال مفتوح:** التوثيق — 10 كنترولرز بلا OpenAPI. قرارك السابق كان "أكمله". هذا يزيد
~400 سطر توثيق في المرحلة 3.
