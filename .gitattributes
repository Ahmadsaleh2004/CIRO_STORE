# Uniform line endings across systems — it prevents diff noise between Windows and the rest
* text=auto

# Binary files — left untouched
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
