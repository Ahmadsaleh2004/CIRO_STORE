# تقرير المرحلة 3 — الكنترولرز وإكمال التوثيق

> **الحالة: مكتملة ومدموجة في `master`.**
> التاريخ: 2026-08-25 · سابقه: [CLEANUP-REPORT-PHASE-2.md](CLEANUP-REPORT-PHASE-2.md)

---

## 1. النتيجة بالأرقام

### الكنترولرز — فصل الكود عن التوثيق

| | البداية | الآن | الفرق |
|---|---:|---:|---:|
| إجمالي الأسطر | 6,955 | 7,056 | +101 |
| منها توثيق OpenAPI | 1,436 | 2,067 | **+631** |
| **كود فعلي** | **5,519** | **4,989** | **−530** |

الرقم الإجمالي يخفي ما حدث: الكود نزل 530 سطراً بينما زاد التوثيق 631. الزيادة مقصودة —
اخترتَ إكمال التوثيق لا حذفه.

### المؤشرات

| المؤشر | البداية | الآن |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | 14 | **0** ✅ |
| نسخ مكرّرة من `respond()` | 16 | **1** ✅ |
| كنترولرز بلا توثيق OpenAPI | 10 | **0** ✅ |
| تغطية الـspec للراوتات | ~55% | **100%** ✅ |
| تحذيرات مولّد الـspec | لم يعمل أصلاً | **0** ✅ |
| استيرادات ميتة | 5 | **0** ✅ |
| أطول دالة | 143 سطر | 137 سطر |

---

## 2. ما أُنجز

### 3أ — `respond()` إلى الكلاس الأب

كانت منسوخة **حرفياً في 16 كنترولر** (نسختان تختلفان في المسافات فقط، لا في المنطق).
مع `jsonError()` صار المجموع 17 نسخة و~130 سطر تكرار صافٍ.

نُقلت مرة واحدة إلى `App\Core\Controller`، وتحوّلت من `private` إلى `protected`.

**المنهج:** سكربت يحذف كل نسخة **فقط بعد تطبيعها ومقارنتها** بالنسخة الموحّدة. أي نسخة
يختلف منطقها كانت ستُترك ويُبلَّغ عنها — أبلغ عن `AuthController` وحدها (فروق مسافات)
فحُذفت يدوياً بعد المعاينة.

### 3ب — `ProductVariantUploader`

ثلاث دوال `private` (82 سطراً) نُقلت من `AdminProductsController` إلى خدمة. وفحص "هل
أُرسلت صورة؟" كان 17 سطراً مضمّنة، صار `hasAnyImage()`.

الخدمة **لا تقرأ من `$_POST` ولا `$_FILES`** — كل ما تحتاجه يصلها وسيطاً. النسخة القديمة
كانت تقرأ `$_POST['default_variant']` من داخلها، وهي تبعية خفية تمنع إعادة استخدامها أو
اختبارها.

`AdminProductsController`: 1027 → 905 سطر.

### 3ج — `StockNotifier`

ثلاث دوال موزّعة على كنترولرين (74 سطراً) جُمعت. كلها تكرّر نفس النمط: اجلب المستهدفين
ثم أرسل لكل واحد — صار دالة `private` واحدة داخل الخدمة.

وُثّق الفرق المقصود بينها: **نفاد المخزون** يشمل الرتب الأربع، بينما **طلب زبون واحد**
يستثني الرتبة A.

### 3د — `beginJsonPost()`

ثلاثة أنماط كانت تُكرَّر بنفس الصياغة حرفياً:

| النمط | التكرار |
|---|---:|
| `header('Content-Type: application/json...')` | 61 |
| `if (REQUEST_METHOD !== 'POST')` → respond | 40 |
| `if (!verifyCsrfToken(...))` → respond بنفس الرسالة | 24 |

جُمعت في دالة واحدة بترتيب مقصود وموثّق: الرأس أولاً كي تكون رسالة الرفض نفسها JSON، ثم
الطريقة، ثم CSRF — فالتحقق من التوكن بلا POST بلا معنى.

طُبِّقت على **18 موضعاً في 6 كنترولرز** عبر سكربت يطابق التسلسل الحرفي الكامل فقط. كل ما
انحرف — رسالة مختلفة، أو `redirect` بدل `respond`، أو منطق بين الكتل — تُرك عمداً.

### إكمال التوثيق — من ~55% إلى 100%

**37 سمة** تغطي 36 راوت متجر + خمسة مسارات 2FA و`forgot` في لوحة الأدمن كانت ناقصة أيضاً.

الأوصاف تشرح **السلوك الفعلي**: حدّ محاولات الدخول وقفل 15 دقيقة، وقيود التسجيل، وأن
رسالة "نسيت كلمة المرور" واحدة سواء وُجد البريد أم لا كي لا تكشف البُرد المسجَّلة، وأن
السلة تُدار في المتصفح ويُعاد التحقق من السعر على الخادم.

وأُضيف `securityScheme` ثانٍ `userSessionAuth` لجلسة `PHPSESSID` المنفصلة تماماً عن
`admin_session`، ووُثّقت في الوصف العام قاعدة قائمة في المشروع: **نقاط JSON تُرجع 200 حتى
عند فشل التحقق** والنتيجة تُقرأ من الحقل `success` — وُثّقت كما هي لا كما ينبغي أن تكون.

---

## 3. ثلاثة أعطال جديدة اكتُشفت

### 3.1 رفع صور الـvariants لم يكن يعمل إطلاقاً ✅ أُصلح

اسم الحقل في الفورم `variants[i][image]`، فيقلبه PHP إلى بنية **متداخلة**:

```php
$_FILES['variants']['tmp_name'][0]['image'] = '/tmp/phpXXXX'
```

أي أن `tmp_name[$idx]` **مصفوفة لا نص**. `extractFileEntry` كانت تفترضه نصاً وتقارن
`$error !== UPLOAD_ERR_OK` بينما `$error` مصفوفة — والمقارنة صادقة دائماً فتُرجع `null`
دائماً. **أي صورة تُرفع من فورمي إضافة وتعديل المنتج كانت تُهمل بصمت.**

اللافت: فحص "هل أُرسلت صورة؟" في **نفس الملف** كان يعالج البنية المتداخلة صحيحاً — فالفورم
يمرّ التحقق ثم تُهمل الصورة عند الرفع. و`AdminBrandingController` يعالجها صحيحاً أيضاً،
فالخطأ كان في مكان واحد بالذات.

**التحقق بإعادة الإنتاج:** أضفت منتجاً حقيقياً عبر HTTP بجلسة أدمن وCSRF **قبل** الإصلاح
→ `image_path` فارغ. وبعده → الصورة على القرص والمسار محفوظ وتُعرض فعلاً في صفحة المنتج.

### 3.2 أداة توليد الـspec لم تكن مثبّتة ✅ أُصلح

`composer.json` يعلن سكربت `docs:generate` يشغّل `vendor/bin/openapi`، لكن
`zircote/swagger-php` **لم يكن في `require` ولا `require-dev` ولا في `composer.lock`** —
فالسكربت كان يفشل دائماً، والـ1,436 سطر توثيق الموجودة أصلاً كانت خاملة.

أُضيفت كتبعية تطوير (6.7.0)، وصُحّح مسار السكربت إلى `app/Controllers` بعد إعادة تسمية
المجلدات في المرحلة 1.

### 3.3 ثلاثة مسارات دعم بنفس الوصف ✅ أُصلح

`AdminSupportController` كان يعلن `#[OA\PathItem]` لثلاثة مسارات **مجرّدة بلا عملية**،
فيدمج swagger-php أول `#[OA\Post]` في كل واحدة — فتخرج `/admin/support` و`/reply`
و`/delete` بنفس الملخّص ونفس `operationId`.

ونفس التكرار في `AdminProductsController`: ثلاث نقاط موثّقة **مرتين** على مستوى الكلاس
وعلى مستوى الميثود.

النتيجة بعد الإصلاح: التوليد يمرّ بـ**صفر تحذيرات**.

---

## 4. خطأ ارتكبتُه أثناء العمل

سكربت لحذف `#[OA\PathItem]` استعمل regex فيه `\P` — وهي في PCRE بداية خاصية Unicode لا
حرف عادي. فشل `preg_replace` وأرجع `null`، فكُتب `AdminSupportController.php` **فارغاً**
(0 بايت).

الاسترجاع كان `git checkout -- <file>` — أمر واحد، صفر خسارة. هذا بالضبط سبب وضع git
كأول خطوة في المرحلة 0 قبل لمس أي كود.

بعدها أعدت التعديل بأداة تحرير دقيقة بدل سكربت، ولم أعد لاستعمال regex لحذف كتل كاملة.

---

## 5. أداة التدقيق: تحسينان

**`audit-imports.php`** صار يكشف أيضاً **الاستيرادات الميتة** — `use` لا يشير إليه شيء.
فوضى لا عطل، فلا يغيّر exit code. يعدّ الذكر داخل التعليقات ذِكراً كي لا يقترح حذف `use`
موجود من أجل `@var`/`@param` فقط. كشف خمسة استيرادات ميتة فحُذفت.

**`audit.php`** — قياس طول الدالة كان **خاطئاً**: يقيس المسافة بين تعريف والذي يليه، فحذف
دالة قصيرة تلي طويلة يجعل الطويلة تبدو أطول. ظهر ذلك عندما قفز رقم `storeEdit` من 100 إلى
133 بلا أي تغيير فيها. صار القياس بعدّ الأقواس.

---

## 6. التحقق

| الفحص | النتيجة |
|---|---|
| سكربت الدخان — 44 راوت GET | ✅ |
| `php -l` — كل ملفات المشروع | ✅ |
| `audit-imports.php` | ✅ صفر كلاس غير مستورد، صفر استيراد ميت |
| توليد الـspec | ✅ صفر تحذيرات، 92 مسار من 92 راوت |
| اختبار وحدة لـ`ProductVariantUploader` | ✅ 14 تأكيداً (تجاهل الصفوف الفارغة، تحويل الأنواع، حماية `gender`، `is_default`، `sort_order`، وشكلَي `$_FILES`) |
| اختبار وحدة لـ`StockNotifier` | ✅ 6 تأكيدات على قاعدة البيانات الحقيقية |
| دورة منتج كاملة عبر HTTP | ✅ إضافة بصورة → تعديل يحافظ على الصورة → حذف ينظّف الصف والملف |
| نقاط JSON بعد `beginJsonPost` | ✅ CSRF يُرفض، بلا جلسة يحوّل 302، وبتوكن صحيح يصل للمنطق |

كل بيانات الاختبار نُظّفت: منتجان، صورة، إشعارات، جلسات أدمن ومستخدم مؤقتة.

---

## 7. القياس التراكمي منذ البداية

| المؤشر | البداية | الآن |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | 14 | **0** ✅ |
| حُرّاس `function_exists()` زائدة | 16 | **0** ✅ |
| كود ميت في `Core` | 2 | **0** ✅ |
| تحذيرات PSR-4 من composer | 48 | **0** ✅ |
| كلاسات في الـclassmap المحسَّن | 8 | **58** ✅ |
| كنترولرز بلا توثيق | 10 | **0** ✅ |
| نسخ `respond()` المكرّرة | 16 | **1** ✅ |
| **أعطال مكتشفة ومُصلَحة** | — | **7** |
| أسطر `<script>` مضمّنة | 579 | 579 |
| أسطر `<style>` مضمّنة | 55 | 55 |
| partials في `views/shared` | 1 | 1 |

### الأعطال السبعة

1. كل صفحات المتجر تُخرج HTML ناقص (`</body>`/`</html>` مفقودان)
2. المشروع لا يعمل على أي خادم Linux (حالة أحرف مجلدات PSR-4)
3. `users.last_activity` لا يتحدّث بالتصفّح
4. «حذف رسالة دعم» يفشل دائماً بـFatal error (كلاس غير مستورد)
5. **رفع صور الـvariants لا يعمل إطلاقاً**
6. أداة توليد الـspec غير مثبّتة
7. ثلاثة مسارات دعم بنفس الوصف والـoperationId

---

## 8. الأدوات المتاحة لك

```bash
php scripts/smoke-test.php        # 44 راوت + صياغة المعدَّل
php scripts/smoke-test.php --lint-all
php scripts/audit.php             # قياس حالة الكود
php scripts/audit-imports.php     # كلاسات غير مستوردة + استيرادات ميتة
php scripts/audit-escaping.php    # تصنيف مخرجات الـviews أمنياً
composer docs:generate            # توليد public/docs/openapi.yaml
```

---

## 9. ما يليه

| المرحلة | المحتوى |
|---|---|
| **4 — الـcore** | `Controller::view()` بدعم layouts — يشيل HTML مكتوباً يدوياً من 4 صفحات، ويصلح `require_once` الذي يمنع عرض view مرتين، و`die()` بدل صفحة 404 |
| **5 — الـviews** | طبقة partials (`views/shared` فيه ملف واحد) + إخراج 579 سطر `<script>` و55 سطر `<style>` |
| **6 — الـJS** | 4,940 سطر، بحاجة تدقيق منفصل |

**ما لم أفعله في المرحلة 3 ولماذا:** لم أستخرج طبقة تحقق (validator) عامة كما اقترحت
الخطة. بعد قراءة الكود تبيّن أن التحقق ليس مكرَّراً بقدر ما هو **مخصّص لكل نقطة**
(قواعد كلمة المرور، والعمر، وحدود السلايدر، وتفرّد الهاتف). بناء validator عام كان سيضيف
طبقة تجريد بلا وفر حقيقي. المكرَّر فعلاً كان المقدّمة الثلاثية — وقد جُمعت.

`AdminBrandingController::save` (130 سطراً) تُركت أيضاً: منطقها متماسك ويصعب اختباره بلا
فورم سلايدر كامل، فالمخاطرة تفوق العائد. مرشّحة للمرحلة 5 مع طبقة الـviews.
