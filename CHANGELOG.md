# سجلّ التغييرات

يتبع [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)، والإصدارات تتبع [SemVer](https://semver.org/lang/ar/).

> بدأ هذا الملف متأخّراً. ما قبل `[1.1.0]` مستخلَص من تاريخ git، والتفاصيل الكاملة لتلك الفترة مؤرشفة خارج المستودع.

---

## [غير مُصدَر]

### أُضيف

- **‏77 اختباراً آلياً** — وحدة وتكامل. كان المشروع بلا اختبار واحد.
  - `Totp` مقابل متجهات RFC 6238 المرجعية — أي أن 2FA متوافق فعلاً مع Google Authenticator.
  - عقد CSRF عبر HTTP: يمسح كل نقاط POST من الراوتر نفسه، فأي نقطة تُضاف غداً تدخل الفحص تلقائياً.
  - تكافؤ الحُرّاس: يقارن جدول المسارات بأجسام الأفعال في الاتجاهين.
  - تغطية OpenAPI: لا راوت بلا توثيق، ولا عملية موثّقة بلا راوت.
- **‏CI على GitHub Actions** — اختبارات على MySQL حقيقي (PHP 8.2 و8.3)، تحليل ثابت، وفحص أمني. كل أداة تعمل في الخطّافات تعمل هنا أيضاً.
- **‏PHPStan** مستوى 5 و**PSR-12** — صفر خطأ في الاثنين، بلا خطّ أساس واحد.
- **مواصفة OpenAPI كاملة**: 16 مخططاً و12 استجابة مشتركة و135 مرجعاً (كانت صفراً)، و53 مثالاً (كان واحداً).
- **حراسة مُعلَنة في جدول المسارات** — 51 حارساً، مع مجموعات ومسارات مسمّاة.
- دعم `PUT` و`PATCH` و`DELETE` في الراوتر.
- ترويسات أمنية: `nosniff` · `X-Frame-Options` · `Referrer-Policy` · `Permissions-Policy` · HSTS مشروط · CSP بوضع الإبلاغ.
- `Database::setConnection()` — منفذ حقن للاختبارات، محصور في CLI.
- `ErrorPage::serverError()` و`ErrorPage::methodNotAllowed()`.
- `envBool()` لقراءة الأعلام المنطقية من `.env`.
- ملفات المستودع: README · CONTRIBUTING · LICENSE · CHANGELOG · `.editorconfig`.

### تغيّر

- **`.env` صار المصدر الوحيد لإعدادات القاعدة.** كانت القيم مكتوبة صراحةً في `config.php` و**تتقدّم** على `.env` — فكان الملف يُقرأ ثم يُتجاهل بالكامل.
- **`display_errors` صار مشروطاً بالبيئة.** كان مثبَّتاً على `1` بلا شرط، فكل استثناء على الإنتاج يطبع مساراته للزائر. والافتراضي عند غياب `APP_ENV` هو **الإنتاج** — نسيان الضبط يجب أن يُخفي لا أن يكشف.
- `config.php` صار يحمّل `.env` بنفسه، فلا تعتمد نقطة دخول على أن تتذكّره.
- **مطابقة المسار صارت على مرحلتين** — المسار أولاً ثم الطريقة، فصار الردّ 405 مع ترويسة `Allow`.
- `Middleware::requireLogin()` صارت تفرّق بين صفحة كاملة ونقطة JSON.
- خريطة MIME واحدة تحكم القبول والامتداد معاً في رفع الصور.
- الوسوم في OpenAPI وُحّدت على `القسم - الاسم` بأوصاف حقيقية.
- التفريغ بين الاختبارات بـ`DELETE` لا `TRUNCATE` — أسرع 33 مرّة (8.585 ث ← 0.256 ث).

### أُصلح

- **تسريب بيانات الاتصال عند فشل القاعدة.** كان `die($e->getMessage())` يطبع المضيف واسم القاعدة واسم المستخدم للزائر، وينهي الطلب بكود **200** فتقرأ أدوات المراقبة الصفحة المكسورة كسليمة. صار 503 والتفصيل إلى السجلّ.
- **مطابقة المسار كانت تعامل النقطة كرمز regex.** مسار مثل `/handlers/notify_handler.php` كان يُبلَغ أيضاً بـ`/handlers/notify_handlerXphp`.
- **`env()` لم تكن تعمل** — أسبقية `??` قبل `?:` تجعل مفتاحاً فارغاً يتخطّى القيمة الافتراضية. لم يظهر لأن صفر مستدعٍ كان يستعملها.
- **`(bool)"false" === true` في PHP** — `APP_DEBUG=false` كان سيفتح التنقيح لا يغلقه.
- **`Router::route()` كانت ترمي على كل اسم** مهما كان صحيحاً — تبحث في خريطة لا يملؤها أحد.
- قائمة MIME المسموحة كانت منفصلة عن أذرع الامتداد: إضافة نوع بلا ذراع تحفظ الملف بامتداد `.jpg` **بصمت**.
- `unset` مكرّرة على `$_SESSION` كانت تُخفي أن سطرين متطابقين نصّاً يعملان على **جلستين مختلفتين**.
- دالتان خاصتان ميّتتان — إحداهما لها نسخة **حيّة** في كنترولر آخر، فكان تعديل قاعدة الإشعار يبدو منجزاً وهو نصف منجز.
- شرطان لا يتحقّقان أبداً: `=== null` بعد `??`، و`=== false` من `token_get_all`.
- `floor()` تُرجع `float` حيث يُطلب `int` في توليد رمز TOTP.
- `X-Powered-By` صار يُزال في التطبيق — `Header always unset` في Apache لا يكفي لأن PHP يضيفه بعد `mod_headers`.

### أُزيل

- سبعة عشر ملف خطة وتقرير من جذر المشروع (مؤرشفة خارج المستودع)، ونسخة CSS مؤقتة.
- `docs/` و`.claude/` من التتبّع.

### الأمان

- ‏`gitleaks` على كامل التاريخ في CI — سرّ حُذف في آخر commit يبقى في التاريخ.
- استثناء gitleaks لمتجه RFC 6238 **بالقيمة الحرفية لا بالمسار**، كي يبقى الملف مفحوصاً.

---

## [1.1.0]

### أُضيف

- توثيق OpenAPI بتغطية كاملة للمسارات.
- أدوات الفحص الأمني: gitleaks و semgrep و trivy، مع قواعد semgrep محلية تُشفّر أعطال هذا المشروع تحديداً.
- بصمة SRI لصفحة توثيق الـAPI.

### تغيّر

- **‏`error_code` صريح بدل نصّ الرسالة** لفشل CSRF. كان العميل يكتشف الفشل بمطابقة بداية النصّ، فأي نقطة تصوغ رسالتها بشكل آخر تفقد إعادة المحاولة **بصمت** — حدث ذلك ثلاث مرّات.
- مقدّمة نقاط JSON وُحّدت: من 18 موضعاً إلى 45 عبر `beginJsonPost()`.
- تصليب الجلسة واحتواء المسارات، وكلمة سرّ القاعدة خرجت من سطر أوامر `mysqldump`.

### أُصلح

- ثغرة CSRF في نقطتَي تسجيل الخروج.
- ثغرة CSRF في نقاط الإشعارات.
- `AdminProductModel::update` — عقد صريح بدل `true` غير مشروط.
- `AdminProductModel::delete` كان يُرجع `true` لمنتج لم يوجد قط، فيكتب الكنترولر **سجلّ تدقيق كاذب**.
- مواضع `die()` الخام التي تسرّب مسارات الخادم.

---

## [1.0.0]

الإصدار الأول بعد ستّ مراحل تنظيف:

1. حذف الكود الميت وتوحيد التسمية.
2. صفر SQL في الكنترولرز — كل الاستعلامات في المودلز.
3. توحيد `respond()` واستخراج خدمتين.
4. الـCore: ثلاثة layouts في `view()`، و404 حقيقي، وهيلبرز عبر composer autoload.
5. الـViews: صفر `<style>` مضمّن، وسبعة partials، والمنطق كله خارج القوالب.
6. الـJS: قاعدة موحّدة لشارة المخزون، وشبكة أمان CSRF عبر طبقة الـJS.
