# تقرير المرحلتين 0 و 1 — شبكة الأمان وحذف الكود الميت

> **الحالة: مكتملة ومدموجة في `master`.**
> التاريخ: 2026-08-25 · الخطة: [CLEANUP-PLAN.md](CLEANUP-PLAN.md)

---

## 1. القرارات الثلاثة التي فوّضتها لي

| القرار | ما اخترته | لماذا |
|---|---|---|
| `.env` داخل git؟ | **خارج git**، مع `.env.example` | يحوي كلمة مرور قاعدة البيانات ومفاتيح Google OAuth و hCaptcha وكلمة مرور SMTP حقيقية |
| توثيق OpenAPI | **يُكمَل لاحقاً، لا يُحذف** | `composer.json` فيه سكربت `docs:generate` و`openapi_info.php` مسجَّل في `autoload.files` — أي أنه مستخدم عن قصد، وحذف 1436 سطر توثيق عامل تخريب لا تنظيف |
| الموديلات static أم instance؟ | **تبقى static** | الـ15 موديل متّسقون (166 دالة static، صفر instance)، ولا يوجد DI container ولا اختبارات وحدة تحتاج mocking. التحويل يلمس الـ24 كنترولر بلا عائد |

---

## 2. ما أُنجز

### المرحلة 0 — شبكة الأمان

**`git init` + خمسة commits** على `master`، والمرحلة 1 على branch مستقل مدموج بـ`--no-ff`
حتى يبقى تاريخها منفصلاً ومرئياً.

`.gitignore` يستثني: `.env` · `vendor/` · `public/docs/` (مُولَّد) · محتويات
`storage/backups/` مع إبقاء `.htaccess` · `css-backup-2026-08-24/`.
و`/public/images/` **متتبَّع عن قصد** لأن قاعدة البيانات تشير إليه بالمسار.

**`scripts/smoke-test.php`** — 320 سطر:
- يقرأ راوتات GET من `public/index.php` بـregex، فلا يتقادم عند إضافة راوت.
- يفحص كود HTTP، وخلو المخرجات من `Fatal error`/`Parse error`/`Warning`/`Notice`/
  `Deprecated`/`Uncaught`/`SQLSTATE`، ومن `View file [...] not found`، ومن الصفحة
  الفارغة، ومن الـHTML غير المكتمل.
- ثم `php -l`: افتراضياً على المعدَّل منذ آخر commit (فوري)، و`--lint-all` للمسح الشامل
  (يغطي مسارات POST الستين التي يتعذّر بلوغها عبر HTTP بدون جلسة وCSRF).
- `exit(1)` عند أي فشل ليصلح لاحقاً كـgit hook.

```bash
php scripts/smoke-test.php
```

**`scripts/audit.php`** — 270 سطر. يطبع أحجام الطبقات، ونسبة توثيق OpenAPI من كل كنترولر،
وعدّادات المؤشرات، وأطول عشر دوال. `--json` للمقارنة الآلية، و`audit-baseline.json` هو
لقطة ما قبل التنظيف.

```bash
php scripts/audit.php
```

### المرحلة 1 — حذف الكود الميت وتوحيد التسمية

| ما حُذف | الحجم | التحقق قبل الحذف |
|---|---:|---|
| `app/Core/Model.php` | 15 سطر | `grep "extends Model"` → صفر (كل الـ15 موديل static) |
| `Controller::model()` | 14 سطر | `grep '$this->model('` → صفر |
| autoloader يدوي في `public/index.php` | 12 سطر | composer يعرّف `App\` أصلاً |
| `require_once` في `AdminController` | 2 سطر | `public/index.php` يحمّل كل الهيلبرز بـglob |
| 11 حارس `function_exists()` | — | كلها تحرس دوالّ helper محمَّلة دائماً |

**حارسان بقيا عن قصد وموثَّقان:** `BackupModel` (فحص أن `exec` غير معطّلة في `php.ini`)
و`getVisitorGender` (قد تُستدعى من سكربتات CLI بترتيب تحميل مختلف).

---

## 3. ثلاثة أعطال اكتُشفت أثناء العمل

### 3.1 كل صفحات المتجر كانت تُخرج HTML ناقصاً ✅ أُصلح

`app/views/inc/footer.php` كان ينتهي عند `</footer>` ولا يغلق `</body>` ولا `</html>`
أبداً — بينما فوتر الأدمن يغلقهما. المتصفحات تُغلق الوسوم ضمنياً فلم يظهر أثر مرئي.

**اكتشفه سكربت الدخان في أول تشغيل له** — 7 صفحات فشلت، وهذا بالضبط ما بُني من أجله.

### 3.2 المشروع لم يكن ليعمل على أي خادم Linux ✅ أُصلح

`composer.json` يعرّف `"App\\" => "app/"`، أي أن `App\Models\ProductModel` يجب أن يكون في
`app/Models/ProductModel.php`. المجلدات كانت `app/models` و`app/controllers` و`app/core`
**بحروف صغيرة**. يعمل على Windows فقط لأن نظام ملفاته غير حسّاس لحالة الأحرف.

**الدليل الرقمي:**

| | قبل | بعد |
|---|---:|---:|
| تحذيرات `does not comply with psr-4` | **48** | **0** |
| كلاسات في الـclassmap المحسَّن | **8** (كلها PHPMailer) | **55** |
| منها من `App\` | **0** | **47** |

يعني `composer dump-autoload -o` — خطوة إنتاج قياسية — كان يُسقط كل كلاسات المشروع.

الإصلاح: `app/models → app/Models` · `app/controllers → app/Controllers` ·
`app/core → app/Core` (عبر `git mv` بخطوتين لأن Windows غير حسّاس)، وإضافة
`exclude-from-classmap` لـ`config/helpers/views` لأنها ليست PSR-4، وتصحيح مسار
`docs:generate`.

### 3.3 عدّاد "المستخدمون النشطون" أقل من الحقيقة ⚠️ موثَّق، لم يُصلح

`ProductController` كان يستدعي `updateUserActivity()` محروسة بـ`function_exists` —
و**الدالة غير معرَّفة في المشروع إطلاقاً**. الحارس كان الشيء الوحيد الذي يمنع Fatal error،
والكتلة لم تُنفَّذ ولا مرة.

الدالة الحقيقية `UserModel::updateActivity()` تُستدعى **عند تسجيل الدخول فقط**
([AuthController.php:72](app/Controllers/AuthController.php:72))، فعمود `users.last_activity`
لا يتحدّث بالتصفّح. النتيجة: مؤشر "النشطون خلال 90 يوماً" في لوحة التحكم
([AdminDashboardModel.php:99](app/Models/AdminDashboardModel.php:99)) يعدّ مستخدماً يتصفّح
يومياً بلا إعادة دخول على أنه **غير نشط**.

**لماذا لم أُصلحه:** الإصلاح يعني كتابة في قاعدة البيانات مع **كل عرض صفحة** — قرار أداء
مستقل لا ينتمي لمرحلة وعدتُ فيها بصفر تغيير في السلوك. تُرك كتعليق موثَّق في مكانه، والقرار
لك. البدائل: تحديث دوري (مثلاً مرة كل 15 دقيقة لكل مستخدم)، أو عند أحداث محددة فقط.

---

## 4. القياس — قبل وبعد

| المؤشر | قبل | بعد |
|---|---:|---:|
| حُرّاس `function_exists()` في ctrl/core/views | 16 | **0** |
| `app/Core/Model.php` كود ميت | 1 | **0** |
| `Controller::model()` كود ميت | 1 | **0** |
| تحذيرات PSR-4 من composer | 48 | **0** |
| كلاسات `App\` في الـclassmap | 0 | **47** |
| إجمالي الأسطر | 29,588 | 29,561 |

الانخفاض 27 سطراً فقط لأن ~41 سطر كود محذوف قابله ~14 سطر تعليق يوثّق سبب الحذف
وموقع العطل 3.3.

**مؤشرات لم تتغيّر (وهذا متوقَّع — نطاق المراحل القادمة):**
استعلامات SQL في الكنترولرز **14** · أسطر `<script>` مضمّنة **579** · أسطر `<style>`
مضمّنة **55** · كنترولرز بلا توثيق **10** · partials في `views/shared` **1**.

---

## 5. التحقق

| الفحص | النتيجة |
|---|---|
| سكربت الدخان — 44 راوت GET | ✅ 44/44 |
| `php -l` — كل ملفات المشروع | ✅ 112/112 |
| تحميل الكلاسات عبر الأوتولودر المحسَّن | ✅ فحص مستقل لـ9 كلاسات من `Core`/`Models`/`Controllers` |
| صفحة تفاصيل المنتج في المتصفح | ✅ العنوان، الألوان، التقييمات، المنتجات المشابهة، وHTML مكتمل، وصفر خطأ PHP |
| `grep "Product_dit"` | ✅ صفر بقايا |
| `grep "app/models\|app/controllers\|app/core"` | ✅ صفر بقايا |

---

## 6. تاريخ git

```
*   دمج المرحلة 1: حذف الكود الميت وتوحيد التسمية
|\
| * توحيد التسمية: Product_dit -> ProductModel، ومجلدات PSR-4 بحروف كبيرة
| * حذف الكود الميت من core والكنترولرز
|/
* إضافة scripts/audit.php — قياس حالة الكود قبل/بعد كل مرحلة
* إضافة scripts/smoke-test.php — شبكة أمان لإعادة الهيكلة
* إصلاح: إغلاق </body> و </html> في فوتر المتجر
* Baseline: الحالة قبل بدء التنظيف
```

للتراجع عن المرحلة 1 كاملة مع إبقاء شبكة الأمان:

```bash
git revert -m 1 HEAD
```

---

## 7. ما يليه

المرحلة 2 — الموديلات: نقل الـ14 استعلام SQL من الكنترولرز، وإنشاء
`StockNotificationModel` (الستة استعلامات في `WishlistController` كلها حول
`stock_notifications`، وأربعة منها مكررة حرفياً في `AdminProductsController`
و`ProductController`).

**قرار ينتظرك:** العطل 3.3 — هل أُصلح تتبّع `last_activity`، وبأي استراتيجية؟
