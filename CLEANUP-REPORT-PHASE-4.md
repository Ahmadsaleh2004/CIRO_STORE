# تقرير المرحلة 4 — الـCore

> **الحالة: مكتملة.**
> التاريخ: 2026-08-25 · سابقه: [CLEANUP-REPORT-PHASE-3.md](CLEANUP-REPORT-PHASE-3.md)

---

## 1. النتيجة بالأرقام

| المؤشر | قبل | بعد |
|---|---:|---:|
| صفحات تكتب `<!DOCTYPE html>` بيدها | **3** | **0** ✅ |
| نسخ منطق تجميع الـlayout | **3** | **1** ✅ |
| `die()` يطبع مسار الخادم في المتصفح | 1 | **0** ✅ |
| `require_once` يمنع عرض view مرتين | 4 | **0** ✅ |
| كود ميت في `Core` + `helpers` | 2 | **0** ✅ |
| تحميل الهيلبرز | `glob()` في الـbootstrap | `composer autoload.files` ✅ |
| أسطر `<script>` مضمّنة في الـviews | 579 | **579** (بلا تغيير) |
| أسطر `<style>` مضمّنة في الـviews | 55 | **55** (بلا تغيير) |

### الأحجام

| الطبقة | قبل | بعد | الفرق |
|---|---:|---:|---:|
| `app/Core` | 666 | 728 | **+62** |
| `app/Controllers` | 7,056 | 7,048 | −8 |
| `app/views` (49 → 52 ملفاً) | 7,160 | 7,241 | +81 |
| `app/helpers` | 488 | 492 | +4 |
| **المجموع** | 30,040 | 30,179 | **+139** |

**الزيادة +139 مضلّلة إن قُرئت وحدها، ففصّلتُها:**

| | إجمالي | تعليق | كود فعلي |
|---|---:|---:|---:|
| `Core/Controller.php` قبل | 118 | 62 | **56** |
| `Core/Controller.php` بعد | 179 | 79 | **100** |
| `views/inc/head-bare.php` (جديد) | 57 | 23 | 34 |
| `views/inc/footer-bare.php` (جديد) | 14 | 9 | 5 |
| `views/errors/404.php` (جديد) | 29 | 10 | 19 |

الكود الفعلي في `Controller::view()` زاد 44 سطراً لأنها صارت تحمل ثلاثة
layouts بدل واحد، ومعالج 404، وحارس قيمة. و100 سطر جديد في ثلاثة ملفات views
هي **رأس وذيل مشتركان يحلّان محل ثلاث نسخ مكرّرة** كانت داخل الصفحات نفسها —
ولهذا نزلت الصفحات الثلاث 19 سطراً و`AdminAuthController` 15 سطراً.

الحصيلة: عدد الأسطر ارتفع، وعدد **نسخ نفس المنطق** نزل من ثلاث إلى واحدة.
الهدف كان الثاني.

---

## 2. ما أُنجز

### 4أ — `Controller::view()` بثلاثة layouts

```php
protected function view(string $view, array $data = [], string $layout = 'store'): void
```

| الـlayout | التجميع |
|---|---|
| `store` (افتراضي) | `inc/head` + `inc/navbar` + view + `inc/footer` |
| `admin` | `admin/inc/head` + `admin/inc/navbar` + view + `admin/inc/footer` |
| `bare` | الـview وحده |

`'store'` افتراضية، فالتسعة كنترولرز التي تستدعي `view()` لم تتغيّر بحرف واحد.

**قيمة layout غير معروفة ترمي `InvalidArgumentException`** بدل السقوط بصمت إلى
`bare` — خطأ الكتابة `'admn'` كان سيُخرج صفحة بلا رأس ولا ذيل ولا رسالة.

**تفصيلة دقيقة:** المتغيرات المحلية داخل `view()` مسبوقة بـ`__` لأن
`extract($data)` يكتب في نفس النطاق. النسخة القديمة كانت تقرأ `$view` **بعد**
`extract($data)`، أي أن مفتاحاً اسمه `view` في `$data` كان سيكسرها. الجديدة
تحسب المسار قبل `extract`.

### 4ب — `require_once` ← `require`

`require_once` تمنع عرض نفس الملف مرتين في الطلب الواحد. عطل كامن يظهر لحظة أن
يعرض view نفس الـpartial مرتين — وهذا بالضبط ما ستبنيه المرحلة 5 (طبقة
partials). `adminView()` تستعمل `require` منذ البداية بلا مشاكل عبر 17 صفحة.

**التحقق من عدم وجود عرض مزدوج كامن:** لقطة بايت-ببايت لكل الراوتات الـ44 قبل
التغيير وبعده → صفر فرق. لو كان أي ملف يُحمَّل مرتين لتضاعف حجم الصفحة.

### 4ج — صفحة 404 حقيقية بدل `die()`

القديم:

```php
require_once APPROOT . '/views/inc/head.php';    // ← أُخرج فعلاً
require_once APPROOT . '/views/inc/navbar.php';  // ← أُخرج فعلاً
if (file_exists($viewFile)) { ... } else {
    die("View file [{$viewFile}] not found!");   // ← بعد فوات الأوان
}
```

ثلاث مشاكل في أربعة أسطر:

1. **الفحص بعد الإخراج.** الترويسات أُرسلت أصلاً، فـ`http_response_code(404)`
   مستحيلة — الصفحة تخرج بكود **200** ونصف محتوى.
2. **تسريب مسار الخادم.** `C:\xampp\htdocs\STORE\app/views/...` مطبوع في
   المتصفح للزائر. لا فائدة له، وكشف لبنية المجلدات.
3. **صفحة مكسورة.** `<head>` و`<navbar>` بلا `</body>` ولا `</html>`.

الجديد: الفحص **قبل أي بايت**، ثم `error_log` بالمسار الكامل (للمطوّر)
و`http_response_code(404)` وصفحة `app/views/errors/404.php` كاملة برسالة عامة.
واحتياط داخلي: لو غاب ملف الـ404 نفسه تُطبع صفحة صغيرة مضمّنة — استدعاء `view()`
من داخل معالج خطأ الـview يعني تكراراً لا نهائياً.

### 4د — الصفحات الثلاث المستقلة إلى `bare`

`admin/login.php` · `admin/store-reauth.php` · `auth/reset-password.php` كانت
تكتب `<!DOCTYPE html>` و`<html>` و`<head>` و`</body></html>` — ثلاث نسخ.

صارت تعلن متغيراتها ثم تستدعي الرأس والذيل المشتركين:

```php
$bareTitle = 'Admin Login — ' . SITENAME;
$bareCss   = ['css/admin/pages/login.css'];
$bareHead  = '<meta name="urlroot" content="' . URLROOT . '">';
require APPROOT . '/views/inc/head-bare.php';
```

وحُذفت `AdminAuthController::renderStandaloneView()` (18 سطراً): كانت موجودة
حصراً لأن `view()` المورثة تفرض layout المتجر.

### 4هـ — `adminView()` تفوّض

بقي فيها ما يخصّ لوحة التحكم وحدها: حقن `$adminName` و`$adminRole` و`$adminId`
و`$csrf` و`$newOrders` و`$newMessages`، وبادئة المسار `admin/`.

**ترتيب الحقن مقصود ومحفوظ حرفياً:** النسخة القديمة كانت تستخرج `$data` أولاً ثم
تُسند متغيرات الأدمن — أي أنها تغلب على أي مفتاح بنفس الاسم قادم من الكنترولر.
الجديدة تكتب فوق `$data` **قبل** تمريرها، وهو نفس الترتيب الفعلي.

**نتيجة جانبية مقصودة:** صفحات الأدمن ورثت فحص الوجود قبل الإخراج وصفحة الـ404.
قبل ذلك كان اسم view خاطئ في كنترولر أدمن يعطي `require(): Failed to open stream`
مكشوفاً بالمسار الكامل.

### 4و — الهيلبرز إلى `composer autoload.files`

كان الـbootstrap يحمّل `functions.php` يدوياً ثم يمرّ بـ`glob()` على المجلد
ويستثني `functions.php` بالاسم. صارت الملفات الستة معلنة في `composer.json`،
و`functions.php` أوّلها صراحةً.

**تحقّقت قبل النقل** أن كل الهيلبرز الستة تعريفات دوال فقط بلا سطر تنفيذي واحد
على المستوى الأعلى — فتحميلها عند `vendor/autoload.php` (أي **قبل** `config.php`)
آمن: `URLROOT` وأخواتها تُقرأ داخل أجسام الدوال وقت الاستدعاء لا وقت التعريف.

`scripts/backfill_blocked_users_cancel_orders.php` يبقى على `glob` الخاص به —
لا يحمّل `vendor/autoload.php` أصلاً بل يسجّل autoloader يدوياً، فلا يتأثر.

---

## 3. ما اكتُشف أثناء العمل

### 3.1 النسخ الثلاث كانت قد تفرّقت فعلاً ✅ أُصلح

هذا هو الدليل العملي على كلفة التكرار، لا مجرّد حجّة نظرية:

| | `admin/login` | `admin/store-reauth` | `auth/reset-password` |
|---|:--:|:--:|:--:|
| Bootstrap بـ`integrity` (SRI) | ✅ | ✅ | **❌** |
| `<meta name="robots">` | ✅ | ✅ | ✅ |
| `<meta name="urlroot">` | ✅ | ❌ | ❌ |

صفحة واحدة من الثلاث كانت تحمّل Bootstrap **بلا تحقق سلامة**. الرأس المشترك
يجعل الثلاث متطابقات، والفرق الوحيد يبقى ما تعلنه كل صفحة عمداً.

`<meta name="urlroot">` مطلوب فعلاً — `js/admin/admin-auth.js:28` يقرأه — فمُرِّر
عبر `$bareHead` لصفحة الدخول وحدها.

### 3.2 `renderStandaloneView` كانت تُخرج نصاً خاماً عند غياب الـview ✅ أُصلح

```php
http_response_code(404);
echo "View not found: {$viewPath}";
```

الكود صحيح لكن الجسم نص عارٍ بلا `<html>`. اختفى مع حذف الدالة.

### 3.3 كودان ميتان في `Core` و`helpers` ✅ حُذفا

| ما حُذف | التحقق قبل الحذف |
|---|---|
| `require_once auth_helper.php` في `Middleware::requirePermission` | الهيلبرز كلها محمَّلة من الـbootstrap قبل أي راوت — الملف الوحيد للدخول هو `public/index.php` |
| حزمة `'admin-auth'` في `cssBundleFiles()` | `grep` على `cssBundle(` → استدعاءان فقط بـ`'store'` و`'admin'`. وكانت تضيف `tokens.css` إلى `login.css` الذي **يعلن في رأسه صراحةً** أنه مستقل ولا يعتمد على أي theme tokens |

### 3.4 عدّاد الأصول المضمّنة في `audit.php` يُخدع بالتعليقات ⚠️ موثَّق

`scripts/audit.php` يعدّ أسطر `<script>`/`<style>` المضمّنة بمسح نصي: يفتح العدّ
عند سطر فيه وسم فتح ويغلقه عند وسم الإغلاق، **ولا يميّز النص داخل تعليقات PHP**.

ثلاثة تعليقات كتبتُها في هذه المرحلة تذكر الوسمين حرفياً، فقفز `<style>` من 55
إلى **96** و`<script>` من 579 إلى **587** بلا أي أصل مضمّن جديد. أُعيدت صياغة
التعليقات فعاد العدّادان إلى 55 و579 بالضبط.

**لم أُصلح الأداة**: الإصلاح الصحيح تحليل الـviews بالـtokenizer لا بمسح
السطور، وهو تغيير مستقل خارج نطاق المرحلة. القيد موثَّق هنا، ويستحق الالتفات في
المرحلة 5 التي ستحرّك هذين الرقمين فعلاً.

### 3.5 راوت غير موجود يعطي نصاً عارياً ⚠️ لم يُصلَح — قرارك

`Router::dispatch()` ينتهي بـ:

```php
http_response_code(404);
echo "404 - Page Not Found";
```

كود صحيح، جسم نص عارٍ. هذا **مسار مختلف** عن الذي عالجته المرحلة: هذا «راوت غير
مسجَّل»، وذاك «الراوت مسجَّل لكن ملف الـview غائب».

الآن وقد صار `app/views/errors/404.php` موجوداً، توصيله بالراوتر تغيير من ثلاثة
أسطر. **لم أنفّذه** لأنه لم يكن ضمن ما وافقتَ عليه، ولأن الراوتر ليس من طبقة
`Core` المذكورة في الخطة. أول توصية في القسم 8.

---

## 4. خطأ ارتكبتُه أثناء العمل

**أداة المقارنة التي بنيتُها كانت تعطي فروقاً كاذبة، ثلاث مرات.**

بنيتُ `compare.php` ليقارن لقطتين من كل راوت بعد تطبيع القيم المتغيّرة. طبّعتُ
توكنات CSRF بـ`preg_replace('/[0-9a-f]{32,64}/i', 'TOKEN', $s)`.

النمط متغيّر الطول والمطابقة طامعة: إن صادف أن الأحرف المجاورة للتوكن hex أيضاً،
امتدّت المطابقة والتهمتها — بمقدار مختلف في كل تشغيل حسب التوكن العشوائي. فيختلف
طول النص المطبَّع لصفحة **لم تتغيّر إطلاقاً**، ويُبلَّغ عن فرق كاذب.

ضاع مني جولتان في التحقيق قبل أن أفهم أن العطل في أداتي لا في الكود. ثم ظهر فرق
كاذب ثالث من مصدر آخر: `git stash` يعيد تطبيع نهايات السطور، فاختلفت اللقطتان
بعدد محارف `\r` فقط.

**الإصلاح:** أطوال ثابتة (`{64}` و`{32}`) مع حدود كلمة، وتطبيع CRLF.
**والدرس الأهم — ضابط:** شغّلتُ الأداة على **لقطتين للأساس نفسه** فأعطت
`متطابق: 44 · مختلف: 0`. أداة مقارنة بلا ضابط سالب لا تُصدَّق.

خطآن أصغر في نفس الاتجاه:

- سكربت الـsnapshot الأول كتبتُه بـheredoc في الـshell، فمُسخ `\\\$` وانكسر
  النمط (مصيدة 4.3 في ملف التسليم — وقعتُ فيها رغم قراءتها). أُعيدت كتابته بأداة
  التحرير.
- اختبار POST الأول شارك جرّة كوكيز واحدة بين أقسامه، فبقي فيها `admin_session`
  من طلب سابق واصطدم بالمزوَّر → فشل كاذب في قسم إعادة المصادقة. عُزلت الجرّة.

---

## 5. ما لم أنفّذه ولماذا

| البند | القرار |
|---|---|
| `Router` 404 نصي | خارج نطاق ما وُوفق عليه — القسم 3.5 |
| إصلاح عدّاد `audit.php` | يحتاج تحليل tokenizer، تغيير مستقل — القسم 3.4 |
| إخراج `<style>` من `reset-password` | نُقلت الكتلة **كما هي حرفياً** إلى `$bareHead` كي تبقى المرحلة بصفر تغيير بصري. هذه مهمة المرحلة 5 صراحةً |
| نقل `touchUserActivity()` أو تعميمها | مقيَّدة بـ`layout === 'store'` كما طُلب. الصفحات الثلاث المستقلة لم تكن تستدعيها من قبل (كانت تُعرض خارج `view()` أصلاً)، فلا تغيّر في السلوك |

---

## 6. التحقق

| الفحص | النتيجة |
|---|---|
| سكربت الدخان — 44 راوت GET | ✅ 44/44 |
| `php -l` — كل ملفات المشروع | ✅ 120/120 |
| `scripts/audit-imports.php` | ✅ صفر كلاس غير مستورد، صفر استيراد ميت |
| `composer dump-autoload -o` | ✅ صفر تحذيرات · 919 كلاساً |
| `composer docs:generate` | ✅ صفر تحذيرات · 92 مساراً |
| **لقطة بايت-ببايت لكل راوتات المتجر الـ44** | ✅ 42 متطابقة تماماً؛ والاثنتان الباقيتان هما `admin/login` و`auth/reset` المقصودتان — وفرقهما تعليقات وترتيب وسوم فقط (فُحص بـ`diff` مباشر) |
| **لقطة 17 راوت أدمن بجلسة أدمن مزوّرة** | ✅ 17/17 متطابقة بايت-ببايت بعد تفويض `adminView()` |
| **مقارنة `getComputedStyle` + `getBoundingClientRect`** | ✅ صفر فرق — الصفحات الثلاث × مقاسين (1280×800 و375×812) = ست حالات، على 30 خاصية لكل عنصر (24 و26 و22 عنصراً). الانتقالات مُعطَّلة قبل القياس |
| اختبار وحدة لمسار الـ404 | ✅ 5 تأكيدات: layout مجهول يرمي استثناء · لا نص `die` القديم · لا تسريب مسار · صفحة كاملة · لا رأس/navbar قبل الخطأ |
| **اختبار HTTP حقيقي للـ404** | ✅ أُخفي `views/page/about.php` مؤقتاً → `/about` أعطى **HTTP 404** وصفحة كاملة بلا تسريب مسار، والمسار في `php_error_log`. أُعيد الملف فوراً |
| **اختبار HTTP حقيقي لمسارات POST الثلاثة** | ✅ 12 تأكيداً: التوكن المطبوع من كل view جديد يُقبل على الخادم، وتوكن خاطئ يُرفض (ضابط سالب) |
| تحميل Bootstrap بـSRI في `reset-password` | ✅ فُحص في المتصفح: 1298 قاعدة CSS محمَّلة فعلاً — الهاش صحيح ولم يُحجب الملف |
| عدّادا الأصول المضمّنة | ✅ 579 و55 — نفس ما كانا قبل المرحلة بالضبط |

### بيانات الاختبار — نُظّفت كلها

| ما أُنشئ | الحالة |
|---|---|
| ملفات جلسات أدمن مزوّرة (5) | ✅ حُذفت — `sess_test*` و`sess_phase4visual*` → صفر |
| صفوف `password_resets` (3) | ✅ حُذفت → صفر |
| صفوف `admin_login_attempts` | ✅ حُذفت → صفر |
| صفّ `admin_audit_log` (`store_mode_reauth_failed`) | ✅ حُذف — كتبه اختبار POST |
| `app/views/page/about.php` المُخفى مؤقتاً | ✅ أُعيد في نفس الأمر |

---

## 7. القياس التراكمي منذ البداية

| المؤشر | البداية | الآن |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | 14 | **0** ✅ |
| وصول DB من الـviews | — | **0** ✅ |
| حُرّاس `function_exists()` زائدة | 16 | **0** ✅ |
| كود ميت في `Core` | 2 | **0** ✅ |
| تحذيرات PSR-4 من composer | 48 | **0** ✅ |
| كنترولرز بلا توثيق OpenAPI | 10 | **0** ✅ |
| نسخ `respond()` المكرّرة | 16 | **1** ✅ |
| **صفحات تكتب `<!DOCTYPE>` بيدها** | **3** | **0** ✅ |
| **نسخ منطق تجميع الـlayout** | **3** | **1** ✅ |
| أسطر `<script>` مضمّنة | 579 | 579 ← المرحلة 5 |
| أسطر `<style>` مضمّنة | 55 | 55 ← المرحلة 5 |
| partials في `views/shared` | 1 | 1 ← المرحلة 5 |
| **أعطال ومشاكل مكتشفة** | — | **10** |

الجديدة الثلاث في هذه المرحلة: تفرّق النسخ الثلاث (SRI ناقص في صفحة) · `die()`
المسرِّب للمسار بعد فوات أوان الـ404 · كودان ميتان في `Core`/`helpers`.

---

## 8. ما يليه — وقرارات تنتظرك

**قراران قبل المرحلة 5:**

1. **`Router` 404 النصي** (القسم 3.5) — أوصله بـ`errors/404.php`؟ ثلاثة أسطر.
   الفائدة: تجربة واحدة متسقة لكل صفحة غير موجودة بدل نصّين مختلفين.
2. **عدّاد `audit.php`** (القسم 3.4) — أصلحه بالـtokenizer قبل المرحلة 5؟
   المرحلة 5 كلها مقيسة بهذين الرقمين، ودقّتهما تهمّ هناك أكثر من هنا.

**المرحلة 5 — الـviews** (3-4 أيام، أكبر مرحلة): طبقة partials (`views/shared`
فيه ملف واحد) · إخراج 579 سطر `<script>` و55 سطر `<style>` · تمرير البيانات
بـ`data-*`. البنية التي بنتها هذه المرحلة (`bare` + `$bareHead`) جاهزة لاستقبال
`reset-password` عندما تُخرَج كتلة أنماطها إلى CSS.

**المرحلة 6 — الـJS**: 4,948 سطراً، بلا تدقيق عميق بعد.

---

## 9. الأدوات المتاحة لك

```bash
php scripts/smoke-test.php        # 44 راوت + صياغة المعدَّل
php scripts/smoke-test.php --lint-all
php scripts/audit.php             # قياس حالة الكود
php scripts/audit-imports.php     # كلاسات غير مستوردة + استيرادات ميتة
php scripts/audit-escaping.php    # تصنيف مخرجات الـviews أمنياً
composer docs:generate            # public/docs/openapi.yaml
composer dump-autoload -o         # بعد أي تعديل على autoload في composer.json
```
