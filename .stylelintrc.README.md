# لماذا أُطفئت قواعد Stylelint هذه

كل إطفاء هنا قرار، لا تهرّب من إصلاح. القاعدة تُطفأ حين تصف **مشروعاً
آخر** لا هذا المشروع — لا حين يصعب إرضاؤها.

## `selector-id-pattern` — 133 مخالفة

المعيار يطلب `kebab-case` للمعرّفات، والمشروع كله على `camelCase`
(`#adminNotifBell`, `#productsGrid`). والاسم ليس في CSS وحده: هو نفسه
في `getElementById` في 34 ملف JS، وفي `id=` في 57 ملف view.

فإرضاء القاعدة يعني إعادة تسمية عبر ثلاث لغات في وقت واحد — تغيير
واسع الخطر لمكسب صفر. العرف القائم متّسق، والاتساق هو ما تخدمه القاعدة
أصلاً.

## `declaration-block-single-line-max-declarations` — 74 مخالفة

قواعد قصيرة مكتوبة في سطر واحد:

```css
.hpc-tag-best { background: #6366f1; color: #fff; }
```

هذه مسألة **تنسيق**، ومالكها Prettier لا Stylelint. تقسيم المسؤولية
مقصود: Prettier للشكل، Stylelint للصحّة. وقاعدتان تحكمان الشكل نفسه
تتصادمان عند أول اختلاف.

لتوحيد التنسيق: `npm run format`.

## `property-no-vendor-prefix` — 11 مخالفة

`-webkit-appearance` و`-moz-appearance` وأخواتها. القاعدة تفترض أن
البادئات صارت زائدة، وهذا صحيح لبعضها لا لكلّها — و`appearance` على
عناصر النماذج ما زالت تحتاجها في متصفحات مستعملة.

حذفها يحتاج قياساً على متصفحات حقيقية، لا ثقةً بقاعدة عامة. وحتى
يُقاس، إبقاؤها أرخص من كسر مظهر حقل إدخال.
