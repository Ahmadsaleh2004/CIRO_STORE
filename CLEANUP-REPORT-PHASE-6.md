# تقرير المرحلة 6 — طبقة الـJS

> **الحالة: الأجزاء الأربعة كلها منجزة.** الجزء الرابع (6ب-2 — تحويل الـ13 ملفاً
> إلى شبكة أمان CSRF) أُجِّل أولاً بقرارك، ثم نُفِّذ بالكامل في ستة commits
> (`00d2348` … `fd3538f`).
> التاريخ: 2026-08-25، ومحدَّث في 2026-08-26 بعد تنفيذ 6ب-2 وإعادة قياس أرقامه.
> الخطة: [CLEANUP-PLAN-PHASE-6.md](CLEANUP-PLAN-PHASE-6.md)

---

## 1. ما أُنجز

| الجزء | الحالة |
|---|---|
| **6ج** — التنظيف | ✅ |
| **6ب-1** — `fetchWithCsrfRetry` تدعم أجسام JSON | ✅ |
| **6أ** — قاعدة بادج المخزون موحّدة | ✅ |
| **6ب-2** — تحويل الـ13 ملفاً | ✅ |

### المؤشرات

| | قبل | بعد |
|---|---:|---:|
| ملفات JS | 35 | **34** |
| `console.log` في الإنتاج | 2 | **0** ✅ |
| `var` | 23 | **0** ✅ |
| ملفات محمَّلة مرتين | 1 | **0** ✅ |
| نسخ قاعدة بادج المخزون | **3** (في لغتين) | **2** (واحدة لكل لغة) ✅ |
| دوال باسم لا يصف سلوكها | 1 | **0** ✅ |
| أشكال أجسام تدعمها شبكة CSRF | 2 | **3** ✅ |
| مواضع POST تمرّ بشبكة أمان CSRF | 20 | **50** ✅ |
| مواضع POST بلا شبكة أمان | 35 | **5** (كلها بسبب موثَّق) ✅ |
| ملفات ترسل POST بلا أي شبكة أمان | 16 | **3** (بسبب موثَّق) ✅ |

---

## 2. الجزء 6ج — التنظيف

### حُذف `js/admin/admin-layout/admin-footer.js` كاملاً (26 سطراً)

كان يربط أزرار سايدبار إشعارات الأدمن الثلاثة — والثلاثة يربطها
`admin-notifications.js` فعلاً:

| الزر | ما في الملف المحذوف | ما في الملف الحقيقي |
|---|---|---|
| Mark All Read | `console.log('TODO: …')` | `POST /mark-all-read` |
| Delete All | `console.log('TODO: …')` | `POST /delete-all` |
| Close | يزيل `.open` **ويترك الـbackdrop ظاهراً** | `toggleSidebar(false)` — يزيل الاثنين |

لم يظهر عطل من زر الإغلاق لأن المعالج الصحيح يعمل معه ويكمل المهمة. تحقّقت أن
`admin-notifications.js` يعمل دائماً: الجرس والبادج والسايدبار ماركب غير مشروط،
فحارسه المبكر لا يُفعَّل أبداً.

### `wishlist.js` كان يُحمَّل مرتين

فوتر المتجر يحمّله على كل صفحة، و`WishlistController::index` يحمّله مجدداً عبر
`extraScripts`. قِستُه في المتصفح: وسمان لنفس الملف. 266 سطراً تُحلَّل وتُنفَّذ
مرتين، ومعالج `DOMContentLoaded` يعمل مرتين.

لم يظهر عطل لأن العرض يستبدل `innerHTML` كاملاً فيمحو أثر التنفيذ الأول. بعد
الحذف: **17 وسماً ← 16**، وصفر تكرار.

### `fixImagePath` ← `imagePathOrEmpty`

```js
function fixImagePath(imgPath) { return imgPath || ''; }   // لا تُصلح شيئاً
```

اسم يَعِد بما يفعله مقابله في PHP: بادئة `images/` للمسارات بلا شرطة، وتمرير
الروابط المطلقة، وصورة بديلة عند الفراغ. **وقد انفجر هذا الوعد فعلاً** في محرّر
السلايدر (أُصلح في متابعات المرحلة 5).

الاسم الجديد يصف ما تفعله، ورأس الملف يوثّق القاعدة: **إصلاح مسار الصورة
مسؤولية الخادم دائماً**. الاسم القديم بقي موجَّهاً للجديد كي لا ينكسر أي مستدعٍ
خارجي.

### آخر 23 `var` ← `const`

في أربعة ملفات، بعد فحص كل موضع (لا إسناد لاحق لأي منها).

**`const` لا يُكشف خطؤه بـ`node --check`** — إعادة الإسناد خطأ وقت تشغيل — فشغّلتُ
الملفات الأربعة فعلياً: نافذة الرد في الدعم تُفتح · فورم الإعدادات يُرسل ويستعيد
نص زره من `originalText` المحوَّل · `const p = new URLSearchParams(...)` يليه
`p.set/p.delete` (استدعاءات على الكائن لا إعادة إسناد) · صفحة إعادة التعيين
تتحقق وترسل.

---

## 3. الجزء 6ب-1 — دعم أجسام JSON

### وصفي في الخطة كان مخفَّفاً — وأصحّحه

كتبتُ أن الدالة «تدعم `FormData` و`urlencoded` فقط». **غير دقيق**: هي لا
**تتجاهل** JSON بل **تفسده**. كل جسم نصّي كان يمرّ على `URLSearchParams`:

```
new URLSearchParams('{"csrf_token":"OLD","items":[…]}')
  →  %7B%22csrf_token%22%3A%22OLD%22%2C%22items%22…=&csrf_token=NEW
```

مفتاح واحد مُرمَّز لا يستطيع `json_decode` قراءته. أي أن شبكة الأمان كانت
**تفشل حتماً** لكل نقطة ترسل JSON — لا تتعطّل أحياناً بل دائماً.

**أثبتُّه على الخادم الحقيقي**: أرسلتُ ما تنتجه النسخة القديمة إلى نقطة JSON
فعلية فرُفض بـ«Invalid CSRF token» **رغم أن التوكن الجديد داخل الجسم**.

### ما تغيّر

استُخرجت `rebuildBodyWithToken()` بثلاثة مسارات صريحة — `FormData` · **JSON** ·
`urlencoded` — و`headerValue()` لقراءة الترويسة من أشكال `fetch` الثلاثة (كائن ·
`Headers` · مصفوفة أزواج) بلا حساسية لحالة الأحرف. كشف JSON بالترويسة أولاً ثم
احتياطاً بشكل الجسم.

**والأهم — ما لا يُعاد**: الدالة ترفض إعادة المحاولة لما لا تعرف إعادة بنائه
(JSON تالف · مصفوفة JSON لا موضع فيها للتوكن · `Blob`). النسخة القديمة كانت في
هذه الحالات تعيد الطلب بجسم بلا توكن جديد — طلب مهدور يفشل حتماً.

وأُبقي إصلاح «أضف التوكن إن غاب» في مسار `FormData`، وهو الذي عالج عطل فورم
`forgot-password` سابقاً.

---

## 4. الجزء 6أ — قاعدة بادج المخزون

كانت مكتوبة **ثلاث مرات في لغتين**:

| الموضع | مخزون > 50 |
|---|---|
| `app/helpers/stock_badge_helper.php` | حسب الوسيط |
| `js/features/wishlist.js::getStockBadgeJs` | **لا شيء** |
| `js/features/product-details.js` (كتلة `if/else`) | **In Stock (N) أخضر** |

النسختان في JS **تختلفان عن بعضهما** في الفرع الثالث — نفس الفرق الذي اكتُشف بين
PHP والـview في المرحلة 5. اتفاقهما كان مصادفة صيانة لا ضماناً، والعتبة 50 مكتوبة
في ثلاثة أماكن.

الآن `stockBadge(stock, showInStock)` في `js/core/utils.js` بنفس توقيع PHP ونفس
شكل المخرَج. وُضعت في `utils.js` لأنه محمَّل في الفوترين معاً فلا يحتاج وسماً
جديداً.

**الملفان يشيران لبعضهما صراحةً**: رأس كلٍّ منهما يقول إن الآخر مرآته وإن تغيير
العتبة أو النصوص يجب أن يقع في الاثنين. التكرار بين لغتين لا مفرّ منه في مشروع
بلا خطوة بناء تشارك الثوابت — لكن **مكانه صار معروفاً وموثَّقاً** بدل أن يكون
مبعثراً في ثلاثة ملفات.

---

## 5. الجزء 6ب-2 — التحويل، وما استُبعد بالقياس

### 6ب-2: تحويل الـ13 ملفاً ✅ نُفِّذ

هذا ما كانت الفجوة تعنيه بالضبط، وهو ما بُنيت عليه ضرورة التحويل. قِستُه بتجربة
حقيقية (بلا لمس أي حساب — عبر مسار «البريد فارغ» الذي يُدوّر التوكن قبل أي فحص):

```
1. صفحة /admin/users تطبع التوكن 1b4f2328…      ✓ مطابق للجلسة
2. الأدمن يخطئ في فورم الدخول → التوكن 277d2194…  ⚠️ دُوِّر
3. الصفحة المفتوحة ما زالت تحمل القديم:

   بلا شبكة أمان:  "Invalid CSRF token…"  ✗ الطلب ضاع
   مع شبكة الأمان: تجلب توكناً وتعيد الطلب  ✓ وصل للمنطق
```

التوكن في هذا المشروع **واحد لكل جلسة**، و`AdminAuthController` وحده يُدوّره
**14 مرة**. فأي فشل في فورم واحد يُبطل التوكن في كل صفحة أخرى مفتوحة.

#### ما نُفِّذ

**30 موضع نداء POST في 13 ملفاً** حُوِّلت إلى `fetchWithCsrfRetry`، في ست خطوات
بـcommit واختبار HTTP بعد كل خطوة كما اقتُرح:

| الملف | مواضع محوَّلة | الملف | مواضع محوَّلة |
|---|---:|---|---:|
| `admin/products` | 4 | `admin/users` | 3 |
| `admin/admin-notifications` | 4 | `features/account` | 3 |
| `admin/category-picker` | 3 | `admin/admins` | 2 |
| `admin/manage-admins` | 3 | `admin/backup` | 2 |
| `admin/my-info` | 2 | `admin/store-reauth` | 1 |
| `features/contact` | 1 | `features/notify-stock` | 1 |
| `shared/order-cancel` | 1 | | |

#### الحصيلة بعد التحويل

| | |
|---|---:|
| ملفات ترسل POST | **23** |
| تستعمل شبكة الأمان في نقطة واحدة على الأقل | **19** |
| كل نقاطها عارية — بسبب موثَّق فوق كل نقطة | **3** |
| فيها نقطة عارية واحدة — بسبب موثَّق | **2** |
| بغلاف محلي خاص بها | **1** (`features/checkout.js` — القسم 6.4) |
| مواضع POST: بالشبكة / عارية بسبب موثَّق | **50 / 5** |

**الخمس نقاط العارية، وسبب كلٍّ منها مكتوب فوق سطرها:**

| النقطة | السبب |
|---|---|
| `/admin/logout` (`admin-navbar.js`) | لا تتحقق من CSRF (تُدمّر الجلسة)، وتُرجع تحويل 302 لا JSON — فالغلاف كان سيرمي عليها |
| `/auth/logout` (`auth.js`) | لا تتحقق من CSRF (تُدمّر الجلسة). ولا شيء لشبكة الأمان لتتعافى منه |
| `/cart/check-stock` (`cart.js`) | لا تتحقق من CSRF (استعلام مخزون للقراءة)، والطلب لا يرسل توكناً أصلاً |
| `/admin/products/categories/suggest` (`category-picker.js`) | `suggestCategory` لا تتحقق من CSRF إطلاقاً — تفحص الصلاحية فقط |
| نقطة دخول الأدمن (`admin-auth.js`) | ثلاثة أسباب مفحوصة، أدناه |

**`admin/admin-auth.js` هو الملف الوحيد الذي كان تحذيره صحيحاً.** ثلاثة أسباب
مستقلة، كلٌّ منها كافٍ وحده: (1) `window.URLROOT` **غير معرَّف في صفحة تسجيل
الدخول** — تحقّقت: صفر إشارة إليه في `admin/login.php`، فالملف يقرأ الجذر من
`<meta name="urlroot">`؛ فالغلاف سيسقط إلى فرع `BASE_URL` وهو غير معرَّف هناك
أيضاً، أي أن إعادة المحاولة ستضرب `undefined/auth/csrf`. (2) الملف **يجدّد
التوكن بنفسه** عبر `refreshCsrfToken()` بعد كل فشل — نفس العمل مكتوباً لسياق
الصفحة. (3) يستعمل `res.ok` قبل قراءة JSON، والغلاف يُرجع بيانات لا `Response`.

#### اكتشاف: التحذير في ستة ملفات كان خاطئاً منذ كُتب

ستة ملفات أدمن حملت تعليقاً يقول: «لا تستخدم `fetchWithCsrfRetry` لأنها تعتمد
على `window.BASE_URL` غير الموجود بصفحات الأدمن».

**غير صحيح.** الدالة تفحص `URLROOT` **أولاً**:

```js
(typeof window.URLROOT !== 'undefined')
  ? window.URLROOT + '/admin/csrf'     // ← صفحات الأدمن
  : window.BASE_URL + '/auth/csrf';
```

وهذا السطر لم يتغيّر **منذ commit الـbaseline** (`841d64d`) — تحقّقت بـ
`git log -S`. و`admin/inc/head.php` يضبط `URLROOT` على كل صفحة أدمن، فلا تُلمس
`BASE_URL` هناك إطلاقاً. والدليل العملي: `admin/orders.js` كان يستعملها خمس مرات
في صفحات الأدمن ويعمل. أُزيل التحذير من الخمسة، وبقي في `admin-auth.js` وحده
مُعاد الصياغة بسببه الحقيقي. **صفر بقايا للتحذير الخاطئ في المشروع الآن.**

#### عطل مُصلَح: «إضافة عنوان» لم تعمل ولا مرة ✅

في `features/account.js` كانت نتيجة الطلب تُسمّى `data` — **نفس اسم** متغيّر
حقول الفورم المُعلَن فوق `try`:

```js
const data = Object.fromEntries(new FormData(form));   // خارج try
try {
    const res  = await fetch(url, { body: JSON.stringify(data) });  // ← هنا
    const data = await res.json();                                  // ← يُظلّله
}
```

`const` مرتبطة بالكتلة، فالتصريح الداخلي يُظلّل الخارجي **داخل `try` كله**،
والسطر الذي يبني الجسم يقرأ `data` في **منطقته الميتة** فيرمي
`ReferenceError: Cannot access 'data' before initialization`. ثم يبتلعه
`catch (err)` الخاص بالملف ويعرض **«Network error, please try again.»** — ولهذا
بدا العطل مشكلة شبكة ولم يُلاحَظ.

أُعيدت تسمية النتيجة إلى `result`، وحُدِّث الشرط `if (data.success)` الذي كان
يقرأ المتغيّر الخطأ أيضاً.

**أُثبت بضابط سالب:** شُغِّل الاختبار على النسخة **قبل** الإصلاح (بتراجع git
حقيقي) فأعطى: الطلب **لم يُرسَل إطلاقاً** والمستخدم يرى «Network error». وبعده:
الطلب يُرسَل بجسم يحمل حقول الفورم، والمستخدم يرى رسالة الخادم. الاختبار يميّز
الحالتين — وهذا ما يجعله اختباراً لا طقساً.

#### تصحيح رقمين في رسائل الـcommits

رسالة الـcommit الختامية (`fd3538f`) ذكرت **«26 نقطة POST»** و**«18 تستعمل
الشبكة و4 مستثناة»**. إعادة الجرد آلياً — بأداة واحدة على نسختَي ما قبل وما بعد —
تعطي **30 موضع نداء** و**19 مستعمِلة و3 عارية بالكامل**. الفرق في الثاني سببه
واضح: `features/auth.js` عُدَّت هناك «مستثناة» رغم أنها **تستعمل الشبكة في ثلاث
نقاط** ولا تستثني إلا نقطة الخروج. والأرقام المثبتة هنا هي المُعاد قياسها.

### بنود من الخطة الأصلية بدّدها القياس

| البند | لماذا لم يُنفَّذ |
|---|---|
| مراجعة `innerHTML` للـXSS | الملفات الخمسة الأكبر في اللوحة فيها **صفر `innerHTML`**؛ والتي تبني HTML بالبيانات تستعمل `escHtml` أصلاً؛ والباقي نصوص حرفية (تسميات أزرار ودوّامات) |
| تنظيف المستمعات والمؤقتات | خمسة من سبعة `setInterval` تُنظَّف، والاثنان الباقيان استطلاع بعمر الصفحة يقتله تفريغها |
| توحيد ملفَي الإشعارات | متشابهان لا متطابقان: جلستان ونقطتان وماركب مختلف |
| تقسيم `admin/products.js` (369 سطراً) | لا تكرار فيه ولا منطق مبعثر — طوله من عدد الأزرار |
| `'use strict'` للـ28 ملفاً | تغيير سلوك محتمل في كل ملف مقابل فائدة نظرية |

---

## 6. ما اكتُشف ولم يُصلَح — للنظر لاحقاً

### 6.1 `features/notifications.js` يحمل فرعاً ميتاً كاملاً

الملف (330 سطراً) مصمَّم بوضعين: يكتشف `#adminNotifBell` فيتصرّف كملف أدمن، وإلا
فكملف متجر. لكن `#adminNotifBell` موجود في `admin/inc/navbar.php` **وحده**، وهذا
الملف يُحمَّل في فوتر المتجر **وحده** — تحقّقت: صفر إشارة إليه في فوتر الأدمن.

النتيجة: `isAdmin` **دائماً false**، و**11 فرعاً** في الملف لا تُنفَّذ أبداً.

لم أحذفها: إزالة فرع من بنية ملف بأكمله تغيير جوهري لا تنظيف، ويحتاج قراراً عن
النيّة الأصلية — أكان المقصود أن يخدم الملف الجانبين ثم انقسم، أم أن الفرع بقايا؟

### 6.2 `#csrfTokenSupport` بلا سمة `name`

`updateCsrfToken` تستهدف `input[name="csrf_token"]`، وحقل صفحة الدعم يحمل `id`
بلا `name` — فلا يُحدَّث بعد التعافي (`window._csrfToken` يُحدَّث). الأثر: أول
إرسال يدوي تالٍ يحتاج جولة تعافٍ أخرى.

### 6.3 رابط ميت في صفحة الدعم

بطاقة المستخدم تنقل إلى `/admin/users/{id}` — راوت **غير موجود**، فتظهر صفحة
الـ404. اكتشفتُه بالصدفة أثناء اختبار آخر (نقرة نقلتني إلى `/admin/users/100`).
موثَّق في `admin/support.js` نفسه كـ`TODO` منذ كُتب.

---

### 6.4 `features/checkout.js` يحمل غلافاً محلياً بتعليق صار قديماً

اكتُشف أثناء إعادة الجرد لهذا التحديث. الملف **لم يكن ضمن الـ13** لأنه يمرّر
الطريقة بمتغيّر (`fetchWithCsrf(url, 'POST', data)`) فلا يطابقه بحث
`method: 'POST'`. ويعرّف غلافاً محلياً باسمه الخاص يرسل نقطتين
(`/user/addresses` و`/checkout`) بلا أي إعادة محاولة.

وتعليقه يبرّر ذلك بـ: «تلك تدعم `FormData` و`urlencoded` فقط؛ وهذه الصفحة ترسل
JSON، وإعادة بناء جسم JSON ليست ضمن ما تعرفه تلك الدالة».

**كان صحيحاً عند كتابته، وصار قديماً بعد 6ب-1 نفسها** — التي علّمت
`fetchWithCsrfRetry` إعادة بناء أجسام JSON بالضبط. أي أن السبب المكتوب لم يعد
قائماً، ونقطتا الدفع بلا شبكة أمان بلا مبرّر حالي.

**لم أصلحه:** خارج البند الموافَق عليه (تحديث هذا التقرير). يحتاج قراراً.

---

## 7. أخطاء ارتكبتُها أثناء العمل

### 7.1 «معرّف غير موجود ⇒ بلا أثر» — افتراض خاطئ، وقعتُ فيه مرتين

بنيتُ اختبارات الـHTTP على أن نقطة تُستدعى بمعرّف غير موجود لا تترك أثراً.
**خاطئ لكل نقطة لا تأخذ معرّفاً أصلاً:**

| النقطة | ما فعلته بالخطأ |
|---|---|
| `/admin/products/categories/add` | أنشأت تصنيفاً حقيقياً في قاعدة البيانات |
| `/admin/backup/create` | أنشأت ملف نسخة احتياطية 79KB |
| `/admin/notifications/mark-all-read` | علّمت إشعارات أدمن كمقروءة |

والأسوأ أن الثالثة جاءت في الدفعة **التالية** للأولَيين — أي أنني تعلّمت الدرس
ولم أعمّمه. حذفتُ التصنيف وملف النسخة وأربعة صفوف أنشأتها الاختبارات، وتحقّقت
بعدها: 16 منتجاً · 4 تصنيفات · صفر نسخ احتياطية · سجل التدقيق عاد إلى آخر صفّ
أصلي (403).

**لكن حالة القراءة غير قابلة للاسترجاع**: لا عمود `read_at` في
`admin_notifications`، فلا سبيل لمعرفة أيّ الإشعارات كان غير مقروء قبل الاختبار.
الصفوف التي أنشأتها الاختبارات حُذفت، لكن حالة القراءة للصفوف الأقدم بقيت كما
تركها الاختبار. أذكره صراحةً بدل أن أمرّره.

استُبعدت النقطة من الاختبار، ووُثّقت القاعدة في رأسه: **لا تُضاف نقطة إلا بعد
التأكد أن معرّفاً غير موجود يجعلها بلا أثر.**

### 7.2 فشلان بدَوا في الكود وكانا في الاختبار

- `users/delete` بتوكن باطل ظهر كأنه يُرجع «استجابة غير JSON». بالاختبار المباشر
  يُرجع JSON سليماً — كان **أول طلب بعد خمول** يعود فارغاً في هذه البيئة (تكرّر
  في اليوم نفسه مع سكربت الدخان). أُضيف طلب إحماء قبل القياس.
- `backup/create` ظهر مرفوضاً — بل **نجح على الخادم** وانتهت مهلة curl (20 ثانية)
  أثناء `mysqldump`. سجل التدقيق أثبت نجاحه.

صار الاختبار يطبع الاستجابة الخام بدل «(غير JSON)» المبهمة، كي لا يتكرّر تشخيص
خاطئ يقود إلى «إصلاح» كود سليم.

### 7.3 حذف جلسات الاختبار بنمط بادئة بدل الأسماء الدقيقة

استُعمل `Remove-Item sess_(cv|dbg|st|…)*` لتنظيف جلسات الاختبار، وقد يكون طال
**جلسات متصفح حقيقية** في مسار الجلسات نفسه. الأثر على صاحب المشروع: تسجيل دخول
من جديد.

**كان يجب الحذف بالاسم الكامل** — وأسماء الجلسات التي أنشأتها كانت مسجَّلة عندي
أصلاً، فلم يكن للنمط أي مبرّر.

### 7.4 رقمان غير دقيقين في رسالة الـcommit الختامية

«26 نقطة» و«18 تستعمل الشبكة و4 مستثناة» — الصحيح 30 و19/3. التفصيل في نهاية
القسم 5. الرقمان كُتبا من عدّ يدوي تراكمي عبر ست خطوات بدل جرد واحد على النسختين.

---

## 8. التحقق

| الفحص | النتيجة |
|---|---|
| سكربت الدخان — 44 راوت GET | ✅ 44/44 |
| `php -l` — كل ملفات المشروع | ✅ |
| `node --check` على كل ملف JS ملموس | ✅ |
| `audit-imports.php` | ✅ نظيف |
| `composer dump-autoload -o` | ✅ صفر تحذيرات · 920 كلاساً |
| `composer docs:generate` | ✅ صفر تحذيرات |
| **اختبار وحدة: شبكة CSRF — 23 تأكيداً** | ✅ `node` يحمّل الملف **الحقيقي** في سياق معزول مع `fetch` مزيّف. يغطي: JSON يُعاد بناؤه مع بقاء الحقول وعربيتها · التوكن يُضاف عند غيابه · `urlencoded` و`FormData` بلا تغيير · الترويسة بأشكالها الثلاثة وبحالات أحرف مختلفة · JSON بلا ترويسة · أربع حالات لا يجوز فيها الإعادة · النجاح من أول مرة لا يطلب توكناً |
| **اختبار HTTP: شبكة CSRF — 6 تأكيدات** | ✅ دورة تدوير توكن فعلية، ثم إثبات أن الجسم المشوَّه (إنتاج القديمة) يُرفض وأن المُعاد بناؤه (إنتاج الجديدة) يُقبل ويولّد secret فعلاً |
| **في المتصفح: شبكة CSRF** | ✅ بتوكن باطل عمداً على صفحة أدمن حقيقية — `FormData` تعافى، و**JSON تعافى ووصل للمنطق** (`success: true`) وهو ما كان مستحيلاً قبل التوسيع |
| **اختبار تكافؤ: بادج المخزون — 22 تأكيداً** | ✅ يشغّل دالة PHP الحقيقية ودالة JS الحقيقية على القيم نفسها ويقارن المخرجات: 0 · 1 · 2 · 25 · 49 · 50 · 51 · 52 · 100 · 9999 × وضعَي الوسيط. تطابق تام |
| **في المتصفح: بادج المخزون — الفروع الثلاثة** | ✅ من الخادم: `In Stock (60)` · تبديل لمخزون 0: `Out of Stock` أحمر · رجوع: **نفس نصّ الخادم بالضبط**. وصنف `fs-6` يبقى سليماً |
| **صفحة المفضّلة** | ✅ البادجان بالقاعدة الموحّدة، و**صفر بادج أخضر** — الضابط الذي يثبت أن الوسيط يعمل |
| **الملفات الأربعة المحوَّلة إلى `const`** | ✅ شُغِّلت كلها فعلياً في المتصفح |
| **صفحة المفضّلة: صفر وسم مكرر** | ✅ 17 ← 16 |
| **6ب-2 — اختبار HTTP على نقاط حقيقية بعد كل خطوة** | ✅ لكل نقطة حُوِّلت: التوكن الباطل **يُرفض** (فهناك ما تتعافى منه)، والتوكن الطازج من `/admin/csrf` **يُقبل ويصل للمنطق** (فالتعافي مجدٍ). يشمل نقطتَي JSON (`my-info` و`2fa/generate`) بترويسة `Content-Type: application/json` حقيقية. أعداد التأكيدات كما سجّلتها الـcommits: 14 في الخطوة (2-4/13) و24 في (5-8/13)، وثلاث نقاط متجر في (9-13/13) |
| **6ب-2 — اختبار وحدة لـ`account.js` مع ضابط سالب** | ✅ `node` يحمّل الملف **الحقيقي** في سياق معزول ويطلق `submit`: 4 تأكيدات. وعلى النسخة قبل الإصلاح (بتراجع git) الطلب **لم يُرسَل إطلاقاً** — الاختبار يميّز الحالتين |
| **6ب-2 — تعافٍ حقيقي في `admin/users.js`** | ✅ بتوكن باطل عمداً، الطلب وصل للمنطق («User not found.» لا رسالة CSRF) |
| **6ب-2 — `store-reauth` بتوكن باطل** | ✅ يُرفض ويُعيد توكناً جديداً في الاستجابة |
| **6ب-2 — `node --check` على كل ملف JS في المشروع** | ✅ |
| **6ب-2 — عشر صفحات أدمن بعد كل التحويلات** | ✅ 200 لكلٍّ |
| **6ب-2 — جرد آلي قبل/بعد بأداة واحدة** | ✅ 20 → **50** موضع POST بالشبكة · 35 → **5** عارية · 16 → **3** ملفات عارية بالكامل |

### بيانات الاختبار — نُظّفت كلها

| ما أُنشئ | الحالة |
|---|---|
| جلسات أدمن ومستخدم مزوّرة (5) | ✅ صفر |
| رمز إعادة تعيين | ✅ حُذف |
| **مخزون المتغيّر 67 رُفع إلى 60 مؤقتاً** | ✅ أُعيد إلى 32 **وتُحقِّق من القيمة بعد الإرجاع** |
| `localStorage` (مفضّلة، سلة) | ✅ نُظّف |
| `admins.totp_enabled` | ✅ ما زال 0 بلا secret |

> **لماذا رفعتُ المخزون؟** لا يوجد في قاعدة البيانات متغيّر بمخزون > 50، فالفرع
> الأخضر لم يكن قابلاً للاختبار حيّاً. رفعتُ واحداً، اختبرتُ، وأعدتُه.

---

## 9. القياس التراكمي منذ البداية

| المؤشر | البداية | الآن |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | 14 | **0** ✅ |
| حُرّاس `function_exists()` زائدة | 16 | **0** ✅ |
| تحذيرات PSR-4 من composer | 48 | **0** ✅ |
| كنترولرز بلا توثيق OpenAPI | 10 | **0** ✅ |
| نسخ `respond()` المكرّرة | 16 | **1** ✅ |
| صفحات تكتب `<!DOCTYPE>` بيدها | 3 | **0** ✅ |
| أسطر `<style>` مضمّنة | 55 | **0** ✅ |
| partials في `views/shared` | 1 | **7** ✅ |
| أسطر `<script>` مضمّنة | 579 | **91** (بيانات فقط) ✅ |
| ملفات JS ميتة أو مكرّرة التحميل | 2 | **0** ✅ |
| `console.log` في الإنتاج | 2 | **0** ✅ |
| `var` في JS | 27 | **0** ✅ |
| مواضع POST بلا شبكة أمان CSRF | 35 | **5** (كلها بسبب موثَّق) ✅ |
| **أعطال مكتشفة** | — | **22** |

---

## 10. ما يليه

**خطة المرحلة 6 اكتملت بأجزائها الأربعة.** لم يبقَ منها بند غير منفَّذ.

وأربعة بنود موثّقة في القسم 6 تنتظر قرارك، ولا أنفّذ منها شيئاً بلا موافقة صريحة:

| البند | الأثر | الحجم |
|---|---|---|
| 6.1 فرع أدمن ميت في `features/notifications.js` | 11 فرعاً لا تُنفَّذ أبداً | متوسط — قرار بنيوي |
| 6.2 `#csrfTokenSupport` بلا `name` | أول إرسال يدوي بعد التعافي يحتاج جولة أخرى | صغير |
| 6.3 رابط ميت إلى `/admin/users/{id}` | المستخدم يرى صفحة 404 | صغير |
| 6.4 غلاف `checkout.js` بتعليق قديم | نقطتا الدفع بلا شبكة أمان بلا مبرّر حالي | صغير |

وبنود أخرى خارج نطاق المرحلة 6 موثّقة في تقرير المرحلة 5، القسمين 5.6 و5.7.

---

## 11. الأدوات

```bash
php scripts/smoke-test.php        # 44 راوت + صياغة المعدَّل
php scripts/smoke-test.php --lint-all
php scripts/audit.php             # قياس حالة الكود
php scripts/audit-imports.php     # كلاسات غير مستوردة + استيرادات ميتة
php scripts/audit-escaping.php    # تصنيف مخرجات الـviews أمنياً
composer docs:generate
composer dump-autoload -o
```
