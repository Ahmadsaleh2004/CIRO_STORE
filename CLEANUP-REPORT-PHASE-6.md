# تقرير المرحلة 6 — طبقة الـJS

> **الحالة: ثلاثة أجزاء من أربعة منجزة.** الجزء الرابع (تحويل الملفات الـ13 إلى
> شبكة أمان CSRF) **لم يُنفَّذ بقرارك** — اخترتَ توسيع الدالة فقط.
> التاريخ: 2026-08-25 · الخطة: [CLEANUP-PLAN-PHASE-6.md](CLEANUP-PLAN-PHASE-6.md)

---

## 1. ما أُنجز

| الجزء | الحالة |
|---|---|
| **6ج** — التنظيف | ✅ |
| **6ب-1** — `fetchWithCsrfRetry` تدعم أجسام JSON | ✅ |
| **6أ** — قاعدة بادج المخزون موحّدة | ✅ |
| **6ب-2** — تحويل الملفات الـ13 | ⛔ **لم يُنفَّذ بقرارك** |

### المؤشرات

| | قبل | بعد |
|---|---:|---:|
| ملفات JS | 35 | **34** |
| `console.log` في الإنتاج | 2 | **0** ✅ |
| `var` | 23 | **0** ✅ |
| ملفات محمَّلة مرتين | 1 | **0** ✅ |
| نسخ قاعدة بادج المخزون | **3** (في لغتين) | **2** (واحدة لكل لغة) ✅ |
| دوال باسم لا يصف سلوكها | 1 | **0** ✅ |
| أشكال أجسام تدعمها شبكة CSRF | 2 | **3** ✅ |

---

## 2. الجزء 6ج — التنظيف

### حُذف `js/admin/admin-layout/admin-footer.js` كاملاً (26 سطراً)

كان يربط أزرار سايدبار إشعارات الأدمن الثلاثة — والثلاثة يربطها
`admin-notifications.js` فعلاً:

| الزر | ما في الملف المحذوف | ما في الملف الحقيقي |
|---|---|---|
| Mark All Read | `console.log('TODO: …')` | `POST /mark-all-read` |
| Delete All | `console.log('TODO: …')` | `POST /delete-all` |
| Close | يزيل `.open` **ويترك الـbackdrop ظاهراً** | `toggleSidebar(false)` — يزيل الاثنين |

لم يظهر عطل من زر الإغلاق لأن المعالج الصحيح يعمل معه ويكمل المهمة. تحقّقت أن
`admin-notifications.js` يعمل دائماً: الجرس والبادج والسايدبار ماركب غير مشروط،
فحارسه المبكر لا يُفعَّل أبداً.

### `wishlist.js` كان يُحمَّل مرتين

فوتر المتجر يحمّله على كل صفحة، و`WishlistController::index` يحمّله مجدداً عبر
`extraScripts`. قِستُه في المتصفح: وسمان لنفس الملف. 266 سطراً تُحلَّل وتُنفَّذ
مرتين، ومعالج `DOMContentLoaded` يعمل مرتين.

لم يظهر عطل لأن العرض يستبدل `innerHTML` كاملاً فيمحو أثر التنفيذ الأول. بعد
الحذف: **17 وسماً ← 16**، وصفر تكرار.

### `fixImagePath` ← `imagePathOrEmpty`

```js
function fixImagePath(imgPath) { return imgPath || ''; }   // لا تُصلح شيئاً
```

اسم يَعِد بما يفعله مقابله في PHP: بادئة `images/` للمسارات بلا شرطة، وتمرير
الروابط المطلقة، وصورة بديلة عند الفراغ. **وقد انفجر هذا الوعد فعلاً** في محرّر
السلايدر (أُصلح في متابعات المرحلة 5).

الاسم الجديد يصف ما تفعله، ورأس الملف يوثّق القاعدة: **إصلاح مسار الصورة
مسؤولية الخادم دائماً**. الاسم القديم بقي موجَّهاً للجديد كي لا ينكسر أي مستدعٍ
خارجي.

### آخر 23 `var` ← `const`

في أربعة ملفات، بعد فحص كل موضع (لا إسناد لاحق لأي منها).

**`const` لا يُكشف خطؤه بـ`node --check`** — إعادة الإسناد خطأ وقت تشغيل — فشغّلتُ
الملفات الأربعة فعلياً: نافذة الرد في الدعم تُفتح · فورم الإعدادات يُرسل ويستعيد
نص زره من `originalText` المحوَّل · `const p = new URLSearchParams(...)` يليه
`p.set/p.delete` (استدعاءات على الكائن لا إعادة إسناد) · صفحة إعادة التعيين
تتحقق وترسل.

---

## 3. الجزء 6ب-1 — دعم أجسام JSON

### وصفي في الخطة كان مخفَّفاً — وأصحّحه

كتبتُ أن الدالة «تدعم `FormData` و`urlencoded` فقط». **غير دقيق**: هي لا
**تتجاهل** JSON بل **تفسده**. كل جسم نصّي كان يمرّ على `URLSearchParams`:

```
new URLSearchParams('{"csrf_token":"OLD","items":[…]}')
  →  %7B%22csrf_token%22%3A%22OLD%22%2C%22items%22…=&csrf_token=NEW
```

مفتاح واحد مُرمَّز لا يستطيع `json_decode` قراءته. أي أن شبكة الأمان كانت
**تفشل حتماً** لكل نقطة ترسل JSON — لا تتعطّل أحياناً بل دائماً.

**أثبتُّه على الخادم الحقيقي**: أرسلتُ ما تنتجه النسخة القديمة إلى نقطة JSON
فعلية فرُفض بـ«Invalid CSRF token» **رغم أن التوكن الجديد داخل الجسم**.

### ما تغيّر

استُخرجت `rebuildBodyWithToken()` بثلاثة مسارات صريحة — `FormData` · **JSON** ·
`urlencoded` — و`headerValue()` لقراءة الترويسة من أشكال `fetch` الثلاثة (كائن ·
`Headers` · مصفوفة أزواج) بلا حساسية لحالة الأحرف. كشف JSON بالترويسة أولاً ثم
احتياطاً بشكل الجسم.

**والأهم — ما لا يُعاد**: الدالة ترفض إعادة المحاولة لما لا تعرف إعادة بنائه
(JSON تالف · مصفوفة JSON لا موضع فيها للتوكن · `Blob`). النسخة القديمة كانت في
هذه الحالات تعيد الطلب بجسم بلا توكن جديد — طلب مهدور يفشل حتماً.

وأُبقي إصلاح «أضف التوكن إن غاب» في مسار `FormData`، وهو الذي عالج عطل فورم
`forgot-password` سابقاً.

---

## 4. الجزء 6أ — قاعدة بادج المخزون

كانت مكتوبة **ثلاث مرات في لغتين**:

| الموضع | مخزون > 50 |
|---|---|
| `app/helpers/stock_badge_helper.php` | حسب الوسيط |
| `js/features/wishlist.js::getStockBadgeJs` | **لا شيء** |
| `js/features/product-details.js` (كتلة `if/else`) | **In Stock (N) أخضر** |

النسختان في JS **تختلفان عن بعضهما** في الفرع الثالث — نفس الفرق الذي اكتُشف بين
PHP والـview في المرحلة 5. اتفاقهما كان مصادفة صيانة لا ضماناً، والعتبة 50 مكتوبة
في ثلاثة أماكن.

الآن `stockBadge(stock, showInStock)` في `js/core/utils.js` بنفس توقيع PHP ونفس
شكل المخرَج. وُضعت في `utils.js` لأنه محمَّل في الفوترين معاً فلا يحتاج وسماً
جديداً.

**الملفان يشيران لبعضهما صراحةً**: رأس كلٍّ منهما يقول إن الآخر مرآته وإن تغيير
العتبة أو النصوص يجب أن يقع في الاثنين. التكرار بين لغتين لا مفرّ منه في مشروع
بلا خطوة بناء تشارك الثوابت — لكن **مكانه صار معروفاً وموثَّقاً** بدل أن يكون
مبعثراً في ثلاثة ملفات.

---

## 5. ما لم يُنفَّذ — وأسبابه

### 6ب-2: تحويل الـ13 ملفاً ⛔ بقرارك

الدالة صارت جاهزة، والتحويل بقي تذكرة مستقلة. **الفجوة ما زالت قائمة**، وهذا ما
تعنيه بالضبط:

قِستُه بتجربة حقيقية (بلا لمس أي حساب — عبر مسار «البريد فارغ» الذي يُدوّر التوكن
قبل أي فحص):

```
1. صفحة /admin/users تطبع التوكن 1b4f2328…      ✓ مطابق للجلسة
2. الأدمن يخطئ في فورم الدخول → التوكن 277d2194…  ⚠️ دُوِّر
3. الصفحة المفتوحة ما زالت تحمل القديم:

   بلا شبكة أمان:  "Invalid CSRF token…"  ✗ الطلب ضاع
   مع شبكة الأمان: تجلب توكناً وتعيد الطلب  ✓ وصل للمنطق
```

التوكن في هذا المشروع **واحد لكل جلسة**، و`AdminAuthController` وحده يُدوّره
**14 مرة**. فأي فشل في فورم واحد يُبطل التوكن في كل صفحة أخرى مفتوحة.

**الملفات الـ13:** `admin/products` (4 POST) · `admin/category-picker` (4) ·
`admin/users` (3) · `features/account` (3) · `admin/my-info` · `admin/backup` (2
لكلٍّ) · و7 بـPOST واحد. وأربعة أخرى تستعملها جزئياً
(`admin-notifications` 4 POST/1 retry · `manage-admins` 3/1 · `admins` 2/1).

### بنود من الخطة الأصلية بدّدها القياس

| البند | لماذا لم يُنفَّذ |
|---|---|
| مراجعة `innerHTML` للـXSS | الملفات الخمسة الأكبر في اللوحة فيها **صفر `innerHTML`**؛ والتي تبني HTML بالبيانات تستعمل `escHtml` أصلاً؛ والباقي نصوص حرفية (تسميات أزرار ودوّامات) |
| تنظيف المستمعات والمؤقتات | خمسة من سبعة `setInterval` تُنظَّف، والاثنان الباقيان استطلاع بعمر الصفحة يقتله تفريغها |
| توحيد ملفَي الإشعارات | متشابهان لا متطابقان: جلستان ونقطتان وماركب مختلف |
| تقسيم `admin/products.js` (369 سطراً) | لا تكرار فيه ولا منطق مبعثر — طوله من عدد الأزرار |
| `'use strict'` للـ28 ملفاً | تغيير سلوك محتمل في كل ملف مقابل فائدة نظرية |

---

## 6. ما اكتُشف ولم يُصلَح — للنظر لاحقاً

### 6.1 `features/notifications.js` يحمل فرعاً ميتاً كاملاً

الملف (330 سطراً) مصمَّم بوضعين: يكتشف `#adminNotifBell` فيتصرّف كملف أدمن، وإلا
فكملف متجر. لكن `#adminNotifBell` موجود في `admin/inc/navbar.php` **وحده**، وهذا
الملف يُحمَّل في فوتر المتجر **وحده** — تحقّقت: صفر إشارة إليه في فوتر الأدمن.

النتيجة: `isAdmin` **دائماً false**، و**11 فرعاً** في الملف لا تُنفَّذ أبداً.

لم أحذفها: إزالة فرع من بنية ملف بأكمله تغيير جوهري لا تنظيف، ويحتاج قراراً عن
النيّة الأصلية — أكان المقصود أن يخدم الملف الجانبين ثم انقسم، أم أن الفرع بقايا؟

### 6.2 `#csrfTokenSupport` بلا سمة `name`

`updateCsrfToken` تستهدف `input[name="csrf_token"]`، وحقل صفحة الدعم يحمل `id`
بلا `name` — فلا يُحدَّث بعد التعافي (`window._csrfToken` يُحدَّث). الأثر: أول
إرسال يدوي تالٍ يحتاج جولة تعافٍ أخرى.

### 6.3 رابط ميت في صفحة الدعم

بطاقة المستخدم تنقل إلى `/admin/users/{id}` — راوت **غير موجود**، فتظهر صفحة
الـ404. اكتشفتُه بالصدفة أثناء اختبار آخر (نقرة نقلتني إلى `/admin/users/100`).
موثَّق في `admin/support.js` نفسه كـ`TODO` منذ كُتب.

---

## 7. التحقق

| الفحص | النتيجة |
|---|---|
| سكربت الدخان — 44 راوت GET | ✅ 44/44 |
| `php -l` — كل ملفات المشروع | ✅ |
| `node --check` على كل ملف JS ملموس | ✅ |
| `audit-imports.php` | ✅ نظيف |
| `composer dump-autoload -o` | ✅ صفر تحذيرات · 920 كلاساً |
| `composer docs:generate` | ✅ صفر تحذيرات |
| **اختبار وحدة: شبكة CSRF — 23 تأكيداً** | ✅ `node` يحمّل الملف **الحقيقي** في سياق معزول مع `fetch` مزيّف. يغطي: JSON يُعاد بناؤه مع بقاء الحقول وعربيتها · التوكن يُضاف عند غيابه · `urlencoded` و`FormData` بلا تغيير · الترويسة بأشكالها الثلاثة وبحالات أحرف مختلفة · JSON بلا ترويسة · أربع حالات لا يجوز فيها الإعادة · النجاح من أول مرة لا يطلب توكناً |
| **اختبار HTTP: شبكة CSRF — 6 تأكيدات** | ✅ دورة تدوير توكن فعلية، ثم إثبات أن الجسم المشوَّه (إنتاج القديمة) يُرفض وأن المُعاد بناؤه (إنتاج الجديدة) يُقبل ويولّد secret فعلاً |
| **في المتصفح: شبكة CSRF** | ✅ بتوكن باطل عمداً على صفحة أدمن حقيقية — `FormData` تعافى، و**JSON تعافى ووصل للمنطق** (`success: true`) وهو ما كان مستحيلاً قبل التوسيع |
| **اختبار تكافؤ: بادج المخزون — 22 تأكيداً** | ✅ يشغّل دالة PHP الحقيقية ودالة JS الحقيقية على القيم نفسها ويقارن المخرجات: 0 · 1 · 2 · 25 · 49 · 50 · 51 · 52 · 100 · 9999 × وضعَي الوسيط. تطابق تام |
| **في المتصفح: بادج المخزون — الفروع الثلاثة** | ✅ من الخادم: `In Stock (60)` · تبديل لمخزون 0: `Out of Stock` أحمر · رجوع: **نفس نصّ الخادم بالضبط**. وصنف `fs-6` يبقى سليماً |
| **صفحة المفضّلة** | ✅ البادجان بالقاعدة الموحّدة، و**صفر بادج أخضر** — الضابط الذي يثبت أن الوسيط يعمل |
| **الملفات الأربعة المحوَّلة إلى `const`** | ✅ شُغِّلت كلها فعلياً في المتصفح |
| **صفحة المفضّلة: صفر وسم مكرر** | ✅ 17 ← 16 |

### بيانات الاختبار — نُظّفت كلها

| ما أُنشئ | الحالة |
|---|---|
| جلسات أدمن ومستخدم مزوّرة (5) | ✅ صفر |
| رمز إعادة تعيين | ✅ حُذف |
| **مخزون المتغيّر 67 رُفع إلى 60 مؤقتاً** | ✅ أُعيد إلى 32 **وتُحقِّق من القيمة بعد الإرجاع** |
| `localStorage` (مفضّلة، سلة) | ✅ نُظّف |
| `admins.totp_enabled` | ✅ ما زال 0 بلا secret |

> **لماذا رفعتُ المخزون؟** لا يوجد في قاعدة البيانات متغيّر بمخزون > 50، فالفرع
> الأخضر لم يكن قابلاً للاختبار حيّاً. رفعتُ واحداً، اختبرتُ، وأعدتُه.

---

## 8. القياس التراكمي منذ البداية

| المؤشر | البداية | الآن |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | 14 | **0** ✅ |
| حُرّاس `function_exists()` زائدة | 16 | **0** ✅ |
| تحذيرات PSR-4 من composer | 48 | **0** ✅ |
| كنترولرز بلا توثيق OpenAPI | 10 | **0** ✅ |
| نسخ `respond()` المكرّرة | 16 | **1** ✅ |
| صفحات تكتب `<!DOCTYPE>` بيدها | 3 | **0** ✅ |
| أسطر `<style>` مضمّنة | 55 | **0** ✅ |
| partials في `views/shared` | 1 | **7** ✅ |
| أسطر `<script>` مضمّنة | 579 | **91** (بيانات فقط) ✅ |
| ملفات JS ميتة أو مكرّرة التحميل | 2 | **0** ✅ |
| `console.log` في الإنتاج | 2 | **0** ✅ |
| **أعطال مكتشفة** | — | **20** |

---

## 9. ما يليه

**البند الوحيد المتبقي من خطة المرحلة 6 هو 6ب-2**: تحويل الـ13 ملفاً إلى شبكة
أمان CSRF. الدالة جاهزة، والتحويل قرارك متى شئت — أقترح تنفيذه **ملفاً ملفاً مع
commit واختبار HTTP بعد كل ملف**، لا دفعة واحدة.

وثلاثة بنود موثّقة في القسم 6 تنتظر قرارك أيضاً.

---

## 10. الأدوات

```bash
php scripts/smoke-test.php        # 44 راوت + صياغة المعدَّل
php scripts/smoke-test.php --lint-all
php scripts/audit.php             # قياس حالة الكود
php scripts/audit-imports.php     # كلاسات غير مستوردة + استيرادات ميتة
php scripts/audit-escaping.php    # تصنيف مخرجات الـviews أمنياً
composer docs:generate
composer dump-autoload -o
```
