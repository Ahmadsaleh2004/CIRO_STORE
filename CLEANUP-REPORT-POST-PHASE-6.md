# تقرير بنود ما بعد المرحلة 6

> **الحالة: البنود الستة كلها منجزة ومدموجة.** خمسة أعطال أُصلحت، وفرع
> ميت حُذف، وتقرير المرحلة 6 صُحِّح.
> التاريخ: 2026-08-26 · الفرع: `بنود-ما-بعد-المرحلة-6` (مدموج بـ`--no-ff`)

---

## 1. الحصيلة

| البند | ما كان | الحالة |
|---|---|---|
| **6.1 (تقرير)** | تقرير المرحلة 6 يصف 6ب-2 كـ«لم يُنفَّذ» بعد أن نُفِّذ | ✅ |
| **7.4** | حذف منتج غير موجود يُجيب «نجح» ويكتب سجل تدقيق كاذباً | ✅ |
| **6.4** | صفحة الدفع خارج شبكة أمان CSRF بمبرّر سقط في 6ب-1 | ✅ |
| **7.3** | بطاقة المستخدم في صفحة الدعم تعطي 404 | ✅ |
| **7.5** | كتلة flash في صفحة النسخ الاحتياطي لم تُنفَّذ ولا مرة | ✅ |
| **7.2** | حقل CSRF في صفحة الدعم لا يراه `updateCsrfToken` | ✅ |
| **7.1 (أ)** | فرع أدمن ميت كامل في `features/notifications.js` | ✅ |

**البنود الخمسة المعروضة كلها صحيحة** — فُحص كلٌّ منها في الكود قبل لمسه،
ولم يسقط أيٌّ منها كما سقطت ثلاثة من خمسة في خطة الـpartials سابقاً.
وأُضيف سادس (6.4) اكتُشف أثناء إعادة الجرد.

---

## 2. تصحيح تقرير المرحلة 6 — ورقمان أُعيد قياسهما

التقرير كُتب **قبل** تنفيذ 6ب-2 وبقي على حاله بعده، فصار يقول عن جزء
منجَز إنه «لم يُنفَّذ بقرارك» في أول أربعة أسطر منه.

**ولم أنقل أرقام الـcommits كما هي.** أعدتُ الجرد بأداة واحدة على نسختَي
ما قبل وما بعد (`git archive` للنسخة القديمة):

| | رسالة `fd3538f` | القياس المُعاد |
|---|---|---|
| مواضع POST محوَّلة | 26 | **30** (في 13 ملفاً — العدد صحيح) |
| ملفات تستعمل الشبكة / مستثناة | 18 / 4 | **19 / 3** |

سبب الثاني: `features/auth.js` عُدَّت «مستثناة» وهي تستعمل الشبكة في
**ثلاث** نقاط ولا تستثني إلا نقطة الخروج. وأُزيل من جدول التحقق رقم
«26 تأكيداً» لأنه غير قابل للإثبات — الأعداد المسجَّلة في الـcommits هي
14 ثم 24 ثم ثلاث نقاط متجر.

وأُضيف للتقرير قسم **«أخطاء ارتكبتُها»** بالثلاثة من السيشن السابقة، كما
في كل تقرير سابق.

---

## 3. البند 7.4 — سجل التدقيق نفسه كان يكذب

`AdminProductModel::delete()` كانت تُرجع `true` **بلا شرط**، و
`DELETE ... WHERE id = ?` على معرّف غير موجود ينجح ويحذف صفر صفوف.
فالنتيجة على معرّف وهمي كانت:

```
{"success":true,"message":"Product deleted successfully."}
```

وأسوأ منها ما ترتّب عليها — صفّ تدقيق حرفيّ نصّه
`Deleted product #5020: #5020` (الاسم نفسه معرّف، لأن `getNameById`
أعادت null فعمل الاحتياطي)، وإشعار أدمن مطابق.

**الإصلاح:** الموديل يقرأ `rowCount()` ويُرجع ثلاثية متمايزة —
`true` حُذف · `false` لم يوجد · `null` فشل تقني. والفصل بين الأخيرتين
ليس تجميلاً: لكلٍّ رسالة مختلفة، ولا يجوز كتابة سجل ولا إطلاق إشعار في
حالة `false`. ولم أستعمل `getNameById() === null` بديلاً عن `rowCount`
لأنها تُرجع null للخطأ التقني أيضاً فتخلط الحالتين من جديد.

**التحقق بضابطين:** السالب (بتراجع git) يُفشل 5 من 7 تأكيدات — الرسالة
الكاذبة وصفّ التدقيق (238 → 239) والإشعار (18 → 19). والموجب يثبت أن
منتجاً حقيقياً ما زال يُحذف مع سجل يحمل الاسم الصحيح: 10/10.

---

## 4. البند 6.4 — صفحة الدفع أفلتت من الجرد كله

`features/checkout.js` لم يكن ضمن الـ13 لأن البحث كان عن
`method: 'POST'`، وهو يمرّر الطريقة بمتغيّر عبر غلافه المحلي:
`fetchWithCsrf(url, 'POST', data)`.

وتعليق الغلاف يبرّر استقلاله بأن `fetchWithCsrfRetry` «تدعم `FormData`
و`urlencoded` فقط». **كان صحيحاً حين كُتب، وأبطلته المرحلة 6ب-1 نفسها**
— وهي التي علّمت تلك الدالة إعادة بناء أجسام JSON بالضبط. أي أن الجزء
الأول من المرحلة 6 أسقط مبرّر الاستثناء، والجزء الثاني لم يره.

**ولماذا يهمّ هنا أكثر من غيره:** التوكن واحد لكل جلسة، فيكفي فشل فورم
في تبويب آخر ليُبطل توكن هذه الصفحة — وضياع الطلب هنا يعني **ضياع عملية
شراء بعد ثلاث خطوات مكتملة**.

**أمان إعادة المحاولة مفحوص لا مفترَض:** `CheckoutController::store`
يتحقق من التوكن **قبل** فحص idempotency وقبل إنشاء أي طلب، فالرفض يعني
أن شيئاً لم يُنشأ. ومفتاح idempotency يبقى في الجسم المُعاد بناؤه —
مؤكَّد بتأكيد مستقل.

**التحقق:** اختبار وحدة بـ14 تأكيداً يحمّل `csrf.js` الحقيقي في سياق
يحاكي صفحة متجر (`BASE_URL` معرَّف و`URLROOT` **غير** معرَّف)، فيثبت
اختيار `/auth/csrf`، ونجاة `idempotency_key` و`items` (مصفوفةً لا نصّاً)
والعربية داخلها. والضابط السالب: نسخة حرفية من الغلاف المحذوف على
الخادم المزيّف نفسه → طلب واحد، لا تعافٍ، والمستخدم يخسر الطلب.

و9 تأكيدات HTTP على الخادم الحقيقي بجلسة مستخدم مزوّرة.

---

## 5. البنود الثلاثة الصغيرة

### 7.3 — رابط يعطي 404

بطاقة رسالة الدعم كانت تنقل إلى `/admin/users/{id}` — راوت غير موجود.
**أثبتّه بطلب حقيقي: HTTP 404.** وفوق السطر TODO يقول إن الصفحة «لسا مو
منشأة»، **وهي منشأة منذ البداية** بشكل آخر: `GET /admin/users/details`
تقرأ `$_GET['id']`. صار الرابط `/admin/users/details?id=…`.

### 7.5 — كتلة لم تُنفَّذ ولا مرة

`BackupController` لا يقرأ `$_SESSION['flash_*']` ولا يمرّر
`$flashMsg`/`$flashErr` إطلاقاً. وفحصتُ المنفذ الآخر أيضاً: **لا يوجد في
المشروع أي تحويل إلى `/admin/backup`** — الصفحة تُفتح بالتنقّل، ونقاطها
JSON أو تنزيل ملف، ونتائجها توست في `backup.js`. فالكتلة غير قابلة
للوصول لا «نادرة». حُذف الاستدعاء ووُضع تعليق يشرح **لماذا** كي لا
يعيدها أحد ظنّاً أنها سهو.

### 7.2 — حقل لا يراه المحدِّث

`updateCsrfToken()` تستهدف `input[name="csrf_token"]`، والحقل كان يحمل
`id` بلا `name`. **وفُحص الخطر قبل الإضافة كما طُلب**: الحقل خارج أي
`<form>` (فورم البحث الوحيد مغلق قبله — مؤكَّد بموضع `</form>` في الـHTML
المُخدَّم)، و`support.js` يبني `FormData` يدوياً. والسابقة قائمة:
`backup.php` و`navbar.php` يحملان `name="csrf_token"` أصلاً.

**التحقق:** 17 تأكيداً على الخادم، بضابطين سالبين — الراوت القديم يعطي
404 فعلاً، والمحدِّد لا يطابق الحقل على نسخة الـHTML بعد نزع السمة برمجياً.

---

## 6. البند 7.1 — الفرع الميت (الخيار أ)

### لم يكن ميتاً بالتقدير بل بثلاث حقائق مستقلة

كلٌّ منها تكفي وحدها:

1. `#adminNotifBell` في `app/views/admin/inc/navbar.php` **وحده**.
2. الملف مُدرَج في `app/views/inc/footer.php` **وحده** — فوتر المتجر.
3. `Core/Controller::view()` يستدعي لـlayout الأدمن
   `admin/inc/footer.php`، وهو ملف مختلف لا يذكر هذا السكربت.

ولوحة الأدمن يخدمها `js/admin/admin-notifications.js` — لم يُمسّ.

### مبدأ التحويل: كل سطر باقٍ هو سطر كان يُنفَّذ

لا إعادة صياغة ولا توحيد للمسارات. أُزيل ما لا يُنفَّذ، وبقي الحيّ
**حرفياً**. ولهذا بقيت الروابط مكتوبة نصّاً ولم تُجمع في `cfg.endpoint`
— تلك الخانة كانت للفرع الميت وحده فحُذفت. فاختلاف السلوك مستحيل
**بالبناء** لا بالفحص وحده.

330 → 264 سطراً، منها 30 سطر توثيق جديد.

### وتظليل متغيّر أزيل في الطريق

`renderSidebar` كانت تعرّف `const dismissFn` (نصّاً) يُظلّل الدالة
`dismissFn` في نطاق الـIIFE. بلا ضرر هنا، لكنه من **نفس عائلة** العطل
الذي عطّل «إضافة عنوان» في `account.js`.

### التحقق: اختبار تكافؤ بضابط حساسية

يشغّل **النسختين الحقيقيتين** — القديمة من `git show HEAD:` والجديدة من
القرص — في نفس الـDOM المزيّف، ويقارن:

- **الماركب المُولَّد متطابق حرفاً بحرف** (مع تأكيد مستقل أنه ليس فارغاً،
  وإلا لكانت المطابقة بلا معنى).
- نفس روابط الإقلاع ونفس البادج، والأزرار الثلاثة تصيب نفس النقاط.
- على `window`: الاسمان الحيّان باقيان، **والميتان وحدهما اختفيا**، ولا
  اسم جديد. وقد تُحقِّق قبل الحذف أن الأسماء الثلاثة الميتة لا يشير
  إليها شيء في المشروع خارج هذا الملف.

**ضابط الحساسية:** النسخة القديمة في DOM أدمن تضرب
`/admin/notifications/list` — فالفرع كان موجوداً فعلاً والاختبار يميّز
الوضعين. لولاه لكان تكافؤاً أعمى يمرّ على أي شيء.

---

## 7. التحقق الشامل

| الفحص | النتيجة |
|---|---|
| `smoke-test.php --lint-all` | ✅ 44/44 راوت · 128 ملف PHP سليم |
| `audit-imports.php` | ✅ نظيف |
| `node --check` على **كل** ملفات JS | ✅ 34/34 |
| `composer dump-autoload -o` | ✅ 920 كلاساً |
| `composer docs:generate` | ✅ صفر تحذيرات |
| اختبارات HTTP على الخادم الحقيقي | ✅ 7 + 10 + 9 + 17 تأكيداً |
| اختبارات وحدة بـ`node` | ✅ 14 + 13 تأكيداً |
| **ضوابط سالبة** | ✅ خمسة — 7.4 (تراجع git) · 6.4 (الغلاف القديم) · 7.3 (404 حقيقي) · 7.2 (نزع السمة) · 7.1 (ضابط حساسية) |

### بيانات الاختبار — نُظّفت كلها وتُحقِّق منها

| | |
|---|---|
| منتجات | 16 (صفر باسم `TEST-%`) |
| تصنيفات | 4 |
| سجل التدقيق | 238 صفاً، آخر معرّف 403 |
| إشعارات الأدمن | 18، آخر معرّف 174 |
| جلسات اختبار | صفر — **حُذفت بالأسماء الكاملة لا بنمط بادئة** |

> **الضابط السالب للبند 7.4 أنشأ فعلاً الصفّين الكاذبين** اللذين يصفهما
> العطل — لا مفرّ من ذلك لإثباته. حُذفا بمعرّفيهما الدقيقين (تدقيق #410،
> إشعار #180) بعد فحص محتواهما. والأرقام أعلاه **مطابقة تماماً** لما
> سجّلته السيشن السابقة قبل أي عمل اليوم.

---

## 8. أخطاء ارتكبتُها أثناء هذا العمل

### 8.1 تأكيدان خاطئان في اختباراتي — لا في الكود

- فحص «صفر أثر للمنتج المؤقت» لم يفلتر بـ`target_type`، فطابق صفّ حذف
  **مستخدم** رقم 98 من 2026-08-23. `target_id` بلا `target_type` لا
  يعني شيئاً في `admin_audit_log`.
- فحص «لا استدعاء لـ`flash-messages.php`» كان `str_contains` على اسم
  الملف، فطابق **التعليق الذي كتبته أنا** ليشرح الحذف.

في الحالتين ظهر «فشل» وكان مصدره الاختبار. الدرس نفسه الذي وثّقته
السيشن السابقة عن `users/delete` و`backup/create` — **افحص الاختبار قبل
أن تشخّص الكود**.

### 8.2 تأكيد دائم النجاح في اختبار 7.1

كتبت `check(..., true)` — تأكيد لا يمكن أن يفشل، أي طقس لا اختبار. حُذف،
والعدد المعلَن 13 لا 14.

### 8.3 مصيدة الـshell أوقعتني ثلاث مرات

heredoc في الـshell يطوي `\\` إلى `\`، فأفسد `str_replace('\\','/')` في
سكربت الجرد مرتين، و`php -r` بنصوص متداخلة فشل مرة. المصيدة موثّقة في
ملف التسليم (4.4) وكان يجب أن أبدأ بأداة التحرير مباشرة.

---

## 9. ما يليه

**لم يبقَ بند مفتوح من المرحلة 6 ولا من قائمة ما بعدها.**

بنود موثّقة خارج هذا النطاق تنتظر قراراً إن أردت:

| البند | المصدر |
|---|---|
| كل صور المنتجات في محرّر السلايدر مكسورة | تقرير المرحلة 5، القسم 5.6 |
| `download()` في `BackupController` يموت بنصّ خام لا برسالة عابرة | لوحظ أثناء البند 7.5 |
| البنود المستبعدة بقياس | تقرير المرحلة 6، القسم 5 — **لا تُعاد بلا سبب جديد** |
