# تسليم: أكمل ما تبقّى في مشروع Cairo Store

> **انسخ هذا الملف كاملاً والصقه كأول رسالة في السيشن الجديدة.**

---

أنت تكمل عملاً بدأته سيشنات سابقة على `C:\xampp\htdocs\STORE` — متجر PHP بمعمارية MVC
مخصّصة (بلا فريم ورك). **المراحل 0 إلى 6 منجزة ومدموجة في `master`.** ما تبقّى بنود
صغيرة محدّدة، مذكورة كلها أدناه.

**اقرأ أولاً هذه الملفات** (بهذا الترتيب) لتفهم ما جرى:
`CLEANUP-PLAN.md` · `CLEANUP-REPORT-PHASE-0-1.md` · `CLEANUP-REPORT-PHASE-2.md` ·
`CLEANUP-REPORT-PHASE-3.md` · `CLEANUP-REPORT-PHASE-4.md` · `CLEANUP-REPORT-PHASE-5.md` ·
`CLEANUP-PLAN-PHASE-6.md` · `CLEANUP-REPORT-PHASE-6.md`

---

## 1. المنهج — التزم به حرفياً

صاحب المشروع يقدّر الدقة والصدق أكثر من السرعة.

### 1.1 قبل أي تعديل

```bash
git checkout -b <فرع-مستقل>
php scripts/smoke-test.php        # يجب أن يكون 44/44 قبل اللمس
```

### 1.2 بعد كل خطوة منطقية

- `php scripts/smoke-test.php` · `php scripts/audit-imports.php`
- `node --check <ملف.js>` لأي ملف JS مُعدَّل
- commit فوري برسالة تشرح **لماذا** لا **ماذا**

### 1.3 التحقق — لا تكتفِ بالصياغة

- `php -l` و`node --check` لا يكشفان أخطاء وقت التشغيل. **شغّل الكود فعلاً.**
- لكل تعديل يمسّ POST: اختبار HTTP حقيقي بجلسة مزوّرة (الطريقة في القسم 4).
- **اكتب ضابطاً سالباً**: اختبار لا يفشل على النسخة المعطوبة ليس اختباراً.
- **نظّف بيانات الاختبار دائماً** وتحقّق من التنظيف بعده.

### 1.4 الصدق في التقارير

- لو اكتشفت عطلاً سابقاً لعملك: **أعد إنتاجه أولاً** لتثبت أنه ليس منك، ثم أصلحه ووثّقه.
- لو أخطأت: قل ذلك بوضوح. (السيشنات السابقة وثّقت أخطاءها كلها — راجع قسم «أخطاء
  ارتكبتُها» في كل تقرير.)
- **لا تدّعِ تحققاً لم تفعله.**

### 1.5 حدود العمل

- **لا تنفّذ ولا تخطّط لبند لم يوافق عليه.** أنجز البند، اكتب التقرير، توقّف واسأل.
- لو بند هنا تبيّن أنه خطأ عند فحص الكود الحقيقي — **قل ذلك ولا تنفّذه**. حدث هذا
  مراراً: ثلاثة من خمسة بنود في خطة الـpartials كانت خاطئة، وأحدها كان سيحذف بادجاً
  ظاهراً للمستخدم.

### 1.6 لغة التواصل

**عربي.** التعليقات في الكود عربية، ورسائل الـcommit عربية، والتقارير عربية.
أسماء الكلاسات والدوال إنجليزية.

---

## 2. الحالة الآن

### 2.1 المؤشرات — كلها بلغت أهدافها

| المؤشر | البداية | الآن |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | 14 | **0** ✅ |
| وصول DB من الـviews | — | **0** ✅ |
| تحذيرات PSR-4 من composer | 48 | **0** ✅ |
| كنترولرز بلا توثيق OpenAPI | 10 | **0** ✅ |
| صفحات تكتب `<!DOCTYPE>` بيدها | 3 | **0** ✅ |
| أسطر `<style>` مضمّنة | 55 | **0** ✅ |
| partials في `views/shared` | 1 | **7** ✅ |
| أسطر `<script>` مضمّنة | 579 | **91** (تمرير بيانات فقط) ✅ |
| `console.log` في الإنتاج | 2 | **0** ✅ |
| `var` في JS | 27 | **0** ✅ |
| ملفات POST بلا شبكة أمان CSRF | 13 | **0** (4 مستثناة بسبب موثَّق) ✅ |
| **أعطال مكتشفة ومُصلَحة** | — | **22** |

### 2.2 الأحجام

| الطبقة | ملفات | أسطر |
|---|---:|---:|
| `app/Controllers` | 24 | 7,077 |
| `app/Models` | 16 | 4,740 |
| `app/views` | 58 | 6,806 |
| `app/Core` | 9 | 761 |
| `app/helpers` | 7 | 570 |
| `public/js` | 34 | 5,704 |
| `public/css` | 68 | 5,195 |

### 2.3 git

`master` نظيف، وكل مرحلة على branch مدموج بـ`--no-ff`. آخر الـcommits:

```
دمج 6ب-2: توحيد شبكة أمان CSRF عبر طبقة الـJS
دمج المرحلة 6أ: قاعدة بادج المخزون موحّدة
دمج المرحلة 6ب-1: fetchWithCsrfRetry تدعم أجسام JSON
دمج المرحلة 6ج — التنظيف
دمج تدقيق المرحلة 6 وخطتها (بلا تنفيذ)
دمج قرارَي المرحلة 5
دمج المرحلة 5: الـViews
```

---

## 3. الأدوات

```bash
php scripts/smoke-test.php              # 44 راوت GET + صياغة المعدَّل (سريع)
php scripts/smoke-test.php --lint-all   # + كل ملفات PHP (بطيء، للتحقق النهائي)
php scripts/audit.php                   # قياس حالة الكود
php scripts/audit.php --json            # للمقارنة الآلية
php scripts/audit-imports.php           # كلاسات غير مستوردة + استيرادات ميتة
php scripts/audit-escaping.php          # تصنيف مخرجات الـviews أمنياً
composer docs:generate                  # يجب أن يمرّ بصفر تحذيرات
composer dump-autoload -o               # بعد أي تعديل على autoload
node --check <ملف.js>                   # صياغة JS
```

**شرط:** Apache و MySQL شغّالان في XAMPP.

---

## 4. مصائد مثبتة — لا تكررها

### 4.1 نهايات السطور CRLF

**كل ملفات المشروع بنهايات CRLF.** `sed` بأنماط تنتهي بـ`$` لن تطابق.

```php
$raw    = file_get_contents($file);
$isCrlf = str_contains($raw, "\r\n");
$src    = $isCrlf ? str_replace("\r\n", "\n", $raw) : $raw;
// ... عدّل $src ...
file_put_contents($file, $isCrlf ? str_replace("\n", "\r\n", $src) : $src);
```

بعد أي ملف جديد تكتبه بأداة التحرير: طبّعه إلى CRLF بنفس الطريقة.

### 4.2 لا تحذف كتل كود بـregex

`preg_replace` يُرجع `null` عند فشل النمط، و`file_put_contents($f, null)` **يفرّغ الملف**.
حدث ذلك فعلاً بسبب `\P` في نمط. **تحقّق `if ($new === null) { /* لا تكتب */ }` دائماً.**

وأنماط `(.*?)` مع `/s` **تعبر حدود الكتل** وتبتلع كوداً مجاوراً — حدث ذلك مع
`admin-notifications.js` فأنتج `const data` مكرراً. **للكتل متعددة الأسطر استعمل أداة
التحرير الدقيقة أو استبدالاً على نطاق أسطر محدَّد مع فحص الحدود قبل التنفيذ.**

### 4.3 `\R` في PCRE يقطع النص العربي

`preg_split('/\R/')` بلا معدِّل `/u` يعمل على البايتات ويطابق `\x85` — وهو **بايت
استمرار شرعي داخل الحروف العربية** بترميز UTF-8. استعمل `/\r\n|\n|\r/` صراحةً.

### 4.4 الـshell يمسخ الـescaping

`php -r '...'` مع backslashes ونصوص متداخلة يفشل بطرق صامتة. **اكتب سكربتاً في
scratchpad وشغّله بـ`php file.php`.**

### 4.5 كاش المتصفح بعد تعديل JS/CSS

**تكرّر ثلاث مرات.** أول قياس بعد تعديل ملف JS/CSS يعطي نتيجة قديمة — بدا مرة أن
الصفحة الرئيسية فارغة تماماً، ومرة أن صور السلايدر ما زالت مكسورة.

```js
await fetch('/STORE/public/js/<ملف>.js', { cache: 'reload' });
// ثم navigate بـ force: true
```

### 4.6 الجلسات منفصلة تماماً

`PHPSESSID` للمستخدم و`admin_session` للأدمن — منفصلتان بالاسم والمحتوى.
**لا تستدعِ `isUser()` في الـbootstrap.**

### 4.7 طريقة اختبار مسارات POST

```php
$path = session_save_path() ?: sys_get_temp_dir();   // C:\xampp\tmp
$sid  = "test" . bin2hex(random_bytes(6));
$tok  = bin2hex(random_bytes(32));
file_put_contents("$path/sess_$sid",
    'admin_id|i:1;admin_name|s:11:"Ahmad Saleh";admin_role|s:1:"A";'
  . 'csrf_token|s:64:"' . $tok . '";last_active|i:' . time() . ';');
// جلسة مستخدم: user_id|i:7;user_name|s:5:"Ahmad";csrf_token|s:64:"…";
// وضع المتجر للأدمن: أضف admin_in_store_mode|b:1;
```

**احذف ملف الجلسة بعدها بالاسم الكامل**، لا بنمط بادئة (حذفٌ بنمط `sess_st*` طال
جلسات متصفح حقيقية في سيشن سابقة).

### 4.8 ⚠️ «معرّف غير موجود» **لا** يعني «بلا أثر»

**أهم مصيدة في هذا المشروع — وقعتُ فيها مرتين.** نقاط لا تأخذ معرّفاً أصلاً تُنفَّذ
فعلاً بمعرّف وهمي:

| النقطة | ما فعلته بالخطأ |
|---|---|
| `/admin/products/categories/add` | أنشأت تصنيفاً حقيقياً في قاعدة البيانات |
| `/admin/backup/create` | أنشأت ملف نسخة احتياطية 79KB |
| `/admin/notifications/mark-all-read` | علّمت إشعارات أدمن كمقروءة — **وهذا غير قابل للاسترجاع** (لا عمود `read_at`) |

**قبل إضافة أي نقطة إلى اختبار، تأكّد أن معرّفاً غير موجود يجعلها بلا أثر.**

### 4.9 أول طلب HTTP بعد خمول يعود فارغاً

تكرّر مرتين. يبدو كأن النقطة أعطت استجابة غير JSON أو أن سكربت الدخان «فشل». **أضف
طلب إحماء قبل القياس، وأعد التشغيل قبل أن تشخّص.**

### 4.10 لوحة المتصفح المخفية تُبطّئ JS

قياس بعد التنقّل قد يصل **بعد 148 ثانية** من تحميل الصفحة، فيفوت أي شيء مؤقّت
(توست بمهلة 3 ثوانٍ مثلاً). لا تعتمد على تأكيد شيء عابر بعد `navigate`.

---

## 5. اتفاقيات المشروع — احترمها

| القرار | السبب |
|---|---|
| **الموديلات كلها `static`** | 16 موديلاً متّسقين، لا DI container. لا تحوّلها. |
| **استجابة JSON بشكل `{success, message, ...}`** | الـfrontend يعتمد عليه. لا تغيّر المفاتيح. |
| **نقاط JSON تُرجع 200 حتى عند فشل التحقق** | سلوك قائم، موثّق في الـspec. |
| **`.env` خارج git** | فيه أسرار حقيقية. البديل `.env.example`. |
| **التوثيق يُكمَل لا يُحذف** | `composer docs:generate` بصفر تحذيرات وتغطية 100%. |
| **`public/images/` متتبَّع** | قاعدة البيانات تشير إليه بالمسار. |
| **إصلاح مسار الصورة مسؤولية الخادم** | `fixImagePath()` في PHP. النسخة في JS اسمها `imagePathOrEmpty` ولا تُصلح شيئاً — **لا تبنِ مسار صورة في JS.** |
| **قاعدة بادج المخزون في مكانين متطابقين** | `stock_badge_helper.php` و`stockBadge()` في `js/core/utils.js`. **غيّرت أحدهما؟ غيّر الآخر.** |

---

## 6. ✅ البند الأول — أكمله فوراً

### 6.1 تحديث `CLEANUP-REPORT-PHASE-6.md`

التقرير كُتب **قبل** تنفيذ 6ب-2، فيقول إنه «لم يُنفَّذ». **وقد نُفِّذ بالكامل بعده.**

المواضع التي تحتاج تصحيحاً (بأرقام أسطرها التقريبية):

- السطر 3-4: «الجزء الرابع … **لم يُنفَّذ بقرارك**» → صار منجزاً
- السطر 16: صفّ الجدول `| **6ب-2** … | ⛔ **لم يُنفَّذ بقرارك** |` → ✅
- القسم 5 (السطر ~145): «6ب-2: تحويل الـ13 ملفاً ⛔ بقرارك» → انقله إلى «ما أُنجز»
- القسم 9 (السطر ~264): «البند الوحيد المتبقي … هو 6ب-2» → لم يعد كذلك

**ما يجب أن يقوله التقرير عن 6ب-2** (من رسائل الـcommits الستة `00d2348` حتى `fd3538f`):

- **26 نقطة POST في 13 ملفاً** حُوِّلت إلى `fetchWithCsrfRetry`.
- الحصيلة: 22 ملفاً يرسل POST، **18 تستعمل شبكة الأمان**، و**4 مستثناة بسبب موثَّق**:
  - `admin/admin-layout/admin-navbar.js` و`features/auth.js` → نقطتا تسجيل خروج
    **لا تتحققان من CSRF** (تُدمّران الجلسة)، والأولى تُرجع 302 لا JSON.
  - `features/cart.js` → `/cart/check-stock` لا تتحقق من CSRF (استعلام قراءة).
  - `admin/admin-auth.js` → ثلاثة أسباب مفحوصة (القسم 6.2 أدناه).
  - و`admin/category-picker.js` تُركت فيها نقطة اقتراح التصنيفات وحدها (لا تتحقق من CSRF).
- **اكتشاف: التحذير في ستة ملفات كان خاطئاً منذ كُتب.** «لا تستخدم
  `fetchWithCsrfRetry` لأنها تعتمد على `window.BASE_URL` غير الموجود بصفحات الأدمن» —
  الدالة تفحص `URLROOT` **أولاً** منذ الـbaseline بلا تغيير، و`admin/orders.js`
  يستعملها في صفحات الأدمن ويعمل. أُزيل من خمسة، وبقي في السادس بسببه الحقيقي.
- **عطل مُصلَح: «إضافة عنوان» لم تعمل ولا مرة.** في `features/account.js` كانت نتيجة
  الطلب تُسمّى `data` — نفس اسم متغيّر حقول الفورم المُعلَن فوق `try`. و`const`
  مرتبطة بالكتلة، فـ`JSON.stringify(data)` كان يقرأ المتغيّر في **منطقته الميتة**
  ويرمي `ReferenceError` يبتلعه `catch` ويعرض «Network error, please try again.».
  أُعيدت تسمية النتيجة إلى `result`. **أُثبت بضابط سالب**: على النسخة القديمة الطلب
  لا يُرسل إطلاقاً.
- التحقق: 26 تأكيد HTTP (لكل نقطة: التوكن الباطل يُرفض، والطازج يُقبل ويصل للمنطق) +
  اختبار وحدة لـ`account.js` بضابط سالب + `node --check` على كل ملف JS.

**وأضف قسم «أخطاء ارتُكبت»** بالثلاثة المذكورة في القسم 8 أدناه — التقارير السابقة كلها
تحوي قسماً كهذا، وهو جزء من منهج المشروع.

---

## 7. البنود المتبقية — كلٌّ منها يحتاج موافقة قبل التنفيذ

**اسأل صاحب المشروع أيّها ينفَّذ. لا تنفّذ شيئاً منها بلا موافقة صريحة.**

### 7.1 🔴 فرع أدمن ميت كامل في `features/notifications.js`

الملف (330 سطراً) مصمَّم بوضعين: يكتشف `#adminNotifBell` فيتصرّف كملف أدمن، وإلا
فكملف متجر. لكن:

- `#adminNotifBell` موجود في `app/views/admin/inc/navbar.php` **وحده**
- `features/notifications.js` يُحمَّل في **فوتر المتجر وحده** (تحقّقت: صفر إشارة إليه
  في `app/views/admin/inc/footer.php`)
- الأدمن يخدمه `admin/admin-notifications.js` (188 سطراً)

**النتيجة: `isAdmin` دائماً `false`، و11 فرعاً في الملف لا تُنفَّذ أبداً**
(الأسطر التقريبية: 16 · 42 · 69 · 70 · 143 · 155 · 166 · 194 · 229 · 260 · 281).

**لماذا لم يُحذف:** إزالة فرع من بنية ملف بأكمله تغيير جوهري لا تنظيف، ويحتاج قراراً
عن النيّة الأصلية — أكان المقصود أن يخدم الملف الجانبين ثم انقسم، أم أن الفرع بقايا؟

**الخياران:** (أ) حذف الفرع الميت من ملف المتجر، أو (ب) العكس: استعمال الملف الواحد
في الجانبين وحذف `admin-notifications.js` — وهذا أكبر بكثير وسبق أن استُبعد لأن
الملفين **متشابهان لا متطابقان** (جلستان ونقطتان وماركب مختلف).

### 7.2 🟠 `#csrfTokenSupport` بلا سمة `name`

`updateCsrfToken()` في `js/core/csrf.js` تستهدف `input[name="csrf_token"]`، وحقل
صفحة الدعم يحمل `id` بلا `name` — فلا يُحدَّث بعد تعافي CSRF (`window._csrfToken`
يُحدَّث). **الأثر:** أول إرسال يدوي تالٍ يحتاج جولة تعافٍ أخرى.

**الإصلاح المرشّح:** إضافة `name="csrf_token"` للحقل في `app/views/admin/support.php`.
⚠️ تحقّق أولاً أن إضافة الاسم لا تُدخل الحقل في `FormData` بشكل يكسر شيئاً.

### 7.3 🟠 رابط ميت إلى `/admin/users/{id}`

بطاقة المستخدم في صفحة الدعم تنقل إلى راوت **غير موجود** فتظهر صفحة 404.
موثَّق في `public/js/admin/support.js` كـ`TODO` منذ كُتب (السطر ~116).

**الخياران:** حذف الرابط، أو توجيهه إلى `/admin/users/details?id={id}` الموجود فعلاً.

### 7.4 🟡 `AdminProductsController::delete` تكذب في نجاحها

تُرجع «Product deleted successfully» لمنتج **غير موجود**، ولا تفحص عدد الصفوف
المتأثرة — **وتكتب صفّ تدقيق يزعم حذف منتج لم يوجد قط**.

**الإصلاح المرشّح:** فحص `rowCount()` والرد بـ«Product not found» عند الصفر، وعدم
كتابة سجل التدقيق حينها.

### 7.5 🟡 كتلة فلاش ميتة في `admin/backup.php`

`BackupController` لا يمرّر `flashMsg` ولا `flashErr` إطلاقاً، فاستدعاء
`shared/flash-messages.php` هناك لم يُنفَّذ ولا مرة. سطر واحد — يُحذف أو يُملأ.

### 7.6 🟡 حزمة `admin-auth` وأخواتها

راجع `CLEANUP-REPORT-PHASE-6.md` القسم 6 و`CLEANUP-REPORT-PHASE-5.md` القسم 6 — بنود
موثّقة أخرى قد يريد صاحب المشروع النظر فيها.

### 7.7 ⚪ بنود استُبعدت بقياس — **لا تعِد فتحها بلا سبب جديد**

| البند | لماذا استُبعد |
|---|---|
| مراجعة `innerHTML` للـXSS | الملفات الخمسة الأكبر في اللوحة فيها **صفر `innerHTML`**؛ والتي تبني HTML بالبيانات تستعمل `escHtml` أصلاً؛ والباقي نصوص حرفية |
| تنظيف المستمعات والمؤقتات | خمسة من سبعة `setInterval` تُنظَّف، والاثنان الباقيان استطلاع بعمر الصفحة |
| توحيد ملفَي الإشعارات | متشابهان لا متطابقان (راجع 7.1) |
| تقسيم `admin/products.js` (369 سطراً) | لا تكرار فيه ولا منطق مبعثر — طوله من عدد الأزرار. **لا تطارد الأرقام** |
| `'use strict'` للـ28 ملفاً | تغيير سلوك محتمل مقابل فائدة نظرية |

---

## 8. أخطاء ارتُكبت في السيشن السابقة — يجب أن تُذكر في تقرير المرحلة 6

**اذكرها كما هي. المشروع يوثّق أخطاءه.**

1. **«معرّف غير موجود ⇒ بلا أثر» — افتراض خاطئ، وقعتُ فيه مرتين.** أنشأت اختباراتي
   تصنيفاً حقيقياً وملف نسخة احتياطية 79KB، ثم في الدفعة التالية علّمت إشعارات أدمن
   كمقروءة عبر `mark-all-read`. حُذف ما أُنشئ، لكن **حالة القراءة غير قابلة
   للاسترجاع** — لا عمود `read_at` في `admin_notifications`. الصفوف الأربعة التي
   أنشأتها الاختبارات حُذفت، لكن حالة القراءة للصفوف الأقدم بقيت كما تركها الاختبار.

2. **فشلان بدَوا في الكود وكانا في الاختبار.** `users/delete` بدا يُرجع استجابة غير
   JSON (كان أول طلب بعد خمول)، و`backup/create` بدا مرفوضاً (نجح على الخادم وانتهت
   مهلة curl أثناء `mysqldump`). صار الاختبار يطبع الاستجابة الخام بدل «(غير JSON)».

3. **حذف جلسات بنمط بادئة بدل الأسماء الدقيقة.** استُعمل
   `Remove-Item sess_(cv|dbg|st|…)*` فقد يكون طال جلسات متصفح حقيقية. الأثر: تسجيل
   دخول من جديد. **كان يجب الحذف بالاسم الكامل** — وكانت الأسماء مسجَّلة أصلاً.

---

## 9. أول أمر تشغّله

```bash
cd /c/xampp/htdocs/STORE && php scripts/smoke-test.php && php scripts/audit.php
```

**يجب أن يكون أخضر: 44/44.** لو لم يكن، **أعد التشغيل مرة** (المصيدة 4.9) ثم حقّق.

---

## 10. المطلوب منك

1. اقرأ التقارير الثمانية المذكورة في الأعلى.
2. شغّل الأمر أعلاه وتأكد أن الـbaseline أخضر.
3. **نفّذ البند 6.1 فوراً** (تحديث تقرير المرحلة 6) — هذا البند الوحيد الموافَق عليه.
4. **اعرض البنود 7.1 إلى 7.5 على صاحب المشروع واسأله أيّها ينفَّذ**، مع رأيك في
   الأولوية. لا تنفّذ شيئاً منها بلا موافقة صريحة.
5. بعد كل بند: تقرير قصير، ثم توقّف واسأل.
