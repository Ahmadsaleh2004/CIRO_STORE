# تقرير: توحيد مقدّمة نقاط JSON وإغلاق ثغرة الخروج

> **الحالة: مكتمل.** الفرع `cleanup/csrf-preamble-unification` — خمسة commits.
> الخطة: [CSRF-UNIFICATION-PLAN.md](CSRF-UNIFICATION-PLAN.md)

---

## 1. النتيجة بالأرقام

| المؤشر | قبل | بعد |
|---|---:|---:|
| مواضع تمرّ بـ`beginJsonPost()` | 18 | **45** |
| استدعاءات `verifyCsrfToken()` مباشرة | 34 | **9** ← كلها موثّقة |
| قراءة يدوية لـ`php://input` في الكنترولرز | 9 | **0** |
| صياغات مختلفة لرسالة فشل CSRF | 4 | **1** |
| نقاط خروج بلا تحقق CSRF | 2 | **0** |

---

## 2. عيب في `beginJsonPost()` كان سيكسر تسع نقاط

الدالة كانت تقرأ التوكن من `$_POST['csrf_token']` **وحده**. لكن تسع نقاط تستقبل **جسم
JSON** (`cart.js` و`account.js` و`my-info.js` ترسل JSON عبر `fetchWithCsrfRetry`) وتبني
`array_merge($_POST, $body)` بنفسها ثم تقرأ التوكن منه. تحويلها كما هي كان **يكسرها
جميعاً** — التوكن ببساطة لن يكون في `$_POST`.

**الحل:** `Controller::requestData()` — تدمج `$_POST` بجسم JSON مرة واحدة لكل طلب
(`php://input` تيّار يُقرأ مرة، والنتيجة تُخزَّن). `beginJsonPost` صارت تقرأ منها.

**هذا هو السبب الحقيقي** لعدم توحيد تلك النقاط في المرحلة 3، لا «انحرافها».

---

## 3. ثلاثة أعطال من فئة واحدة — ظهرت الفئة ثلاث مرات

`js/core/csrf.js` يكتشف فشل التوكن بـ`message.startsWith('Invalid CSRF token')` ليجلب
توكناً جديداً ويُعيد المحاولة. **ثلاث نقاط كانت ترد برسائل لا تبدأ بالبادئة**، فكانت
إعادة المحاولة معطّلة لها بصمت — رغم أن الـJS يستدعي `fetchWithCsrfRetry` فعلاً:

| النقطة | الرسالة الخاطئة | الأثر |
|---|---|---|
| `WishlistController::notify` | `Invalid session, please refresh the page.` | زر «نبّهني عند التوفّر» |
| `ContactController::send` | `Invalid request, please refresh the page and try again.` | نموذج «اتصل بنا» |
| ست نقاط أخرى | `Invalid CSRF token.` / `…please try again.` | تعمل صدفةً — البادئة تطابق |

المستخدم بتوكن منتهٍ كان يرى **فشلاً نهائياً** في هذين الزرّين، بينما يرى تعافياً صامتاً
في كل زر آخر.

> **الاستنتاج:** ظهور الفئة ثلاث مرات يجعل ربط الاكتشاف بنصّ رسالة عيباً بنيوياً لا
> صدفة. البديل — حقل `error_code: 'csrf_invalid'` صريح — صار قراراً مستحقاً (القسم 7).

---

## 4. الثغرة: نقطتا الخروج بلا تحقق CSRF

`AuthController::logout` و`AdminAuthController::logout` كانتا تستدعيان `session_destroy()`
مباشرةً. المتصفح يرسل كوكي الجلسة تلقائياً مع أي طلب من أي أصل، فكان
`<img src=".../auth/logout">` في أي صفحة يزورها المستخدم كافياً لتسجيل خروجه.

**جانب المتجر:** `beginJsonPost()` قبل أي مساس بالجلسة. و`js/features/auth.js` **كان لا
يرسل توكناً إطلاقاً** — صار يقرأه من الحقل المخفي الذي تطبعه مودالات المصادقة (تحققتُ:
موجود على الرئيسية والمنتجات ومن-نحن واتصل-بنا وقائمة الأمنيات)، وصار يستعمل
`fetchWithCsrfRetry`.

**جانب الأدمن:** لا `beginJsonPost` — النقطة تحوّل بـ302 ولا تُرجع JSON. تحقق يدوي، وعند
الفشل تحويل إلى `/admin/home` **بلا تدمير الجلسة**: الطلب لم يأتِ من صفحاتنا فالسلوك
الآمن ألّا نطيعه.

**التحقق بالاختبار:**

| | بلا توكن | بتوكن صحيح |
|---|---|---|
| المستخدم | يُرفض · `/user/info` يبقى **200** (الجلسة نجت) | يخرج ويعيد `redirect` |
| الأدمن | 302 إلى `/admin/home` · `/admin/home` يبقى **200** | 302 إلى `/admin/login`، وبعده **302** |

---

## 5. ما استُبعد ولماذا — تسعة، كلها موثّقة في مكانها

| الموضع | السبب |
|---|---|
| `AdminAuth::login` · `verify2FALogin` · `forgotPassword` | تدوّر توكن CSRF عند كل فشل (`unset` + `generateCsrfToken`). التحويل يُسقط التدوير على أحسّ مسار في المشروع |
| `AdminAuth::reauth` | تدوّر التوكن **وتعيده في الاستجابة** كي يواصل `admin-auth.js` بلا جلب إضافي |
| `AdminAuth::logout` | تحوّل بـ302 ولا تُرجع JSON |
| `AdminBranding::save` | تفشل بـ`redirectWithError` — صفحة فورم لا نقطة API |
| `ContactController::contact` · `ProductController::show` | **لا تفشلان أصلاً**: تضعان النص في متغيّر وتُكملان عرض الصفحة (تخدمان GET وPOST معاً) |

**السكربت كاد يبتلع الثلاث الأولى.** أضفتُ حارس `bodyIsOnlyRespond` يرفض أي كتلة تفعل
أكثر من الردّ — وهو ما أمسكها. هذه بالضبط المخاطرة الأولى التي حذّرت منها الخطة.

---

## 6. تصحيحان لادعاءين سابقين

**1. «ثماني نقاط بلا فحص `REQUEST_METHOD` تقبل GET» — خطأ.**
`Router::dispatch()` يطابق الطريقة والمسار معاً، وGET على مسار POST يسقط إلى 404.
اختبرتُ ثلاث نقاط فرجعت كلها صفحة 404. **فهذه المرحلة تنظيف اتساق لا إصلاح أمني** —
المكسب الأمني كان في القسم 4 وحده.

**2. المحاولة الأولى للسكربت أخفقت.** خلّفت رأس JSON مكرراً وكتلة `REQUEST_METHOD` غير
مبتلعة ومتغيّر `$token` بلا استخدام. أرجعتُ بـ`git checkout --` وأعدتُ كتابته بمنهج أبسط:
مسح **مقدّمة الدالة** كاملة وتصنيف كل سطر، بدل ملاحقة الكتل المجاورة.

---

## 7. ما لم يُنجَز — وقرار ينتظرك

**`csrf.js` ما زال يكتشف الفشل بنصّ رسالة.** ظهور الفئة ثلاث مرات في هذه الجلسة وحدها
يثبت هشاشة الاقتران: أي صياغة جديدة تُعطّل إعادة المحاولة **بصمت**. وثّقتُه في docblock
`beginJsonPost()` باعتباره عقداً لا تجميلاً، لكن الإصلاح الحقيقي حقل صريح:

```json
{"success": false, "error_code": "csrf_invalid", "message": "…"}
```

يغيّر عقد الاستجابة، فهو قرارك.

**وبنود أخرى خرجت من التتبّع ولم تُلمس:**
- `Database.php:35` — `die()` يطبع رسالة خطأ قاعدة البيانات الخام
- `/admin/support/delete` لرسالة غير موجودة يجيب «نجح» — نفس فئة العطل الذي أُصلح سابقاً
  للمنتجات (البند 7.4)

---

## 8. التحقق

| الفحص | النتيجة |
|---|---|
| سكربت الدخان — 44 راوت GET | ✅ 44/44 بعد كل commit |
| `audit-imports.php` | ✅ صفر |
| `node --check` على ملفَي JS المعدَّلين | ✅ |
| FormData: رفض بلا توكن · قبول به | ✅ حذف رسالة دعم |
| **جسم JSON**: رفض بلا توكن · قبول به | ✅ توليد سرّ 2FA · إضافة عنوان — الحالة التي كانت ستنكسر |
| نقطة مستبعَدة (`branding/save`) | ✅ ما زالت تحوّل بـ302 لا JSON |
| خروج المستخدم والأدمن | ✅ الجدول في القسم 4 |
| `/contact/send` و`/auth/reset` بلا توكن | ✅ الرسالة الموحّدة |

**بيانات الاختبار نُظّفت كلها:** عنوان أُضيف ثم حُذف · طلب مخزون · أربع جلسات مزوّرة.
لم تُتلف بيانات حقيقية (تحققتُ من عدّاد رسائل الدعم قبل وبعد).

---

## 9. متابعة: الرمز الصريح بدل نصّ الرسالة — وثغرة سادسة

بعد اعتماد التوصية في القسم 7، نُفِّذت — وكشفت أثناء التنفيذ ثغرة لم تكن معروفة.

### 9.1 التغيير

`Controller::ERR_CSRF_INVALID` و`respondCsrfFailure()` تُرجع
`{success:false, message, error_code:csrf_invalid}`. و`csrf.js` صار يكتشف بالرمز.
الرسالة صارت للعرض وحدها.

### 9.2 إثبات أن الحذف آمن **قبل** تنفيذه

بُني سكربت يعدّ كل مسار يُستدعى عبر `fetchWithCsrfRetry` — **37 مساراً** — ويربطه بالدالة
التي تخدمه ويقول هل ستحمل `error_code`. كشف **خمسة لا تمرّ بالبوابة**، ولولاه لفقدت إعادة
المحاولة بصمت. هذا هو الفرق بين حذف مدروس وحذف على الحدس.

### 9.3 الثغرة السادسة: نقاط الإشعارات بلا تحقق CSRF إطلاقاً

`NotificationController` كان فيه `requireAuth()` و`requirePost()` خاصتان به و**صفر
`verifyCsrfToken` في الملف كله** — فبدا محميّاً وهو ليس كذلك. `js/features/notifications.js`
يرسل التوكن في كل نقطة، والخادم لا ينظر إليه.

**`/notifications/delete-all`** — حذف كل إشعارات المستخدم — كان قابلاً للتنفيذ من أي موقع
خارجي. أُثبتت الثغرة قبل الإصلاح: طلب بلا توكن رجع `{"success":true}`.

الأربع تمرّ الآن بالبوابة، وحُذفت `requirePost` لأن وجودها بجانب `requireAuth` كان يوحي
بحماية غائبة. و`suggestCategory` كانت آخر نقطة تقبل POST بلا فحص — صارت تمرّ بالبوابة،
و`category-picker.js` صار يرسل التوكن في ندائيه (كلاهما كان بلا توكن).

### 9.4 قاعدتا semgrep

| القاعدة | تمنع |
|---|---|
| `cairo-csrf-detect-by-message` (ERROR) | العودة لاكتشاف الفشل بنصّ الرسالة |
| `cairo-json-endpoint-without-csrf` (WARNING) | نقطة JSON بلا بوابة ولا تحقق |

**الصياغة الأولى للقاعدة الثانية أنتجت 11 نتيجة أربعٌ منها كاذبة** — قاعدة بهذه النسبة
تُدرَّب الناس على تجاهلها، أسوأ من لا قاعدة. صُحّحت بمطابقة الدالة كاملة، ثم بمطابقة
الاستدعاء داخل شرط `if` لأنه لا يقع كعبارة مستقلة. النتيجة: **صفر نتائج كاذبة**، وتُمسك
العطل فوراً عند إعادة إنتاجه.

### 9.5 التحقق

الدورة كاملة مُحاكاة بأربعة طلبات HTTP متتابعة: توكن خاطئ ← `error_code` ← جلب توكن جديد
← إعادة المحاولة تنجح. ونُظّفت كل بيانات الاختبار، ولم تُحذف إشعارات حقيقية (تحققتُ من
العدّاد: 24 قبل وبعد).
