// ══════════════════════════════════════════════════════════════
// Vitest — اختبارات الواجهة
// ══════════════════════════════════════════════════════════════
//
// المشروع بلا وحدات ES: 34 ملفاً تُحمَّل بوسوم <script> وتتشارك نطاقاً
// عاماً واحداً. فالاختبار لا يستطيع `import` شيئاً منها.
//
// الحلّ: تُحمَّل الملفات في بيئة jsdom بـeval على النطاق العام، تماماً
// كما يفعل المتصفح. الاختبار عندها يفحص **ما يعمل فعلاً**، لا نسخة
// معدَّلة منه صُنعت لتكون قابلة للاختبار.
//
// وهذا قرار مقصود: تحويل الملفات إلى وحدات ES كان سيغيّر 76 اسماً
// عامّاً و34 ملفاً في وقت واحد، لمكسب لا يظهر للمستخدم — والنمط
// القائم يعمل ويُقرأ.

import { defineConfig } from 'vitest/config';

export default defineConfig({
    test: {
        environment: 'jsdom',
        include: ['tests/js/**/*.test.mjs'],
        globals: false,
        restoreMocks: true,
    },
});
