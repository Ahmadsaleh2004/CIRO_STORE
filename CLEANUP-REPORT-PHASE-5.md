# تقرير المرحلة 5 — الـViews

> **مُحدَّث 2026-08-26:** كل بند كان معلَّقاً في هذا التقرير أُغلق — العطل
> 5.6 والكتلة 5.7 والقرار في القسم 6. الإشارات موضعية عند كل بند، والنصّ
> الأصلي باقٍ كما كُتب: التقرير سجلّ للحظته لا لوحة حالة.

> **الحالة: مكتملة.** المعايير الثلاثة كلها تحقّقت.
> التاريخ: 2026-08-25 · سابقه: [CLEANUP-REPORT-PHASE-4.md](CLEANUP-REPORT-PHASE-4.md)

---

## 1. معايير المرحلة

| المعيار | قبل | بعد | |
|---|---:|---:|:--:|
| `<style>` مضمّن داخل الـviews | 55 سطراً | **0** | ✅ |
| partials في `views/shared` | 1 | **7** | ✅ |
| `<script>` مضمّن — **منطق** | ~488 سطراً | **0** | ✅ |
| `<script>` مضمّن — **تمرير بيانات** (مسموح) | ~91 | 91 | — |

الرقم الإجمالي للـ`<script>` المضمّن: **579 ← 91**. الباقي كله إسنادات من شكل
`window.X = <?= json_encode(...) ?>;` — فحصتُها سطراً سطراً بعد تجريد تعبيرات PHP،
فلا سطر منطق واحد.

### الأحجام

| الطبقة | قبل | بعد | الفرق |
|---|---:|---:|---:|
| `app/views` (49 → 58 ملفاً) | 7,221 | 6,806 | **−415** |
| `public/js` (28 → 35 ملفاً) | 4,948 | 5,592 | +644 |
| `public/css` (66 → 68 ملفاً) | 4,997 | 5,195 | +198 |
| `app/helpers` (6 → 7) | 492 | 564 | +72 |
| `app/Controllers` | 7,048 | 7,074 | +26 |

الـviews نزلت 415 سطراً، وما زاد في `js` و`css` **ليس نموّاً**: هو نفس الكود منقولاً
إلى طبقته الصحيحة زائد تعليقات توثّق سبب كل قرار. الفارق بين −415 و+644 هو رؤوس
الملفات الجديدة (اثنا عشر ملفاً، لكلٍّ منها رأس يشرح من أين جاء ولماذا).

---

## 2. تصحيح: ثلاثة من خمسة بنود في خطة الـpartials كانت خاطئة

هذا أهم ما في المرحلة. قائمة الـpartials المقترحة في ملف التسليم مبنية على قراءة
بشرية، وثبت عند فحص الكود أن ثلاثة منها لا تصحّ — واحد منها كان **سيحذف بادجاً
ظاهراً للمستخدم**.

| البند المقترح | ما وجدتُه | ما فعلتُه |
|---|---|---|
| `shared/stock-badge.php`: «product_dit يعيد كتابة `getStockBadge()` يدوياً» | **غير دقيق.** صفحة التفاصيل تطبّق حالة **ثالثة** لا تعرفها الدالة: مخزون > 50 → الدالة تُرجع `null` (لا بادج)، بينما الصفحة تعرض «In Stock (N)» بالأخضر | أضفتُ وسيطاً `getStockBadge($stock, $showInStock)` بدل استدعاء الدالة كما هي. **الاستدعاء المباشر كان سيحذف البادج الأخضر** |
| `shared/category-buttons.php`: «`$catEmoji` مكرر حرفياً» | **الخريطة نعم، الماركب لا.** الرئيسية تبني `<a class="btn btn-outline-dark">`، وصفحة المنتجات تبني `<option>` داخل `<select>` للفرز. لا يشبه أحدهما الآخر | دالة `categoryEmoji()` في هيلبر. partial مشترك كان سيجبر ماركبين مختلفين على قالب واحد بشروط |
| `shared/rating-stars.php`: «ثلاثة مواضع في product_dit» | **موضعان** بسطر واحد متطابق (حلقة تطبع ★/☆)، والثالث **مختلف كلياً**: ودجت تقييم تفاعلي بكلاس `star-span` وحالة `active` وحقل مخفي | **لم أنفّذه.** سطر واحد مكرّر مرتين لا يبرّر ملفاً برأس توثيقي أطول منه |

### وبدلها: تكرار حقيقي لم تذكره الخطة

بنيتُ سكربت كشف تكرار (نافذة منزلقة على الـviews بعد تطبيع المسافات) بدل الاعتماد على
القائمة. أخرج ما يلي:

| المكرَّر فعلاً | الحجم |
|---|---|
| **كتلة رسائل الفلاش** في خمسة ملفات أدمن | 13 سطراً **متطابقة بايت-ببايت** × 5 |
| **حقل الهاتف** (منطق التقسيم + الماركب) في `account/my-info` و`admin/my-info` | 26 سطراً × 2 |
| **جدول الطلبات** في `admin/users/details` و`admin/manage-admins/details` | 33 سطراً × 2 |
| **صفّ الجدول الفارغ** في خمسة جداول أدمن | 4-6 أسطر × 5 |
| **بادج حالة الطلب** في أربعة ملفات أدمن | خريطة حالة→لون × 4 |
| **نمط رسالة التوست** في `admin/branding` و`product/product_dit` | كتلة `<script>` × 2 |

---

## 3. الـpartials السبعة

| الملف | يحلّ محلّ |
|---|---|
| `shared/order-cancel-button.php` | *(كان موجوداً)* |
| `shared/flash-messages.php` | خمس نسخ متطابقة |
| `shared/phone-input.php` | نسختان (منطق + ماركب) |
| `shared/order-status-badge.php` | أربع خرائط حالة→لون |
| `shared/table-empty-row.php` | خمسة صفوف «لا توجد سجلات» |
| `shared/admin-orders-table.php` | جدولان |
| `shared/flash-toast.php` | نسختان |

### ودالتان في الهيلبرز بدل partials

`categoryEmoji()` في `functions.php`، و`productTag()` في `product_tag_helper.php` الجديد —
كانت `getTag()` **دالة عامة معرَّفة داخل view**، لا تصلح للاستدعاء من view آخر، وتسقط
بـ`Cannot redeclare` لو عُرض الملف مرتين (وقد صار ذلك ممكناً بعد أن حوّلت المرحلة 4
`require_once` إلى `require`).

---

## 4. إخراج الأصول المضمّنة

### `checkout` — الأولوية التي حدّدتها الخطة، والسبب كان موثّقاً في الملف نفسه

تعليق داخل `css/store/pages/checkout.css` كان يشرح صراحةً أن كل قاعدة استجابية فيه
تحتاج `!important` لأنها تتصارع مع `<style>` مضمّن في جسم `checkout.php` — يُحمَّل بعد
الملف المربوط في `<head>` فيغلبه في أي تعادل. **عشرون `!important` سببها واحد.**

بعد الدمج: **20 ← 3**، والثلاث الباقية موروثة من الكتلة الأصلية نفسها (تغلب حدود
Bootstrap على عناصر الفورم) لا من الصراع.

**الدمج ليس نسخاً.** لكل خاصية متصارعة أبقيتُ الفائز **فعلياً** قبل التغيير:

| | ما كان مكتوباً في `checkout.css` | ما كان يظهر فعلاً |
|---|---|---|
| `.step-circle` خلفية | `var(--section-border)` | `var(--card-bg)` (من الكتلة المضمّنة) |
| `.step-circle` لون | `var(--muted-text)` | `var(--text-color)` |
| `.step-label` حجم | `0.8rem` | `.72rem` |
| `.step-item.active .step-label` | `var(--text-color)` | `var(--accent)` |

أي أن أربع قيم في ذلك الملف **لم يرها المستخدم قط**.

### بقية الملفات

| من | إلى | أسطر |
|---|---|---:|
| `checkout/checkout.php` | `js/features/checkout.js` | 157 |
| `admin/my-info.php` | `js/admin/my-info.js` | 144 |
| `admin/store-reauth.php` | `js/admin/store-reauth.js` | 63 |
| `auth/reset-password.php` | `js/features/reset-password.js` + `css/store/pages/reset-password.css` | 58 + 25 |
| `inc/footer.php` | `js/core/modal-input-colors.js` | 29 |
| `home.php` + `product/product.php` | `js/features/products-catalog.js` | 23 |
| `admin/inc/navbar.php` | `js/admin/admin-layout/admin-navbar.js` | 8 |
| `checkout/confirmation.php` | `css/store/pages/confirmation.css` | 7 |
| جدولا الأدمن | `css/admin/components/data-table.css` | 2 |

**تمرير البيانات بـ`data-*`** طُبِّق حيث كان الحقن حقيقياً: صفحة الدفع تمرّر أربع قيم
(URLROOT · توكن CSRF · مفتاح idempotency · العناوين) عبر سمات على `<main>` القائم — لا
غلاف جديد، فلا تغيير في بنية الـDOM.

---

## 5. سبعة أعطال اكتُشفت — كلها سابقة لعملي وأُعيد إنتاجها أولاً

### 5.1 معرّف `main-content` مكرَّر في **كل** صفحة متجر ✅ أُصلح

`inc/navbar.php` كان يطبع `<div id="main-content"></div>` فارغاً كمرساة لرابط «تخطَّ
إلى المحتوى» — بينما **كل views المتجر التسعة تعلن `<main id="main-content">` بنفسها**.
عنصران بنفس الـid في كل صفحة: HTML غير صالح، و`getElementById` يُرجع الـdiv الفارغ لا
المحتوى.

اكتشفتُه لأن سمات `data-*` لم تصل الـJS في صفحة الدفع. أعدتُ إنتاجه بـ`curl` على HTML
الصفحة قبل أي تعديل: معرّفان. حُذف الـdiv، ورابط التخطّي صار يصل `<main>` الحقيقي —
وهو المقصود منه أصلاً.

### 5.2 زر «تفعيل 2FA» لم يعمل ولا مرة منذ كُتب ✅ أُصلح

`AdminMyInfoController::generate2FASecret` تقرأ `$post['csrf_token']` و**`$post` غير
معرَّف في هذه الدالة**. الدوال الثلاث الأخرى في نفس الملف كلها تسبقه بـ
`$post = array_merge($_POST, $body);` وهذه وحدها لا. النتيجة: توكن فارغ دائماً، ورفض
دائم بـ«Invalid CSRF token» — والطلب يصل بجسم JSON فلا يملأ `$_POST` أصلاً.

**بعد الإصلاح، دورة حقيقية في المتصفح:** الزر يولّد secret · صورة QR تُحمَّل فعلاً من
الخادم الخارجي · المفتاح اليدوي يظهر · كود خاطئ يُرفض بـ«Invalid code» لا برسالة CSRF.
وجدول `admins` لم يتغيّر: `totp_enabled = 0` وبلا secret.

### 5.3 إعادة تعيين كلمة المرور تنهار إن حُذف الحساب ✅ أُصلح

`AuthController::resetPassword` كانت تفعل `(int)$user['id']` بلا فحص. صفّ
`password_resets` يبقى صالحاً بعد حذف صاحبه، فـ`$user` يصير `null` ويُطبع تحذير PHP
**قبل** جسم الاستجابة — فتصل الواجهة صفحة HTML لا JSON، ويسقط `json()` وتظهر رسالة
«Something went wrong» التي لا تصف شيئاً.

التقطتُ الاستجابة الخام فإذا هي `<br /><b>Warning…`، ووجدت في `php_error_log`:
`Trying to access array offset on value of type null … AuthController.php on line 467`.
الفرع الإداري كان مصاباً بنفس العلة.

**مدى الوصول بدقة:** `forgot()` لا تنشئ رمزاً إلا لبريد موجود، فالمسار لا يُبلَغ إلا إذا
حُذف الحساب بين طلب الرابط واستعماله — ضيّق لكنه حقيقي، واللوحة تتيح حذف المستخدمين.

### 5.4 زر إعادة التحقق يعرض كياناً حرفياً بعد أول فشل ✅ أُصلح

`btn.textContent = 'Verify &amp; Return';` — و`textContent` لا يفكّ كيانات HTML.
**أعدتُ إنتاجه في المتصفح قبل النقل**: بعد كلمة مرور خاطئة تحوّل نص الزر من
«Verify & Return» إلى «Verify &amp; Return».

### 5.5 صفحة الأدمن تطلب نفسها كصورة في كل تحميل ✅ أُصلح

`<img id="twofaQr" src="">` — المتصفح يحلّ `src=""` إلى عنوان الصفحة الحالية فيطلبه
كصورة ثم يفشل. طلب مهدور و404 في الـconsole على كل زيارة. حُذفت السمة؛ الصورة داخل
حاوية مخفية و`width/height` يحجزان مساحتها، وjs يضبط المصدر عند بدء الإعداد.

### 5.6 كل صور المنتجات في محرّر السلايدر مكسورة ✅ أُصلح لاحقاً

> **أُصلح في `850eab8`** بعد قرار صاحب المشروع. `BrandingModel` صار يمرّر
> `product_image_url` ناتجَ `fixImagePath()`، و`branding.js` يستهلكه مباشرة
> بلا بناء يدوي. تحقّقت في 2026-08-26: **6/6 صور تُخدَّم بـ200**.
> ما دون هذا السطر نصّ التقرير كما كُتب يوم الاكتشاف.

`admin/branding` يمرّر `window._existingSlidersData = json_encode($sliders)` — صفوف
الموديل خاماً، و`product_image_path` فيها اسم ملف عارٍ مثل `airpods.jpg`. الـJS يبني
`URLROOT + '/' + path` فيخرج `/airpods.jpg` بدل `/images/airpods.jpg` → **404**، ويسقط
على صورة بديلة «Image not found».

قِستُه: ست صور، كلها 404، وكلها تعرض البديل. الأدمن لا يرى أي منتج يشير إليه كل سلايد.

الهيلبر `fixImagePath()` يعالج هذه الحالة بالضبط («إن لم يكن في المسار شرطة مائلة فأضف
`images/`») — لكنه في PHP، وجانب JS يبني المسار بنفسه. **هذا مثال حيّ على ازدواج المنطق
بين PHP وJS الذي تستهدفه المرحلة 6.** لم أُصلحه لأنه في طبقة JS خارج نطاق هذه المرحلة،
والإصلاح قرار بين موضعين (تمرير المسار مُصلَحاً من الكنترولر، أو دالة مكافئة في JS).

### 5.7 كتلة فلاش ميتة في `admin/backup.php` ✅ حُذفت لاحقاً

> **حُذفت في `f3196aa`** بعد فحص أوسع: لا يوجد في المشروع أي تحويل إلى
> `/admin/backup` يضع رسالة عابرة، فالكتلة غير قابلة للوصول لا «نادرة».
> وُضع مكانها تعليق يشرح السبب كي لا يعيدها أحد ظنّاً أنها سهو.
> ما دون هذا السطر نصّ التقرير كما كُتب.

`BackupController` لا يمرّر `flashMsg` ولا `flashErr` إطلاقاً، فالكتلة لم تُنفَّذ ولا مرة
منذ كُتبت. تركتُ الاستدعاء الموحّد مكانها بدل حذفه: سطر واحد، والصفحة قد تحتاجه لاحقاً.

---

## 6. اختلاف مقصود تُرك خارج التوحيد ✅ صدر القرار ونُفِّذ

**`account/my-info.php` (صفحة الزبون) تعرض الطلب الملغى بـ`bg-secondary` رمادي، بينما
صفحات الأدمن الأربع كلها تعرضه بـ`bg-danger` أحمر.**

هذا اختلاف حقيقي فيما يراه المستخدم لا تفاوت صياغة. لم أوحّده: تغيير لون بادج في صفحة
الزبون قرار تصميم لا إعادة هيكلة، وخارج ما وُوفق عليه. الاختلاف موثَّق في رأس
`shared/order-status-badge.php` كي لا يضيع.

**القرار المطلوب:** أنوحّد على الأحمر (رأي الأغلبية 4:1)، أم يبقى الرمادي في صفحة
الزبون عن قصد (الطلب الملغى ليس «خطأ» بالنسبة له)؟

> **اختار صاحب المشروع التوحيد على الأحمر، ونُفِّذ في `850eab8`.**
> `'cancelled' => 'bg-danger'` في `shared/order-status-badge.php` وحده،
> وهو المصدر الوحيد للون في المشروع كله.
>
> تُحقِّق منه في 2026-08-26 **من الـHTML المُخدَّم لا من الكود** على أربع
> صفحات بطلب ملغى حقيقي: صفحة الزبون `/user/info` (`bg-danger ms-2`) ·
> قائمة الطلبات · تفاصيل الطلب (`bg-danger fs-6`) · تفاصيل المستخدم.
> **صفر `bg-secondary`** في الأربع، مع ضابط يثبت أن الكاشف كان سيلتقط
> الرمادي لو وُجد.

---

## 7. أخطاء ارتكبتُها أثناء العمل

**1. عدّاد الأصول المضمّنة خُدع بتعليقاتي — مرتين.**
كتبتُ تعليقاً يذكر `<style>` حرفياً مكان الكتلة المنقولة، فقفز العدّاد من 55 إلى **337**.
المرة الأولى كان تعليق PHP (أصلحتُه في توصيات المرحلة 4)، والثانية تعليق HTML لم يمسّه
ذلك الإصلاح. أضفتُ `blankHtmlComments()` — وسم داخل `<!-- -->` ليس أصلاً مضمّناً لأن
المتصفح لا ينفّذه.

**2. استبدال بـ`strpos` أصاب الكتلة الخطأ.**
سكربت لاستبدال جدول الطلبات في `admin/users/details.php` طابق **أول** `<div
class="table-responsive">` في الملف — والملف فيه أربعة. حذف الجدول الخطأ وترك المستهدف.
تراجعتُ بـ`git checkout --` وأعدتُها بالاستبدال على **نطاق أسطر محدَّد مع فحص الحدود**
قبل التنفيذ.

**3. مقارنة بصرية غير صالحة.**
لاختبار حالات شريط الخطوات في صفحة الدفع، حقنتُ الكتلة المضمّنة الأصلية في الصفحة
بعد الدمج — فأعطت **91 فرقاً كاذباً**. السبب: الحقن بلا الـ`!important`ات يعيد تركيبةً
لم توجد قط. أعدتُ القياس بتراجع `git` حقيقي فصار الفرق صفراً.

**4. اختبار وحدة بتوقّعات خاطئة.**
افترضتُ أن علامتَي الاقتباس حول مصطلح البحث تُهرَّبان إلى `&quot;`، وهما محرفان حرفيان
في نصّ PHP الأصلي لا يمرّان بـ`htmlspecialchars`. الكود كان مطابقاً للأصل والاختبار هو
الذي صُحِّح — لا العكس.

**5. قراءة سلبية من كاش المتصفح.**
أول قياس بعد نقل JS الرئيسية أظهر الصفحة **فارغة تماماً** — كاش لملف JS المعدَّل. بعد
`fetch(url, {cache:'reload'})` ظهر كل شيء سليماً. لا تُقرأ نتيجة سلبية بعد تعديل JS/CSS
قبل إبطال الكاش.

**6. تحقّق مرّ زائفاً.**
سكربت مقارنة قرأ ملفين من `/tmp` وطبع «✓ لا فرق» — والملفان **غير موجودين** أصلاً
(Git Bash وPHP يريان `/tmp` في مكانين مختلفين على ويندوز). أعدتُه بمسار صريح وبفحص
`if ($a === "" ) exit(1)` قبل المقارنة.

---

## 8. التحقق

| الفحص | النتيجة |
|---|---|
| سكربت الدخان — 44 راوت GET | ✅ 44/44 |
| `php -l` — كل ملفات المشروع | ✅ |
| `node --check` على ملفات JS الجديدة السبعة | ✅ |
| `audit-imports.php` | ✅ نظيف |
| `composer dump-autoload -o` | ✅ صفر تحذيرات · 920 كلاساً |
| `composer docs:generate` | ✅ صفر تحذيرات |
| **لقطة 44 راوت متجر** | ✅ متطابقة بعد استبعاد التغييرات المقصودة (الـdiv المحذوف والكتل المضمّنة) |
| **لقطة 20 راوت أدمن** (بمعرّفات سجلات حقيقية) | ✅ متطابقة عدا طابع «آخر نشاط» الذي يتغيّر بالتصفّح |
| **7 صفحات بجلسة مستخدم حقيقية** | ✅ متطابقة عدا مفتاح idempotency العشوائي |
| **مقارنة `getComputedStyle` — صفحة الدفع** | ✅ **أربعة عروض** (1280 · 768 · 375 · 320) × **حالتين** (الخطوة الأولى مفعّلة، ثم الأولى `done` والثانية `active`) → صفر فرق على 487 عنصراً في كل قياس، على 34 خاصية زائد `::before`/`::after` وأبعاد الصندوق |
| **مقارنة `getComputedStyle` — إعادة التعيين** | ✅ 1280 و375 → صفر فرق على 21 عنصراً |
| **اختبار وحدة: بادج الحالة** | ✅ 13 تأكيداً — أربع حالات × ثلاث صيغ استدعاء، مقارَنة بالنص الحرفي للنسخ المحذوفة |
| **اختبار وحدة: بادج المخزون** | ✅ 17 تأكيداً — سبع قيم × وضعَي الوسيط. وتحقّقت أن العمودين `unsigned` فلا حالة مخزون سالب |
| **اختبار وحدة: صفّ الجدول الفارغ** | ✅ 8 تأكيدات، منها مصطلح بحث خبيث يبقى مُهرَّباً |
| **اختبار وظيفي: رسائل الفلاش** | ✅ الصفحات الخمس، برسالتين تحويان `<b>` و`&` و`<script>` — مخرجات متطابقة حرفياً قبل/بعد |
| **دورة الدفع الثلاثية في المتصفح** | ✅ 1→2→3→2→1، وحالات شريط الخطوات صحيحة، والملخّص يبني السلة والمجموع والعنوان وطريقة الدفع |
| **اعتراض `fetch` لزر تنفيذ الطلب** | ✅ POST إلى `/checkout` بـ`application/json` والمفاتيح الخمسة نفسها — بلا إنشاء طلب حقيقي |
| **اختبار HTTP: توكن من `data-*`** | ✅ الخادم يقبله (يتجاوز CSRF ويُرفض عند العنوان)، وتوكن خاطئ يُرفض. صفر طلب أُنشئ |
| **دورة إعادة تعيين كلمة مرور كاملة** | ✅ بمستخدم حقيقي مؤقت: الهاش تغيّر · `password_verify` تقبل الجديدة · الرمز استُهلك. ثم حُذف المستخدم |
| **دورة 2FA** | ✅ توليد QR حقيقي · كود خاطئ يُرفض برسالته الصحيحة · الإلغاء يعيد الحالة |
| **الرئيسية وصفحة المنتجات** | ✅ السلايدر شريحتان، والأقسام الثلاثة 7 منتجات لكلٍّ · 9 أزرار مفضّلة، والنقر يستدعي `toggleWishlist`، والقلب يمتلئ للمحفوظ |
| **الجداول الفارغة حيّة** | ✅ بحث لا يطابق شيئاً على users و orders و products، و backup بلا نسخ |

### ما لم أستطع تأكيده

**ظهور التوست تلقائياً بعد التنقّل.** مؤقّته ثلاث ثوانٍ، ولوحة المتصفح مخفية في هذه
البيئة فتُبطّئ تنفيذ JS — وصل قياسي بعد **148 ثانية** من تحميل الصفحة. تحقّقت من
الأجزاء الثلاثة منفصلة (طباعة العنصر بسماته مُهرَّبة · تحميل ملف JS · عمل `showToast`)
ومن منطق الملف مطبَّقاً على عناصر الصفحة الحقيقية. أذكره صراحةً ولا أدّعي ما لم أفعله.

### بيانات الاختبار — نُظّفت كلها

| ما أُنشئ | الحالة |
|---|---|
| مستخدم مؤقت (`phase5-tempuser`) | ✅ حُذف |
| صفوف `password_resets` | ✅ صفر |
| صفوف `admin_audit_log` من محاولات إعادة تحقق فاشلة | ✅ صفر |
| جلسات مزوّرة (مستخدم وأدمن) | ✅ صفر |
| `localStorage` في المتصفح (سلة، مفضّلة، لقطات القياس) | ✅ نُظّف |
| رسائل فلاش حُقنت في جلسة اختبار | ✅ أُزيلت |

---

## 9. القياس التراكمي منذ البداية

| المؤشر | البداية | الآن |
|---|---:|---:|
| استعلامات SQL في الكنترولرز | 14 | **0** ✅ |
| وصول DB من الـviews | — | **0** ✅ |
| حُرّاس `function_exists()` زائدة | 16 | **0** ✅ |
| كود ميت في `Core` | 2 | **0** ✅ |
| تحذيرات PSR-4 من composer | 48 | **0** ✅ |
| كنترولرز بلا توثيق OpenAPI | 10 | **0** ✅ |
| نسخ `respond()` المكرّرة | 16 | **1** ✅ |
| صفحات تكتب `<!DOCTYPE>` بيدها | 3 | **0** ✅ |
| نسخ منطق تجميع الـlayout | 3 | **1** ✅ |
| **أسطر `<style>` مضمّنة** | **55** | **0** ✅ |
| **partials في `views/shared`** | **1** | **7** ✅ |
| **أسطر `<script>` مضمّنة** | **579** | **91** (بيانات فقط) ✅ |
| **أعطال مكتشفة** | — | **17** |

---

## 10. ما يليه

**المرحلة 6 — الـJS**: 5,592 سطراً عبر 35 ملفاً (كان 4,948 عبر 28؛ الزيادة كلها منقولة
من الـviews في هذه المرحلة). **لم تُدقَّق بعمق بعد.**

ثلاثة خيوط جاهزة تسلّمها هذه المرحلة للمرحلة 6:

1. **ازدواج منطق مسارات الصور بين PHP وJS** — العطل 5.6 مثال حيّ عليه: `fixImagePath()`
   موجودة في PHP وحدها، وكل مكان يبني مسار صورة في JS يعيد اختراعها أو يخطئ.
2. ~~**`fetchWithCsrfRetry` لا تدعم أجسام JSON**~~ — **أُنجز.** وُسِّعت الدالة في
   المرحلة 6ب-1، وتبيّن أنها لم تكن **تتجاهل** JSON بل **تفسده**. ثم حُوِّل
   `admin/my-info` في 6ب-2، وصفحة الدفع في بند 6.4 بعدها.
3. ~~`js/features/account.js` غير محمَّل من أي مكان~~ — **هذا البند خاطئ، صُحِّح في
   تدقيق المرحلة 6.** الملف يحمّله `MyInfoController` عبر `extraScripts`. بنيتُ
   الاستنتاج على قائمة فوتر المتجر وحدها وأغفلتُ آلية extraScripts التي استعملتُها
   أنا نفسي في هذه المرحلة. الفحص الكامل للـ35 ملفاً: **صفر ملف JS غير محمَّل.**

~~**وقرار ينتظرك:** لون بادج الطلب الملغى في صفحة الزبون (القسم 6).~~
**صدر القرار — التوحيد على الأحمر — ونُفِّذ. لم يبقَ بند مفتوح في هذا التقرير.**

لمتابعة ما جرى بعد هذه المرحلة:
[CLEANUP-REPORT-PHASE-6.md](CLEANUP-REPORT-PHASE-6.md) ثم
[CLEANUP-REPORT-POST-PHASE-6.md](CLEANUP-REPORT-POST-PHASE-6.md).

---

## 11. الأدوات

```bash
php scripts/smoke-test.php        # 44 راوت + صياغة المعدَّل
php scripts/smoke-test.php --lint-all
php scripts/audit.php             # قياس حالة الكود
php scripts/audit-imports.php     # كلاسات غير مستوردة + استيرادات ميتة
php scripts/audit-escaping.php    # تصنيف مخرجات الـviews أمنياً
composer docs:generate
composer dump-autoload -o
```
